[
  {
    "question": "Production data shows the agent skips get_customer and calls lookup_order using only the customer's stated name, causing misidentified accounts. What change most effectively addresses this?",
    "options": [
      "Add a programmatic prerequisite blocking lookup_order/process_refund until get_customer returns a verified customer ID",
      "Enhance the system prompt to state get_customer is mandatory",
      "Add few-shot examples showing the agent always calls get_customer first",
      "Implement a routing classifier to enable only appropriate tools per request type"
    ],
    "answer": 0,
    "topics": [
      "Domain 1"
    ]
  },
  {
    "question": "Agent frequently calls get_customer instead of lookup_order because both tools have minimal descriptions. What is the most effective first step?",
    "options": [
      "Add few-shot examples demonstrating correct tool selection",
      "Expand each tool's description to include input formats, example queries, edge cases, and boundaries",
      "Implement a routing layer that parses input and pre-selects tools",
      "Consolidate both tools into a single lookup_entity tool"
    ],
    "answer": 1,
    "topics": [
      "Domain 2"
    ]
  },
  {
    "question": "Agent achieves 55% first-contact resolution and escalates straightforward cases. What's the most effective way to improve escalation calibration?",
    "options": [
      "Add explicit escalation criteria to the system prompt with few-shot examples",
      "Have the agent self-report confidence and route low scores to humans",
      "Deploy a separate classifier model trained on historical tickets",
      "Use sentiment analysis to escalate on negative sentiment"
    ],
    "answer": 0,
    "topics": [
      "Domain 1"
    ]
  },
  {
    "question": "Where should a project-scoped custom /review slash command be stored so it's available to every developer who clones the repo?",
    "options": [
      "In the .claude/commands/ directory in the project repository",
      "In ~/.claude/commands/ in each developer's home directory",
      "In the CLAUDE.md file at the project root",
      "In a .claude/config.json file with a commands array"
    ],
    "answer": 0,
    "topics": [
      "Domain 3"
    ]
  },
  {
    "question": "Restructuring a monolith into microservices requires exploring dependencies and service boundaries. Which approach should you take?",
    "options": [
      "Enter plan mode to explore the codebase and design an implementation approach before making changes",
      "Start with direct execution and make changes incrementally",
      "Use direct execution with comprehensive upfront instructions",
      "Begin in direct execution and switch to plan mode if complexity appears"
    ],
    "answer": 0,
    "topics": [
      "Domain 3"
    ]
  },
  {
    "question": "Test files are spread across the codebase and you want Claude to apply conventions automatically. What's the most maintainable way?",
    "options": [
      "Create rule files in .claude/rules/ with YAML frontmatter specifying glob patterns",
      "Consolidate all conventions in the root CLAUDE.md file",
      "Create skills in .claude/skills/ for each code type",
      "Place separate CLAUDE.md in each subdirectory"
    ],
    "answer": 0,
    "topics": [
      "Domain 3"
    ]
  },
  {
    "question": "A coordinator decomposed \"creative industries\" too narrowly into only visual arts, missing music and film. What is the most likely root cause?",
    "options": [
      "Synthesis agent lacks instructions for identifying coverage gaps",
      "Coordinator's task decomposition is too narrow, omitting relevant domains",
      "Web search agent's queries are not comprehensive",
      "Document analysis agent filters out non-visual sources"
    ],
    "answer": 1,
    "topics": [
      "Domain 1"
    ]
  },
  {
    "question": "A web search subagent times out. Which error propagation approach best enables intelligent recovery?",
    "options": [
      "Return structured error context including failure type, attempted query, partial results, and alternatives",
      "Implement automatic retry with exponential backoff and return a generic status after retries",
      "Catch the timeout and return an empty result set marked as successful",
      "Propagate the timeout to a top-level handler that terminates the workflow"
    ],
    "answer": 0,
    "topics": [
      "Domain 2"
    ]
  },
  {
    "question": "Synthesis agent requires frequent simple verifications adding latency. How reduce overhead while maintaining reliability?",
    "options": [
      "Give the synthesis agent a scoped verify_fact tool for simple lookups and keep coordinator for complex cases",
      "Have the synthesis agent batch verification needs and send them at the end",
      "Give synthesis agent access to all web search tools to handle verifications directly",
      "Have web search agent proactively cache extra context anticipating synthesis needs"
    ],
    "answer": 0,
    "topics": [
      "Domain 1"
    ]
  },
  {
    "question": "Claude Code hangs in CI waiting for interactive input. What's the correct way to run Claude Code non-interactively?",
    "options": [
      "Add the -p flag (claude -p \"Analyze this pull request\")",
      "Set CLAUDE_HEADLESS=true before running",
      "Redirect stdin from /dev/null",
      "Add a --batch flag"
    ],
    "answer": 0,
    "topics": [
      "Domain 3"
    ]
  },
  {
    "question": "You want to save costs: should you switch both a blocking pre-merge check and an overnight report to the Message Batches API?",
    "options": [
      "Use batch processing for the overnight report only; keep real-time calls for pre-merge checks",
      "Switch both workflows to batch processing with polling",
      "Keep real-time calls for both workflows",
      "Switch both to batch with timeout fallback to real-time"
    ],
    "answer": 0,
    "topics": [
      "Domain 4"
    ]
  },
  {
    "question": "A single-pass review of a 14-file PR yields inconsistent results. How should you restructure the review?",
    "options": [
      "Split into focused per-file passes, then a separate integration pass for cross-file issues",
      "Require developers to split large PRs into smaller submissions",
      "Switch to a higher-tier model with a larger context window",
      "Run three independent review passes and flag issues found in at least two runs"
    ],
    "answer": 0,
    "topics": [
      "Domain 4"
    ]
  },
  {
    "question": "Your customer support agent uses an agentic loop. After each model response, what is the correct primary signal to decide whether the loop should iterate again or terminate?",
    "options": [
      "Whether the assistant's text content contains a phrase like \"I'm done\" or \"resolved\"",
      "Whether the response's stop_reason is \"tool_use\" (continue) or \"end_turn\" (terminate)",
      "Whether the loop has reached a maximum iteration count configured by the developer",
      "Whether any tool result returned an error code, which should always halt the loop"
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Wrong — parsing assistant text for 'done'/'resolved' is brittle (locale- and model-version-sensitive). A tool-calling turn may include narrative text before its tool_use block, so matching on text terminates early and silently drops the actual action.",
      "Correct — stop_reason is the canonical signal. 'tool_use' means Claude is requesting a tool execution and the loop must continue (run tools, append tool_result blocks, re-send). 'end_turn' means Claude has finished and the loop terminates.",
      "Wrong — iteration caps are a circuit-breaker, not a control mechanism. Using a cap as the primary stopper truncates legitimate multi-step tasks mid-flight while still letting broken loops burn the full budget.",
      "Wrong — tool errors should be returned to the model as tool_result blocks so it can react (retry with different params, escalate, give up gracefully). Halting the loop on error is a category mistake."
    ]
  },
  {
    "question": "A developer reports that their multi-agent research loop occasionally terminates while Claude was still mid-task. Inspecting the implementation, the loop exits as soon as the assistant emits any non-empty text block. What is the most accurate diagnosis?",
    "options": [
      "Claude's tool-use turns can also include narrative text before or alongside tool_use blocks; using text presence instead of stop_reason causes premature termination",
      "The model is hallucinating an end_turn signal; the fix is to lower the temperature and re-prompt for tool calls",
      "Tool results are not being appended to history, so the model produces text instead of tool calls",
      "The loop is missing an iteration cap, so the runaway is being killed by a downstream timeout"
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Correct — Claude's tool_use turns can include narrative text *before* or alongside the tool_use block. Using `len(text) > 0` as a termination check misses that the actual stop_reason is still 'tool_use', so the loop exits early and the requested tool is never executed.",
      "Wrong — there's no 'hallucinated end_turn'; stop_reason is set deterministically by the API. Lowering temperature is unrelated to the structural bug here.",
      "Wrong — failing to append tool results causes loops or hallucinated outcomes, not premature termination. The symptom described is exit-on-text, not skipped-tool-call.",
      "Wrong — a missing iteration cap allows runaway loops; it does not cause early termination. Caps are circuit-breakers, not the primary signal."
    ]
  },
  {
    "question": "You're building an agentic loop for a refund-processing workflow. Between iterations, after executing the tools Claude requested, what must be appended to the conversation history before the next request?",
    "options": [
      "A summarized natural-language description of what the tools did, so the context stays compact",
      "Only the tools that returned successfully — failed tool calls should be omitted to avoid confusing the model",
      "Tool result blocks (each correlated by tool_use_id to its corresponding tool_use) so the model can reason over the actual returned data on the next iteration",
      "A developer-authored \"system\" message instructing Claude on what to do next based on the tool outputs"
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Wrong — summarising tool results into prose loses the structured data Claude needs to reason. The model may re-call tools or hallucinate outcomes when it can't see the actual returned data.",
      "Wrong — failed tool calls must be reported back as tool_result blocks too. Hiding failures means the model can't react (retry, escalate, give up); it just sees the tool_use 'disappear' and may re-call it.",
      "Correct — tool result blocks correlated by tool_use_id let Claude match what it asked for to what came back. This is the canonical mechanism that keeps the loop's causal trail intact.",
      "Wrong — developer-authored 'system' messages between iterations bypass model-driven decision-making. The model should reason from the actual tool outputs, not from your hand-crafted instructions about them."
    ]
  },
  {
    "question": "A team has hard-coded their support agent to always call get_customer, then lookup_order, then process_refund in a fixed sequence, treating Claude as a step within the pipeline rather than the controller. Which limitation most directly results from this design choice?",
    "options": [
      "The pipeline cannot use the Anthropic API's tool_use stop_reason because fixed sequences require manual control",
      "Claude can no longer adapt to requests that don't fit the sequence (e.g., a status inquiry that needs no refund) and cannot reorder or skip calls based on intermediate results",
      "Tool results no longer need to be appended to history, eliminating the loop's main reliability issue",
      "The system will fail any request that exceeds the configured iteration cap, even simple ones"
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Wrong — fixed sequences don't prevent using stop_reason; they just don't take advantage of it. The Anthropic API doesn't reject anything based on whether you scripted the pipeline.",
      "Correct — model-driven decision-making is the defining property of an agentic loop. Hard-coding the sequence forces every request through the full path (status inquiries walk through refund), prevents reordering or skipping based on intermediate findings, and removes adaptability.",
      "Wrong — appending tool results to history is required regardless of whether the pipeline is scripted or model-driven. The two concerns are independent.",
      "Wrong — iteration caps are independent of pipeline rigidity. A scripted pipeline may run within or above any configured cap without the cap being the failure mode described."
    ]
  },
  {
    "question": "A junior engineer proposes \"making the agentic loop safer\" by capping it at 10 iterations and treating that cap as the main termination mechanism. Why is this an anti-pattern, even though the cap itself isn't harmful as a safety net?",
    "options": [
      "Iteration caps cause the Anthropic API to reject subsequent requests once the limit is hit",
      "The cap will be exceeded by any non-trivial agentic task, so it provides no real safety",
      "Caps as the primary stopper truncate legitimate multi-step tasks mid-flight while still letting broken loops burn the full budget; stop_reason should be the primary signal and the cap only a circuit-breaker",
      "stop_reason is not reliable enough to drive termination, so the cap must be the primary signal in production systems"
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Wrong — the API doesn't track or reject based on your application's iteration count. Caps are an entirely client-side construct.",
      "Wrong — many legitimate agentic tasks fit within 10 iterations; the issue is making the cap *primary*, not the cap value itself.",
      "Correct — caps as the *primary* stopper truncate legitimate multi-step tasks mid-flight while still letting broken loops burn the full N iterations. stop_reason should drive iteration; the cap is a circuit-breaker that fires only when stop_reason has been wrong for too long.",
      "Wrong — stop_reason is the deterministic signal Claude emits to indicate continuation vs termination. It is exactly the signal you should rely on; the cap is the safety net."
    ]
  },
  {
    "question": "In an agentic loop built on the Claude Agent SDK, which value of `stop_reason` indicates that your code should execute the requested tools and continue iterating?",
    "options": [
      "\"end_turn\"",
      "\"tool_use\"",
      "\"max_tokens\"",
      "\"stop_sequence\""
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "pre",
    "explanations": [
      "Wrong — `\"end_turn\"` means Claude has finished its turn; the loop terminates, it does not iterate.",
      "Correct — `\"tool_use\"` means Claude has emitted one or more tool_use blocks and is asking your code to execute them; the loop must continue (run tools, append tool_result blocks, re-send).",
      "Wrong — `\"max_tokens\"` means the response was truncated by the token budget. It signals an incomplete answer, not a tool execution request.",
      "Wrong — `\"stop_sequence\"` means generation hit a configured stop string. It is a termination cause, not a tool-execution cue."
    ]
  },
  {
    "question": "After your loop executes the tools Claude requested in a given turn, what does Claude need to see on the *next* request so it can reason over the actual outputs?",
    "options": [
      "Tool result blocks appended to the conversation history (each correlated by tool_use_id)",
      "A natural-language summary of what the tools did",
      "Only the tool results that succeeded; failures should be omitted",
      "Nothing — Claude infers tool outcomes from its own prior reasoning"
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "pre",
    "explanations": [
      "Correct — tool_result blocks correlated by tool_use_id let Claude match each result to the call that asked for it. This is the canonical mechanism that keeps the causal trail intact between iterations.",
      "Wrong — summarising tool outputs into prose loses the structured data Claude needs to reason. The model may re-call the tool or hallucinate the outcome when it cannot see the actual fields.",
      "Wrong — failed calls must be reported back as tool_result blocks too, so the model can react (retry with different params, escalate, give up gracefully). Hiding failures looks like the call vanished.",
      "Wrong — Claude does not 'remember' that it asked for a tool call across requests; without an appended tool_result on the next request, from the model's perspective the call never produced any output."
    ]
  },
  {
    "question": "Which statement most accurately describes \"model-driven decision-making\" in an agentic loop?",
    "options": [
      "A separate classifier model picks the next tool before each iteration",
      "Claude chooses the next tool at each step based on the accumulated conversation context",
      "Tools are always invoked in parallel by default",
      "A pre-defined decision tree routes between tools deterministically"
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "pre",
    "explanations": [
      "Wrong — there is no separate classifier model in an agentic loop; that's a routing pipeline pattern, not the loop itself.",
      "Correct — the defining property of an agentic loop is that Claude picks the next tool from accumulated context at each step. Your code is a transport, not a controller.",
      "Wrong — parallel tool calls are possible (multiple tool_use blocks in one turn) but are not the default and are not what 'model-driven' means.",
      "Wrong — pre-defined decision trees are scripted pipelines, the *opposite* of model-driven decision-making. They remove Claude's ability to adapt to intermediate findings."
    ]
  },
  {
    "question": "Your loop receives a model response with `stop_reason: \"end_turn\"`. What is the correct loop behavior?",
    "options": [
      "Append the response to history and immediately re-send to keep Claude reasoning",
      "Force a tool call by switching tool_choice to \"any\" and re-sending",
      "Terminate the loop — Claude has finished its turn",
      "Increase the iteration cap and try again"
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "pre",
    "explanations": [
      "Wrong — `\"end_turn\"` means Claude has finished. Re-sending the same conversation would either get an identical end_turn back or push the model into making something up.",
      "Wrong — forcing a tool call after Claude has decided to end the turn fights the model. If you genuinely need a tool call you have a prompting/design problem upstream, not a tool_choice problem.",
      "Correct — `\"end_turn\"` is the canonical signal that the loop is done. Terminate.",
      "Wrong — iteration caps are circuit-breakers, unrelated to `\"end_turn\"`. There's nothing to retry; Claude has finished."
    ]
  },
  {
    "question": "A production agent occasionally exits while Claude is still mid-task. The loop is implemented as: \"break out of the loop as soon as the assistant emits any non-empty text block.\" What is the most likely root cause?",
    "options": [
      "Claude's tool-using turns can include narrative text alongside tool_use blocks, so text presence is not a reliable termination signal — the canonical signal is stop_reason",
      "The model's temperature is too high and is hallucinating end_turn",
      "Tool results are being appended in the wrong order in history",
      "The Anthropic API is dropping the final tool_use block when responses are long"
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "pre",
    "explanations": [
      "Correct — a tool_use turn can include narrative text *before* its tool_use block (e.g., 'I'll look up the order'). Treating any non-empty text as termination terminates legitimate multi-step turns; stop_reason is the canonical signal.",
      "Wrong — there is no 'hallucinated end_turn'. stop_reason is set deterministically by the API based on what the response actually contains. Temperature is unrelated.",
      "Wrong — wrong-order tool_results cause confused reasoning, but the symptom described (early exit) is structurally about *what triggers exit*, not result ordering.",
      "Wrong — the API does not silently drop tool_use blocks. The bug is in the loop's termination heuristic, not the response payload."
    ]
  },
  {
    "question": "A customer support agent reliably handles refunds but consistently fails to call `escalate_to_human` even when needed. Inspecting the trace, Claude does emit text like \"I'll escalate this case\" right before what should be the escalation tool call — but the loop has already exited by then. What is the bug?",
    "options": [
      "The escalate_to_human tool's description is too short; rewrite it to win selection",
      "tool_choice should be set to {\"type\": \"tool\", \"name\": \"escalate_to_human\"} on every turn",
      "The agent doesn't have permission to call escalate_to_human and is silently failing",
      "The loop is using assistant text content (e.g., \"escalate\") as the termination signal instead of stop_reason, so it terminates on the narrative preamble before the tool call"
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "pre",
    "explanations": [
      "Wrong — escalate_to_human's description is fine; the loop is exiting before that tool ever gets called. Improving descriptions doesn't help if execution never reaches them.",
      "Wrong — forcing a specific tool every turn breaks every other case (refunds, status checks). The bug is termination, not tool selection.",
      "Wrong — silent permission failures show up as tool errors, not as loops that exit before the call is made. The trace shows narrative followed by exit, not a denied call.",
      "Correct — the loop is using assistant text as the termination signal, so the model's preamble ('I'll escalate this case') trips the heuristic and the loop exits before the tool_use block is even returned. Use stop_reason instead."
    ]
  },
  {
    "question": "A team hard-codes their support agent as a fixed pipeline: `get_customer` → `lookup_order` → `process_refund`. They report that even simple status-only inquiries walk the full sequence and that the agent can't adapt when, say, the order doesn't exist. What's the right structural fix?",
    "options": [
      "Add an extra \"is_status_inquiry\" prerequisite step at the start of the pipeline",
      "Replace the fixed pipeline with an agentic loop and let Claude pick tools from context based on stop_reason",
      "Increase model temperature so the agent is more creative within the pipeline",
      "Add few-shot examples showing the pipeline being used end-to-end"
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "pre",
    "explanations": [
      "Wrong — adding a fourth scripted prerequisite step doesn't fix rigidity; it adds another fixed step. The whole pattern is the problem.",
      "Correct — model-driven decision-making is the structural answer. Letting Claude pick tools from context based on stop_reason is what enables skipping irrelevant steps and adapting to missing data.",
      "Wrong — temperature changes generation creativity within a fixed pipeline; it does nothing to remove the pipeline itself or restore tool-selection adaptability.",
      "Wrong — few-shot examples don't help when the architecture forces every request through the same path. The agent isn't picking poorly; it isn't picking at all."
    ]
  },
  {
    "question": "Which of the following is an *anti-pattern* when designing an agentic loop?",
    "options": [
      "Continuing the loop while `stop_reason == \"tool_use\"`",
      "Appending tool_result blocks (with their tool_use_id) before the next request",
      "Using a heuristic on the assistant's text content as the primary termination signal",
      "Letting Claude pick the next tool from accumulated context"
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "pre",
    "explanations": [
      "Wrong — this is the correct continuation rule. `\"tool_use\"` means Claude is requesting tool execution and the loop must iterate.",
      "Wrong — appending tool_result blocks correlated by tool_use_id is exactly what keeps the causal chain intact. This is required behavior.",
      "Correct — text-content heuristics for termination are brittle (locale- and model-version-sensitive) and bypass the canonical stop_reason signal. The exam treats this as a textbook anti-pattern.",
      "Wrong — letting Claude pick tools from context is the defining property of an agentic loop and is exactly the right design."
    ]
  },
  {
    "question": "Which statement about iteration caps in a well-designed agentic loop is correct?",
    "options": [
      "An iteration cap should be the primary termination mechanism; stop_reason is unreliable",
      "An iteration cap is a circuit-breaker / runaway-safety net; stop_reason is the primary termination signal",
      "Iteration caps must be set so low that they fire on every multi-step task",
      "Iteration caps are equivalent to stop_reason and either can be used interchangeably"
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "pre",
    "explanations": [
      "Wrong — caps as the primary stopper truncate legitimate multi-step tasks while still letting broken loops burn the full N. stop_reason is the deterministic signal designed for this purpose.",
      "Correct — the cap is a safety net for runaway behavior; stop_reason drives normal termination. Both can coexist; neither replaces the other.",
      "Wrong — setting the cap so low it fires on every task means it is not a circuit-breaker, it is a guillotine. Caps should be roomy enough to never fire on healthy traces.",
      "Wrong — caps and stop_reason are not interchangeable. stop_reason is what Claude tells you about the turn; the cap is what you tell yourself about runaway protection."
    ]
  },
  {
    "question": "A developer drops failed tool_result blocks from history \"to avoid confusing the model.\" Why is this problematic?",
    "options": [
      "The model needs to see the failure to react intelligently — retry with different params, escalate, or give up gracefully; hiding failures makes the agent look like it's ignoring tools",
      "It violates Anthropic's pricing terms and can result in throttling",
      "It causes the API to throw 429 errors on the next request",
      "It only matters when the same tool fails twice in a row"
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "pre",
    "explanations": [
      "Correct — Claude needs to see failures in context to react: retry with different params, choose a different tool, escalate, or give up gracefully. Hiding failures makes the model behave as if the call never happened, leading to repeated calls or hallucinated outcomes.",
      "Wrong — there is no Anthropic pricing or throttling penalty for omitting tool_result blocks. The failure mode is correctness, not billing.",
      "Wrong — the API does not return 429 for omitted tool_result blocks. 429 is rate limiting, unrelated to message-shape choices.",
      "Wrong — the problem manifests on the very first failure: Claude can't react to a failure it cannot see, regardless of how often it recurs."
    ]
  },
  {
    "question": "Your refund agent receives a response with `stop_reason: \"tool_use\"` containing **two** tool_use blocks in the same turn — `lookup_order` and `check_loyalty_status` (Claude has emitted parallel tool calls). What is the correct loop behavior?",
    "options": [
      "Execute only the first tool_use block, append its tool_result, and re-send — loops should always step one tool at a time",
      "Choose whichever tool the assistant text mentions first, execute that, and discard the other",
      "Execute both tool calls, append both tool_result blocks (each correlated by its own tool_use_id), and re-send",
      "Reject the response and re-prompt with `tool_choice: {\"type\": \"tool\", \"name\": \"lookup_order\"}` to force a single call"
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Wrong — agentic loops do not require one-tool-at-a-time stepping. When the model emits multiple tool_use blocks in a single turn, all of them are intentional parallel calls and all must be executed before the next request.",
      "Wrong — picking based on assistant text fragments is exactly the brittle heuristic the loop must avoid. The tool_use blocks are the canonical instruction; honor both.",
      "Correct — execute every tool_use block in the turn and append a tool_result for each, correlated by its own tool_use_id. The next request then carries Claude's full context for both calls and it can synthesize accordingly.",
      "Wrong — forcing a single tool_choice override fights the model's deliberate parallel selection. Parallel calls are a feature, not an error to reject."
    ]
  },
  {
    "question": "An engineer's loop strips `tool_use_id` from tool_result blocks before appending them to history, claiming \"it shrinks the payload.\" Over long sessions the agent starts behaving as if previous tools never ran. What is the structural diagnosis?",
    "options": [
      "tool_use_id is what correlates a tool_result back to its originating tool_use; without it Claude cannot match results to its prior calls and effectively reasons as if the calls didn't happen",
      "Removing the id makes the message too short, so the model falls back to its training data for completions",
      "tool_use_id is required by the Anthropic SDK only; the model itself doesn't consume it",
      "stop_reason is computed from the presence of tool_use_id, so dropping it returns the wrong stop_reason"
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Correct — tool_use_id is the correlation key between a tool_use block and its tool_result. Without it Claude cannot match results to its prior calls; over a long session it loses the ability to reason about which result came from which call, so it behaves as if the calls never produced output.",
      "Wrong — falling back to training data is not how the model handles missing correlation; the failure mode here is concrete (lost causal mapping in this session's history), not training-data drift.",
      "Wrong — tool_use_id is consumed by the model's reasoning, not just by the SDK. The model uses it to thread results to calls; dropping it confuses reasoning, not just bookkeeping.",
      "Wrong — stop_reason is computed from the response's structure (presence of tool_use blocks, end_turn signal, etc.), not from tool_use_id on prior tool_results."
    ]
  },
  {
    "question": "An agent produces correct results 90% of the time but in failing traces, Claude appears to \"ignore\" the result of a tool it just called and re-issues the same tool a second time on the next iteration. Which loop-level cause best explains this pattern?",
    "options": [
      "Temperature was too high on that request",
      "tool_choice was set to \"any\" instead of \"auto\"",
      "The model exceeded its token budget, so the result was implicitly discarded",
      "The loop forgot to append the tool_result block for that call before sending the next request, so from Claude's perspective the call never produced output"
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Wrong — temperature changes creativity; it doesn't cause the model to forget that it just received a tool_result. The pattern is structural, not stochastic.",
      "Wrong — `tool_choice: \"any\"` forces *some* tool call but does not remove existing tool_results from history. The pattern of re-issuing the same tool points to missing history, not forced selection.",
      "Wrong — token-budget overflow truncates with `stop_reason: \"max_tokens\"`, surfacing visibly. It does not silently discard prior tool_results from the next request.",
      "Correct — if a tool_result is never appended, the next request shows Claude a tool_use with no matching result. From the model's perspective the call never produced output, so it dutifully re-issues it. Always append a tool_result for every tool_use before the next request."
    ]
  },
  {
    "question": "Which of the following workflows is best described as an *agentic loop* rather than a *scripted pipeline*?",
    "options": [
      "Code calls Claude with a fixed prompt template, parses the text response with regex, then deterministically calls tool A, then re-prompts with another fixed template, then calls tool B",
      "Code sends a request, inspects stop_reason; if `\"tool_use\"`, executes the requested tools, appends tool_result blocks, and re-sends — repeating until `\"end_turn\"`",
      "A shell script that runs `claude_call.sh` followed by `tool_a.sh` followed by `tool_b.sh` in a fixed order",
      "A workflow engine where each node is a hard-coded prompt and edges are pre-defined transitions"
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Wrong — this is a scripted pipeline: fixed prompt template, regex parsing, deterministic ordering. Claude is reduced to a step in your code's logic, not the controller.",
      "Correct — this is the canonical agentic loop: send, inspect stop_reason, on `\"tool_use\"` execute and re-send with appended results, terminate on `\"end_turn\"`. The model picks each tool from context.",
      "Wrong — sequential shell scripts with hard-coded ordering are workflow automation, not agentic. There is no model-driven decision-making, just fixed dependencies.",
      "Wrong — workflow engines with pre-defined nodes and edges are explicitly *not* agentic. The structure of the graph encodes the decisions, leaving the model with no control."
    ]
  },
  {
    "question": "A developer reports their refund-handling loop runs `process_refund` correctly but then keeps issuing additional tool_use turns instead of producing a final confirmation with stop_reason \"end_turn\". What is the most useful *loop-layer* debugging step?",
    "options": [
      "Lower the model's temperature to push it toward terminating",
      "Add explicit \"stop now\" instructions to the system prompt",
      "Inspect the message history to confirm that for every prior tool_use, a matching tool_result block (correlated by tool_use_id) was appended before the next request",
      "Cap iterations at 3 to force termination"
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Wrong — temperature affects creativity, not whether the model perceives the task as complete. The loop layer is what builds Claude's perception of state via the message history.",
      "Wrong — system-prompt 'stop now' instructions are brittle workarounds. If the model is iterating because it doesn't see prior results, no system-prompt instruction will fix that.",
      "Correct — re-issuing tool_use turns past completion is the classic signature of missing tool_results in history. Walk the message history and verify every prior tool_use has a matching tool_result (by tool_use_id) appended before the next request was sent.",
      "Wrong — a hard cap of 3 truncates legitimate work alongside the bug. Caps are circuit-breakers, not debugging tools, and they hide rather than diagnose the structural issue."
    ]
  },
  {
    "question": "Two engineers debate loop termination. Engineer A: \"Just use stop_reason.\" Engineer B: \"Use stop_reason as the primary signal, but also keep an iteration cap as a circuit-breaker.\" Which is the most defensible production design?",
    "options": [
      "A — caps are unnecessary if stop_reason is implemented correctly",
      "B — stop_reason drives normal termination; the cap acts as a circuit-breaker that prevents a misbehaving model or buggy loop from burning unbounded tokens",
      "Neither — natural-language matching on assistant text is more reliable than either",
      "Either is fine — they're functionally identical in production"
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Wrong — caps are inexpensive insurance against bugs in your loop, infrastructure failures, and unexpected model behavior. Production systems need defense in depth even when the primary signal is correctly implemented.",
      "Correct — stop_reason is the canonical signal for normal termination; the cap is a circuit-breaker for the cases where stop_reason isn't reaching you correctly (your bug, network issue, or model misbehavior). They serve different purposes.",
      "Wrong — text-matching is exactly the anti-pattern both engineers are trying to avoid. It is locale- and version-fragile.",
      "Wrong — they are not interchangeable: stop_reason answers 'what does Claude say?' while a cap answers 'what is the worst we will let happen?'. Conflating them produces either fragile termination or runaway costs."
    ]
  },
  {
    "question": "A new agent builder's loop is: send → if `assistant_text` contains the word \"I'll\" continue, else terminate. They report inconsistent behavior across model upgrades and across non-English customer interactions. What is the structural reason this design is unstable?",
    "options": [
      "The model's stylistic phrasing varies across versions and locales, so any natural-language termination heuristic will drift; stop_reason is the version- and locale-stable canonical signal",
      "The Anthropic API normalizes locale automatically, so the heuristic is fine in practice",
      "An iteration cap would address this issue completely on its own",
      "Tool descriptions need to be localized so the heuristic continues to work"
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Correct — natural-language phrasing varies across model versions and across user/agent locales. Any termination heuristic that depends on a phrase will drift the moment the model is updated or the conversation is in a different language. stop_reason is stable across both.",
      "Wrong — there is no automatic locale normalization in the API; the model produces language appropriate to the conversation. Heuristics on text content remain locale-sensitive.",
      "Wrong — an iteration cap doesn't fix wrong termination decisions; it just bounds how long the wrong decision can run. The structural fix is replacing the heuristic with stop_reason.",
      "Wrong — localizing tool descriptions is unrelated to assistant-text-based loop termination. The fragility is in the loop's exit condition, not in tool selection."
    ]
  },
  {
    "question": "A multi-agent research system delegates work to a subagent via the Task tool. Inside that subagent, an agentic loop runs to completion (terminating on its own `end_turn`). The coordinator then incorporates the subagent's final output. From the *coordinator's* perspective, what drives the coordinator's own loop iteration?",
    "options": [
      "The coordinator iterates while *its own* response's stop_reason is \"tool_use\" — invoking the subagent is just a tool call from the coordinator's perspective; the subagent's internal stop_reason has no effect on the outer loop",
      "The coordinator iterates whenever any subagent has any `tool_use` block, anywhere in the hierarchy",
      "The coordinator's loop terminates when the subagent's final assistant text contains a phrase like \"done\" or \"finished\"",
      "There is a single shared stop_reason across the entire agent hierarchy that all loops read"
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Correct — from the coordinator's perspective the subagent invocation is a tool call. The coordinator iterates on its own stop_reason: `\"tool_use\"` continues (the subagent's output comes back as a tool_result), `\"end_turn\"` terminates. The subagent's internal stop_reason governs only its own loop.",
      "Wrong — there is no shared notion of `tool_use` across the hierarchy. Each loop reads only its own response's stop_reason; the subagent's internal calls are invisible to the coordinator's loop logic.",
      "Wrong — terminating the coordinator's loop based on assistant text from anywhere is the same anti-pattern as text-based termination in a single-agent loop, and it's worse here because the text comes from a different agent's output.",
      "Wrong — stop_reason is per-response, not shared. Each agent has its own loop reading its own stop_reason; no cross-hierarchy field exists."
    ]
  },
  {
    "question": "A loop's termination check is `if response.content[0].type == \"text\" and response.content[0].text: break`. Why is this an anti-pattern, even when it appears to work on simple tasks?",
    "options": [
      "Index 0 may not exist on long responses, causing IndexError",
      "tool_use blocks are always serialized as type \"text\" in this SDK, so the check never fires",
      "`response.content` is always a single block, so indexing is unnecessary",
      "Tool-calling turns can also contain text blocks (preamble narration before a tool_use); using \"non-empty text\" as the termination signal terminates legitimate multi-step turns mid-flight — the canonical signal is stop_reason"
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Wrong — index 0 always exists on a non-empty response; the bug isn't index validity, it's *what the check tests*. The structural anti-pattern is using text content as the termination signal.",
      "Wrong — tool_use blocks are not serialized as type 'text'; they have type 'tool_use'. The check fires on actual text blocks, which can appear in tool-calling turns.",
      "Wrong — `response.content` can contain multiple blocks (text + tool_use, or multiple tool_use). Indexing into [0] is itself a hidden bug, but the question is about why the *termination logic* is wrong.",
      "Correct — tool-calling turns frequently contain a text block (the model narrating intent) followed by tool_use blocks. Treating any non-empty text as termination kills legitimate multi-step turns mid-flight. Use stop_reason instead."
    ]
  },
  {
    "question": "Two agentic-loop implementations are reviewed. Implementation A treats every stop_reason that isn't `\"end_turn\"` as termination. Implementation B explicitly handles `\"tool_use\"` as continue, `\"end_turn\"` as terminate, and treats other values like `\"max_tokens\"` or `\"stop_sequence\"` as separate cases that surface to the caller or trigger continuation rather than silent termination. Which design is more robust in production, and why?",
    "options": [
      "A — collapsing every non-end_turn case to \"terminate\" is simpler and avoids edge-case handling code",
      "B — only `\"tool_use\"` should iterate the loop; non-tool_use codes (especially `\"max_tokens\"`) often represent conditions that warrant retry, continuation, or escalation, not silent termination",
      "They are functionally identical because stop_reason values other than tool_use and end_turn never occur in practice",
      "Neither — assistant text content should be the primary termination signal regardless of stop_reason"
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post",
    "explanations": [
      "Wrong — collapsing every non-end_turn case to 'terminate' silently drops legitimate continuation cases like `\"tool_use\"`, which is exactly the signal the loop must continue on.",
      "Correct — only `\"tool_use\"` should iterate the loop; other codes (`\"max_tokens\"` for truncation, `\"stop_sequence\"` for configured stops, refusal codes) deserve specific handling — retry, surface to the caller, or escalate — rather than silent termination.",
      "Wrong — non-`tool_use`/`end_turn` codes do occur in practice (`max_tokens` from long generations, `stop_sequence` when a stop string is configured) and ignoring them masks bugs.",
      "Wrong — text content is exactly the anti-pattern both designs are trying to avoid. It's not the right primary signal in any production system."
    ]
  },
  {
    "question": "Your loop appends `tool_result` blocks under a new `assistant` role message after running tools. Claude responds inconsistently and sometimes acts as if no tool was ever called. What is the structural fix?",
    "options": [
      "Switch the assistant message to a single text block containing the tool outputs as JSON; Claude will reason over the text directly.",
      "Move the tool_result blocks into a `system` message so the API treats them as authoritative context.",
      "Append `tool_result` blocks under a **user**-role message between assistant turns. The API expects tool_result blocks in user-role messages so they thread back to the prior assistant tool_use as input to the next assistant turn.",
      "Increase `max_tokens` so Claude has room to re-process the results within the same assistant turn."
    ],
    "explanations": [
      "Wrong — converting structured tool_result blocks into stringified JSON inside a text block loses the structured correlation Claude relies on (the tool_use_id linkage). The model is built to reason over tool_result blocks specifically, not over text-encoded approximations of them.",
      "Wrong — the system role is for the developer-authored system prompt, not for tool outputs. Putting tool results there breaks the canonical message structure and is not how the API consumes them.",
      "Correct — tool_result blocks must live in user-role messages. They are inputs to Claude's next assistant turn; placing them in assistant content makes them look like Claude's own output rather than the result of a prior tool call.",
      "Wrong — max_tokens controls generation length, not message-structure interpretation. The bug is the role/shape of the appended message, not the response budget."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "Between iterations of a healthy agentic loop, what happens to the conversation history?",
    "options": [
      "It grows monotonically — each turn (assistant tool_use, user tool_result, the next assistant response) is appended. Claude sees the full trace on every request, which is what lets it reason about prior calls.",
      "The Anthropic SDK auto-prunes older turns once they have been acted upon, so the history stays bounded.",
      "Only the most recent tool_use and its tool_result are retained; older turns are dropped to save tokens.",
      "The loop replaces the previous assistant turn with the new one each iteration so the history stays a fixed shape."
    ],
    "explanations": [
      "Correct — the message history grows monotonically across iterations. Every assistant response (text + any tool_use blocks) and every user response (tool_result blocks) is appended in order. This growing history is exactly what enables model-driven decision-making across turns.",
      "Wrong — the SDK does not auto-prune turns. Truncation/compaction is a different concern (handled at session boundaries or by /compact, not by the loop itself).",
      "Wrong — pruning older turns mid-loop would erase the causal trail Claude reasons over. Tools called several iterations back are still relevant context for the next decision.",
      "Wrong — replacing prior turns would erase the tool_use/tool_result correlations Claude depends on. Each iteration appends; nothing is overwritten."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "A response arrives with `stop_reason: \"tool_use\"` and content `[text(\"Looking up the order…\"), tool_use(name=lookup_order, …)]`. What is the correct loop behavior?",
    "options": [
      "Terminate — the assistant emitted text, which means Claude is narrating rather than acting.",
      "Skip the tool_use block since the text block came first; Claude can be re-prompted if a tool call is actually needed.",
      "Re-prompt Claude to clarify whether it wants to call a tool or just narrate, since the response is ambiguous.",
      "Execute the tool_use block, append its tool_result (correlated by tool_use_id), and re-send. The text is incidental narration; `stop_reason: \"tool_use\"` is the canonical instruction to continue."
    ],
    "explanations": [
      "Wrong — using text presence as a termination signal is the textbook anti-pattern. stop_reason is \"tool_use\" here, which is the canonical instruction to continue.",
      "Wrong — content blocks are not ranked by order. Claude's tool_use blocks are deliberate calls regardless of whether text appears alongside them.",
      "Wrong — the response is not ambiguous. stop_reason: \"tool_use\" is unambiguous; the loop continues by executing the tool.",
      "Correct — stop_reason is the canonical signal. Text content alongside a tool_use block is just narration (\"preamble\") and must not change loop behavior; execute the tool and proceed."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "An engineer adds: \"if the model returns only text with `stop_reason: \\\"end_turn\\\"` but our system thinks more work is needed, call `escalate_to_human` ourselves before terminating.\" What is structurally wrong with this?",
    "options": [
      "escalate_to_human is reserved for the model and the Anthropic API will reject any call originating outside a tool_use block.",
      "It bypasses model-driven decision-making — the loop has become the controller. Whether to escalate is a context-dependent decision the model should make from accumulated history; introducing loop-side heuristics diverges agent behavior from what its prompt and tool descriptions imply.",
      "Tools called outside a tool_use block are not added to conversation history, so the model never learns the escalation happened.",
      "The Anthropic SDK validates that every tool execution corresponds to a tool_use_id from a prior assistant turn and will throw at runtime."
    ],
    "explanations": [
      "Wrong — there is no API-level restriction on which tools your code can run; the API does not police your client logic. The objection is architectural, not enforced by the API.",
      "Correct — model-driven decision-making is the defining property of an agentic loop. Adding loop-side heuristics that act as if the model decided puts the loop in the controller seat and makes agent behavior diverge from what the prompt and tool descriptions describe.",
      "Wrong — you can append whatever you want to history. The structural problem is who is making the decision, not whether the artifact gets logged.",
      "Wrong — the SDK does not validate tool executions against tool_use_ids; it just forwards messages. The objection is architectural."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "A long, multi-tool turn returns `stop_reason: \"max_tokens\"` — Claude was emitting tool_use blocks and got truncated mid-stream. What should the loop do?",
    "options": [
      "Treat it as `\"end_turn\"` and terminate; max_tokens means Claude effectively finished what it could fit.",
      "Treat it as `\"tool_use\"` and execute whatever tool_use blocks are present; the truncated content can be inferred by the next iteration.",
      "Surface the condition to the caller (or retry with a higher max_tokens, or let the model continue if the SDK supports continuation). `\"max_tokens\"` is **neither** continue nor terminate — it is an incomplete response that needs explicit handling, not silent mapping to either bucket.",
      "re-send the same prompt with `tool_choice: \"any\"` to force Claude to pick a single tool and produce a shorter response."
    ],
    "explanations": [
      "Wrong — max_tokens means the response was cut off, not that Claude finished its turn. Mapping it to end_turn discards a real signal that the system needs to react to.",
      "Wrong — the response is partial; the tool_use blocks present may be incomplete (missing inputs) and there may be more Claude wanted to emit. Executing them blindly is unsafe.",
      "Correct — max_tokens is a third state distinct from tool_use/end_turn. It signals truncation, which calls for retry with a larger budget, escalation, or surfacing to the caller. Silently coercing it into either bucket masks the underlying problem.",
      "Wrong — switching tool_choice fights the issue. Claude's response was not malformed by tool selection; it ran out of tokens. The fix is budget, not selection mode."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "A response with `stop_reason: \"tool_use\"` contains three tool_use blocks (`lookup_order`, `check_loyalty_status`, `fetch_recent_returns`). Your loop executes only the first, appends its tool_result, and re-sends — ignoring the other two. What is the predicted downstream symptom?",
    "options": [
      "Claude is likely to re-issue the two missing tools on the next turn (because their tool_use entries have no matching tool_result), wasting iterations and confusing the message history. Every tool_use in a turn must be executed and have its tool_result appended before re-sending.",
      "The Anthropic API will reject the next request because the message history is structurally invalid (tool_use without matching tool_result).",
      "The two un-executed tool_use blocks will be silently dropped from history when the next request is sent; Claude will have no record they were ever requested.",
      "Claude will continue its task as if the unexecuted tools succeeded with empty results, since `stop_reason: \"tool_use\"` was already consumed."
    ],
    "explanations": [
      "Correct — from the model's perspective, an unmatched tool_use looks like a call that produced no output, so it dutifully re-issues the same call. The discipline is: if the turn emitted N tool_use blocks, the loop runs all N and appends N tool_result blocks before re-sending.",
      "Wrong — the API does not validate tool_use/tool_result symmetry. The structural inconsistency causes model confusion, not an API rejection.",
      "Wrong — nothing is silently dropped from your client-managed message history. The blocks remain unmatched, which is the source of the bug.",
      "Wrong — Claude does not infer empty results for unmatched tool_use blocks; it generally re-issues them, because from its perspective the call produced no data."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "A `lookup_order` tool throws an exception in your code. The loop must communicate this back to Claude so it can react. Which is the correct pattern?",
    "options": [
      "Drop the assistant turn that contained the tool_use entry and re-send a clean conversation, since the tool call effectively did not happen.",
      "Append a `system` message explaining the error, then re-send so the model treats the failure as authoritative context.",
      "Append nothing — Claude will infer the tool was not called when no new data appears in the next turn.",
      "Append a `tool_result` block correlated by `tool_use_id` with `isError: true` and a short error description. The model sees the failure in the canonical channel and can react in context (retry with different params, choose a different tool, escalate)."
    ],
    "explanations": [
      "Wrong — rewriting history to hide the tool_use entry erases the causal trail and is structurally fragile. The right move is to communicate the failure, not pretend the call did not happen.",
      "Wrong — system messages are not the channel for tool failures. Tool failures must be reported via tool_result so they correlate to the originating tool_use_id and Claude can react.",
      "Wrong — Claude does not infer 'tool failed' from absent data; it generally re-issues the call (since from its perspective the call produced no output).",
      "Correct — `tool_result` with `isError: true` is the canonical channel for tool failures. It preserves correlation by tool_use_id and lets Claude reason about the failure (retry, fall back, escalate) using the same machinery it uses for successful results."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "An iteration counter is added for production observability. The team debates whether it should also be the primary termination signal. Which design fits agentic-loop principles?",
    "options": [
      "Drop the counter — it is redundant with stop_reason traces and just bloats logs.",
      "Log every iteration for observability AND keep a generous hard cap (well above any expected legitimate task length) as a runaway circuit-breaker. stop_reason still drives normal termination; the cap fires only on broken loops.",
      "Make the counter the primary termination signal (cap = N) — it is deterministic and easy to reason about, while stop_reason is fragile.",
      "Cap iterations very tightly (e.g., 3) so any non-trivial task fails fast and forces developers to redesign for fewer steps."
    ],
    "explanations": [
      "Wrong — observability of iteration count is genuinely useful for debugging runaway loops, performance, and cost analysis. Logging it is not bloat; conflating it with control is the mistake.",
      "Correct — observability and runaway protection are complementary to (not substitutes for) stop_reason-driven termination. A roomy cap is a safety net; stop_reason remains the canonical signal.",
      "Wrong — using the cap as the primary signal truncates legitimate multi-step tasks while letting broken loops still burn the full N. stop_reason is exactly the deterministic per-turn signal designed for this purpose.",
      "Wrong — a tight cap that fires on every non-trivial task is a guillotine, not a circuit-breaker. It punishes correct behavior and obscures bugs."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "You move an agentic workflow from an interactive Claude Code session into CI/CD via `claude -p` (non-interactive). The loop terminates early in CI but works interactively. The implementation uses `if response.stop_reason == \"end_turn\": terminate`. What is the most likely diagnosis?",
    "options": [
      "`-p` mode forces a single iteration regardless of stop_reason; agentic loop semantics are unavailable in non-interactive mode.",
      "`-p` mode disables tool_use entirely; only text generation is supported in CI.",
      "`-p` mode does not change agentic-loop semantics — stop_reason behaves identically. The early termination must come from elsewhere in the loop (a heuristic on text, a low cap, missing tool_result append). Inspect the loop, not the CLI flag.",
      "The Anthropic API rate-limits non-interactive sessions to a small fixed iteration count and silently terminates after the limit is reached."
    ],
    "explanations": [
      "Wrong — `-p` is about IO, not loop semantics. The loop runs the same way; what differs is interactivity (no prompts, output sent to stdout).",
      "Wrong — tool_use works in non-interactive mode. The flag changes how the session interacts with the user, not what the model can do.",
      "Correct — agentic-loop semantics are identical across interactive and non-interactive modes. If the symptom only appears in CI, look for environment differences (a hard cap that triggers under different timing, a missing tool_result append surfacing only on certain inputs, or a text heuristic that fires on language the model produces only in batch mode).",
      "Wrong — the API does not impose a hidden iteration cap based on `-p`. Iteration limits are entirely in your client code."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "Your team adds an automated invariant check that runs after every loop iteration. Which invariant most directly guards against the most common loop bugs?",
    "options": [
      "Every `tool_use` block in the conversation history has a matching `tool_result` block (correlated by `tool_use_id`) appended before the next request. Violations cause Claude to re-issue tools, hallucinate outcomes, or behave as if calls did not happen.",
      "The total token count of the conversation stays under 10% of the model context window so the model never feels constrained.",
      "Each turn's assistant text content is a complete, non-empty sentence — empty text indicates a malformed response.",
      "The number of tool_use blocks per turn is exactly 1, since parallel tool calls are unsupported and indicate a model error."
    ],
    "explanations": [
      "Correct — unmatched tool_use blocks are the single most common cause of agentic-loop pathologies (re-issued tools, hallucinated outcomes, runaway iterations). Asserting tool_use ↔ tool_result symmetry catches almost every history-correctness regression.",
      "Wrong — token-count thresholds are useful for cost/performance, but they do not catch the structural bugs that cause loop misbehavior. A loop can be perfectly healthy near the context limit and broken at 1% utilization.",
      "Wrong — empty assistant text content is fine; tool_use turns can have minimal or no text. The presence/absence of text says nothing about loop correctness.",
      "Wrong — multiple tool_use blocks per turn (parallel calls) are a feature, not a bug. The invariant should be that all of them are executed and matched; not that there is exactly one."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "An engineer asks: \"In our agentic loop, do we need to send the full message history on every iteration, or just the latest tool_result?\" Choose the correct answer with the principle behind it.",
    "options": [
      "The full message history (system, prior user, prior assistant turns including tool_use blocks, prior tool_results) must be sent on each iteration. The Anthropic Messages API is stateless across requests, so without prior turns Claude cannot correlate tool_use_ids or recall earlier reasoning.",
      "Only the latest user message; Anthropic stores conversation state server-side keyed by a conversation_id and replays it for you.",
      "Only the prior assistant message; tool_results are server-cached for an hour and re-attached automatically.",
      "Whichever shape your SDK helper chooses; the API tolerates both stateful and stateless use of the messages endpoint."
    ],
    "explanations": [
      "Correct — the API has no built-in session/state, so the developer-owned message history is the loop's memory. Sending only deltas would leave the model unable to match prior tool_use blocks to their results or recall what it has already done.",
      "Wrong — there is no server-side conversation_id replay in the standard Messages API. Treating it as if there were causes Claude to lose all prior context immediately and re-issue prior tools or hallucinate outcomes.",
      "Wrong — tool_results are not server-cached; they live in the developer-managed history. If you only resend the prior assistant message, every prior tool_result is lost and the next turn is essentially blind.",
      "Wrong — the API is stateless; statefulness is not a configuration. Believing the helper chose statefulness for you usually surfaces as inconsistent behavior across helper versions."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "A team is choosing between an agentic loop and a fixed pipeline (`get_customer` → `lookup_order` → `process_refund`) for a customer-support workflow whose inbound requests range from simple status checks to multi-issue escalations. Which is the strongest single argument for the agentic loop?",
    "options": [
      "Pipelines cannot return errors, so they are unsuitable for support workflows.",
      "Pipelines are always faster than agentic loops, so the loop adds latency for no benefit.",
      "Inbound requests vary in shape, and an agentic loop lets Claude pick (and skip) tools per request from accumulated context, while a fixed pipeline forces every request through the same steps. Pipelines remain valid only when the workflow is genuinely uniform across inputs.",
      "Agentic loops use less context-window than scripted pipelines because Claude prunes prior turns automatically."
    ],
    "explanations": [
      "Wrong — pipelines can absolutely return errors; that is not the distinguishing trade-off. The mistake is comparing a feature parity question to the actual issue (adaptability).",
      "Wrong — for genuinely uniform workflows a pipeline can be lower-latency than a multi-turn loop. The loop's advantage is adaptability, not speed.",
      "Correct — model-driven decision-making is the defining property. When inputs vary, only the loop can adapt the tool sequence per request; a pipeline locks every input into a fixed flow even when steps are irrelevant.",
      "Wrong — Claude does not auto-prune prior turns. Loops typically use *more* tokens than a pipeline because each iteration carries the growing history."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "To save tokens, a loop drops every `tool_result` block from prior iterations and keeps only the current turn's `tool_result`s in history. What is the most likely production symptom?",
    "options": [
      "Faster responses with no behavioral impact, since Claude only needs the latest tool results.",
      "The loop crashes because tool_use_id pairing is missing when tool_results are removed.",
      "stop_reason flips to \"end_turn\" early as an automatic safety the API applies when history is malformed.",
      "Claude re-issues earlier `tool_use` calls because their results are gone — every `tool_use` in history needs a matching `tool_result`, and dropping them forces redundant re-invocation and inconsistent state across the conversation."
    ],
    "explanations": [
      "Wrong — Claude needs the *whole* prior history to reason about what has already been done. Dropping prior results consistently produces re-issued tools or hallucinated outcomes, not silent equivalence.",
      "Wrong — the loop does not crash; the API will accept a malformed history. The bug is behavioral (Claude acts as if prior tools never ran), not a runtime error.",
      "Wrong — the API does not silently rewrite stop_reason based on history shape. stop_reason is the model's signal, not a safety net the platform applies for malformed inputs.",
      "Correct — the canonical invariant is that every `tool_use` in history has its matching `tool_result`. Trimming verbose *content* inside a result is fine; removing the block (and its tool_use_id) is not."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "Your loop sees `stop_reason: \"tool_use\"` with one `tool_use` block (`process_refund`). The tool returns `{ approved: false, reason: \"policy_violation\" }`. The dev's code does NOT append the `tool_result`; instead it injects a synthetic `user` message (\"Refund could not be processed. Please escalate.\") and resumes the loop. What is the principled critique?",
    "options": [
      "The fix is fine — adding context as a synthetic user message is equivalent to appending a tool_result, just expressed in natural language.",
      "Synthetic user messages break the canonical channel: Claude no longer sees the structured failure correlated by `tool_use_id`, so its escalation reasoning is divorced from the actual tool outcome. The correct fix is to append the `tool_result` (with `isError: true` if appropriate) and let Claude pick the next action.",
      "The loop should retry `process_refund` automatically until it succeeds; failures should never propagate to the model.",
      "Claude cannot read user messages mid-loop, so the synthetic message is silently ignored by the API."
    ],
    "explanations": [
      "Wrong — natural-language summaries are not equivalent. The model loses the tool_use_id ↔ tool_result correlation, the structured field shape, and the canonical signal that the *tool itself* failed (vs the user changing tack).",
      "Correct — tool outcomes belong in `tool_result` blocks. Bypassing that channel undermines model-driven decision-making and discards the structured failure information Claude is trained to react to.",
      "Wrong — silent in-harness retries strip Claude's ability to escalate or branch. Some failures (policy violations) are not retryable; the model should see them and decide.",
      "Wrong — Claude does read user messages. The bug is not that the message is dropped; it is that the message is the wrong channel for a tool outcome."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "A team adds a custom tool `finish_task` and prompts Claude: \"Call `finish_task` when you are done.\" Their loop terminates when Claude calls `finish_task` instead of when `stop_reason == \"end_turn\"`. Why is this an anti-pattern?",
    "options": [
      "`finish_task` collides with built-in tool names, and the API rejects the call at validation time.",
      "tool_use_id cannot be assigned to no-op tools, so the call always errors with a schema mismatch.",
      "Termination is meant to be driven by the canonical, model-controlled signal (`stop_reason`). Routing termination through a tool the model decides to call is text-matching in disguise — Claude can forget to call it, call it prematurely, or skip it on a multi-step path. `stop_reason: \"end_turn\"` is the API-level guarantee.",
      "Custom tools cannot return without arguments, so `finish_task` always raises a schema error at decode time."
    ],
    "explanations": [
      "Wrong — there is no built-in collision; the API does not reject this. The anti-pattern is conceptual, not syntactic.",
      "Wrong — tool_use_id is assigned by the model for any tool_use block regardless of payload. Schema mismatches are not the failure mode.",
      "Correct — `stop_reason` is the canonical signal precisely because it is model-controlled at the API level. A tool the model can forget to call gives termination the same brittleness as parsing assistant text for \"done\".",
      "Wrong — schema-zero tools (no required args) are valid. The structural problem is that termination semantics belong on `stop_reason`, not in a tool the model elects to call."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "A response has content `[text(\"I will look up the order now\"), tool_use(name=lookup_order, id=tu_01)]` and `stop_reason: \"tool_use\"`. Your loop inspects only `content[0]`, sees text, treats the turn as informational, and re-sends without running `lookup_order`. What is the predicted symptom?",
    "options": [
      "`lookup_order` is silently dropped: the next request lacks a `tool_result` for `tu_01`, so Claude is forced to either re-emit the same tool_use or hallucinate the order data. The loop must iterate over ALL content blocks and execute every `tool_use`.",
      "Nothing — Claude will simply repeat the request and the order is fetched on the next turn at no cost.",
      "The loop deadlocks because Claude keeps emitting only text after seeing no tool_result.",
      "The Anthropic API auto-runs the tool when the loop ignores it and returns the result on the next call."
    ],
    "explanations": [
      "Correct — a turn can mix text and tool_use blocks; `stop_reason` is the canonical signal, not `content[0]`. Skipping content blocks past the first violates the loop's contract: execute every `tool_use` in the turn.",
      "Wrong — re-emitting wastes iterations and tokens, and is not guaranteed: Claude can equally hallucinate the result or branch elsewhere. Either way the bug surfaces in production.",
      "Wrong — deadlock is the wrong failure mode. Claude is not blocked waiting for anything; it just receives a malformed history and adapts unpredictably.",
      "Wrong — the API does not execute tools for you. Tool execution is the developer's responsibility; the harness is the only entity that can run `lookup_order`."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "Your harness registers tools `[get_customer, lookup_order, process_refund]`. Claude returns a `tool_use` block for `cancel_subscription` — a tool the harness does not implement. What is the correct loop behavior?",
    "options": [
      "Throw an exception in the loop and abort the request; the missing tool indicates a configuration bug that should fail loudly.",
      "Silently drop the tool_use; do not append anything for it; re-send and hope Claude picks another tool on the next turn.",
      "Reply with a fresh user message saying \"Tool not available\" and continue.",
      "Append a `tool_result` for the matching `tool_use_id` with `isError: true` and a description (e.g., \"Tool `cancel_subscription` is not available; pick a registered alternative or terminate\"); the loop continues. Claude can then redirect, escalate, or end the turn based on the structured failure."
    ],
    "explanations": [
      "Wrong — aborting the whole request strips Claude's ability to recover (e.g., escalate, pick a registered alternative). Crash-on-mismatch is a worse user experience than surfacing the failure in the canonical channel.",
      "Wrong — silently dropping leaves an unmatched tool_use in history, which violates the core invariant. Claude will likely re-emit the same call, or hallucinate the outcome.",
      "Wrong — tool outcomes belong in `tool_result` blocks, not synthetic user messages. The user-message channel discards the tool_use_id correlation and undermines model-driven recovery.",
      "Correct — this is the structured-error pattern: the loop honors the tool_use ↔ tool_result invariant and lets Claude reason about an `isError` result. Most production agents recover gracefully when given an explicit failure description."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "A junior engineer suggests \"renaming tools at the loop layer\": when Claude requests `process_refund`, the dev's code substitutes `manual_refund` if the amount > 500 USD. Why is this an anti-pattern?",
    "options": [
      "The substitution breaks model-driven decision-making — Claude requested `process_refund` based on its understanding of the available tools; running a different tool silently grounds subsequent reasoning in a false premise. The right pattern is to surface the constraint via the original tool's result (e.g., `requires_manual_review: true`) and let Claude pick the next action.",
      "Renaming is fine if logged, since Claude does not track tool names across turns and only correlates by tool_use_id.",
      "tool_use_id collisions will occur because the harness reuses the same id across the two tools.",
      "The Anthropic API rejects substituted tool results with HTTP 400 because the tool name in the response does not match a registered tool."
    ],
    "explanations": [
      "Correct — silent substitution is a failure of model-driven decision-making. Claude's next reasoning step assumes `process_refund` ran with refund-shaped semantics; running `manual_refund` instead invalidates that assumption silently. Surface the constraint via the tool result and let Claude branch.",
      "Wrong — Claude absolutely tracks tool names across turns (they appear in tool_use blocks). The model's next action is conditioned on the tool it requested, not just the id.",
      "Wrong — tool_use_id is unique per tool_use block; it does not cause cross-tool collisions. The bug is conceptual, not a name clash.",
      "Wrong — the API does not validate `tool_name` in `tool_result` against the registered list this strictly; it correlates by `tool_use_id`. The harm is behavioral, not an API-level rejection."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "Inspecting a failing trace, history shows three `tool_use` blocks (ids `tu_a`, `tu_b`, `tu_c`) but `tool_result` blocks for only `tu_a` and `tu_c` — `tu_b`'s result is missing. The next assistant turn re-emits the same operation under a new id `tu_b2`. What invariant has been violated, and how do you restore it?",
    "options": [
      "tool_use_id values must be alphabetical; reorder them so `tu_b` sorts between `tu_a` and `tu_c`.",
      "tool_result blocks must be combined into a single aggregate block per turn; restructure the history accordingly.",
      "Every `tool_use` in history must have a matching `tool_result` (correlated by `tool_use_id`) appended before the next request. The fix is for the loop to append a result for every block — including `isError: true` results when a tool failed — so Claude does not re-issue the call from scratch.",
      "stop_reason was misread as `end_turn` instead of `tool_use`; check the parsing of the response object."
    ],
    "explanations": [
      "Wrong — tool_use_id is opaque; alphabetic ordering carries no semantic weight. The bug is the missing match for `tu_b`, not the order of ids.",
      "Wrong — tool_result blocks are per-call, correlated by tool_use_id. Aggregating them into one block would itself break the correlation invariant.",
      "Correct — the canonical invariant is one-to-one tool_use ↔ tool_result by id. Re-emitting under `tu_b2` is the textbook symptom of a missing result for `tu_b`; the fix is to always append a result (even `isError`) for every executed or attempted tool.",
      "Wrong — the symptom (re-emitted tool_use) tells you stop_reason was `tool_use` as expected. The bug is not in stop_reason parsing; it is in tool_result emission."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "Pick the best one-sentence description of an agentic loop — the one a candidate should be able to give an interviewer.",
    "options": [
      "A loop where the developer hard-codes a sequence of API calls and Claude generates text for each step in the sequence.",
      "A loop in which the model emits `tool_use` requests, the harness executes them and appends `tool_result` blocks (correlated by `tool_use_id`) to history, and iteration is driven by `stop_reason` until the model returns `end_turn`.",
      "A loop that calls Claude exactly once and processes the response with regex matching on the assistant text to decide what to do next.",
      "A loop where Claude is invoked across a thread pool and tool results are reduced via map-reduce into a final answer."
    ],
    "explanations": [
      "Wrong — that describes a scripted pipeline, not an agentic loop. Hard-coding the sequence eliminates model-driven decision-making, the defining property of agentic execution.",
      "Correct — captures all four essentials: model emits `tool_use`, harness executes and appends `tool_result` with id correlation, iteration is driven by `stop_reason`, and `end_turn` terminates. No anti-patterns (text matching, iteration caps, hard-coded sequences) sneak in.",
      "Wrong — a single-call regex loop is neither agentic nor a loop in any meaningful sense. Regex matching on assistant text is a known anti-pattern (brittle, locale/version-sensitive, bypasses the canonical signal).",
      "Wrong — concurrency and map-reduce describe a different architecture (parallel tool fan-out, batch synthesis). They are orthogonal to what makes a loop agentic."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.1"
    ],
    "phase": "post"
  },
  {
    "question": "A team is designing a multi-agent research system. Which best describes the 'hub-and-spoke' architecture used in coordinator-subagent orchestration?",
    "options": [
      "All subagents communicate freely with each other and only the final result returns to the coordinator.",
      "The coordinator is the central hub through which all inter-subagent communication flows; subagents do not talk directly to each other.",
      "The coordinator runs first; once it finishes, subagents are released to operate independently.",
      "Each subagent has its own coordinator, forming a tree of nested hubs."
    ],
    "explanations": [
      "Wrong — peer-to-peer subagent communication breaks observability and consistent error handling. The coordinator must remain the single point of routing.",
      "Correct — hub-and-spoke means every message flows through the coordinator (the hub). This is what enables centralized observability, error handling, and information control.",
      "Wrong — subagents are not 'released' to run autonomously after the coordinator finishes. Each invocation is a discrete request-response routed through the coordinator while it is still running.",
      "Wrong — that describes nested coordinators, not hub-and-spoke. In hub-and-spoke there is one coordinator and a flat ring of specialised subagents."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "pre"
  },
  {
    "question": "When a coordinator spawns a subagent in a hub-and-spoke system, what does the subagent automatically receive about the current task?",
    "options": [
      "The full coordinator conversation history, including prior user turns and reasoning.",
      "All prior subagent results from this session, deduplicated.",
      "Only the context the coordinator passes explicitly in the subagent's invocation; subagent context is isolated.",
      "A read-only handle to the coordinator's memory that it can query as needed."
    ],
    "explanations": [
      "Wrong — this is the most common misconception. Subagents do not inherit the coordinator's history; treating them as if they do is why so many subagent briefs end up underspecified.",
      "Wrong — there is no automatic cross-subagent state sharing. If results from a prior subagent are needed, the coordinator must pass them in explicitly.",
      "Correct — subagent context is isolated. The coordinator must brief each subagent with whatever scope, constraints, and prior context that subagent needs.",
      "Wrong — there is no shared memory handle. The coordinator passes context by value in the invocation; the subagent has no way to call back into the coordinator's state."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "pre"
  },
  {
    "question": "Which of the following is NOT one of the four primary responsibilities of a coordinator agent in a hub-and-spoke system?",
    "options": [
      "Decomposing the user request into subtasks aligned with available subagents.",
      "Delegating each subtask to a specific subagent with an explicit brief.",
      "Aggregating subagent results into a coherent input for downstream steps.",
      "Maintaining persistent long-term memory of all prior user sessions for cross-session learning."
    ],
    "explanations": [
      "This is a coordinator role — decomposition is the first step in turning a user request into something subagents can act on.",
      "This is a coordinator role — delegation requires an explicit brief because subagent context is isolated.",
      "This is a coordinator role — aggregation is what turns parallel subagent outputs into the next step's input.",
      "Correct — long-term cross-session memory is not part of the four coordinator responsibilities (decomposition, delegation, aggregation, dynamic subagent selection). Cross-session persistence is an out-of-band concern."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "pre"
  },
  {
    "question": "A coordinator handles two requests in the same session: (1) 'what is TSLA's current price?' and (2) 'compare TSLA and F's 5-year fundamentals and write a one-pager.' How should its behaviour differ?",
    "options": [
      "Dynamically select the subagent set per query — invoke only a finance lookup for (1), and the full search/docs/synthesis/report pipeline for (2).",
      "Always run the full pipeline so output formatting is consistent across requests.",
      "Run the same pipeline but summarise more aggressively for (1) to keep latency low.",
      "Reject (1) — the multi-agent system is over-engineered for a single-step factual lookup."
    ],
    "explanations": [
      "Correct — dynamic subagent selection is exactly this: the coordinator analyses query requirements and invokes only the subagents it actually needs. (1) is a lookup; (2) is a comparative synthesis.",
      "Wrong — running every subagent regardless of query is a fixed pipeline disguised as orchestration. It wastes tokens and dilutes synthesis when most subagents had nothing useful to add.",
      "Wrong — 'summarise more aggressively' is a downstream bandage; the upstream issue is that the coordinator picked the wrong subagent set in the first place.",
      "Wrong — rejecting trivial queries punishes users for asking simple questions. The right behaviour is to invoke a smaller subagent set, not to refuse."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "pre"
  },
  {
    "question": "A multi-agent research system was asked for a report on creative industries' adoption of generative AI. The synthesis covers visual arts thoroughly but never mentions music or film. Where is the root cause most likely to be?",
    "options": [
      "The web search subagent's queries returned poor results for music and film.",
      "The synthesis subagent ignored part of its input.",
      "The coordinator's task decomposition was too narrow and never assigned music or film as subtasks.",
      "The user's prompt was too vague to act on without clarification."
    ],
    "explanations": [
      "Wrong — the search subagent can only search what it's asked to search. If the coordinator never delegated a 'music' or 'film' query, the search subagent had nothing to fail on.",
      "Wrong — the synthesis subagent only synthesises what it received. Missing sub-domains in the output usually mean missing sub-domains in the input, which traces back to decomposition.",
      "Correct — overly narrow decomposition is the canonical root cause for missing sub-domains. The fix is at the coordinator: broader decomposition plus an iterative refinement loop to detect gaps.",
      "Wrong — 'creative industries' implies music, film, and visual arts to most readers. The coordinator should decompose accordingly rather than punt to the user."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "pre"
  },
  {
    "question": "A coordinator fans out three search subagents in parallel, each given the prompt 'research generative AI in creative industries.' The synthesis stage receives heavily overlapping evidence. What is the most likely cause?",
    "options": [
      "Three is the wrong number of parallel subagents; two would have produced complementary results.",
      "The coordinator failed to partition the research scope, so all three subagents covered the same ground.",
      "The synthesis subagent is over-eager about deduplication and is collapsing distinct findings.",
      "The search subagents need better tool descriptions to differentiate themselves."
    ],
    "explanations": [
      "Wrong — count isn't the issue. Two subagents on the same broad query would have duplicated just as much. Distinct slices, not specific counts, are what create coverage.",
      "Correct — without scope partitioning (by subtopic or by source type), parallel subagents converge on the same evidence. The coordinator must hand each subagent a distinct slice.",
      "Wrong — the duplicates are real, not over-deduplication artifacts. The synthesis subagent is correctly noticing the inputs overlap.",
      "Wrong — tool descriptions affect tool selection, not how a subagent partitions its query. Differentiation must come from the coordinator's brief."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "pre"
  },
  {
    "question": "A coordinator invokes a doc-analysis subagent with the prompt 'extract key findings from these documents.' The user's actual question — 'which OEMs adopted gen-AI in 2025?' — is in the coordinator's history but never repeated in the subagent's brief. The subagent returns a generic summary. What's the architectural mistake?",
    "options": [
      "The coordinator assumed the subagent would inherit the user's question from history, but subagent context is isolated — the question must be passed explicitly.",
      "The subagent should have asked a clarifying question before responding.",
      "The subagent prompt is missing chain-of-thought reasoning instructions.",
      "Doc-analysis subagents should not be used for question-driven extraction; a separate extraction subagent should be invoked."
    ],
    "explanations": [
      "Correct — the subagent's context is isolated. The coordinator's history is invisible to it, so 'extract key findings' is all it sees, and a generic summary is the rational response. Pass the user's question explicitly.",
      "Wrong — relying on subagents to ask clarifying questions is unreliable and wastes a round trip. The coordinator is responsible for briefing the subagent fully up front.",
      "Wrong — chain-of-thought won't help when the subagent doesn't know what question it's answering. The missing input is the user's question, not the reasoning style.",
      "Wrong — doc-analysis is fine for question-driven extraction. The bug is that the subagent doesn't know the question, not that it's the wrong subagent."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "pre"
  },
  {
    "question": "A coordinator is built to invoke every subagent (search, docs, synthesis, report) in fixed sequence on every user query, regardless of what was asked. Which best describes this design?",
    "options": [
      "A robust orchestration baseline that maximises coverage on every query.",
      "An efficient default — downstream subagents simply no-op when their input is empty.",
      "The recommended pattern when subagent latency is unpredictable, since it amortises overhead.",
      "A scripted/fixed pipeline disguised as orchestration; the coordinator isn't doing dynamic subagent selection."
    ],
    "explanations": [
      "Wrong — running every subagent regardless of query inflates token cost and dilutes synthesis with irrelevant inputs. 'Robust' here is a euphemism for 'rigid.'",
      "Wrong — subagents don't free no-op; each invocation costs tokens and can introduce errors. The right move is to skip them, not to invoke and rely on no-op.",
      "Wrong — latency unpredictability is a separate concern. Always invoking every subagent doesn't help with latency; it makes the worst-case latency the only case.",
      "Correct — by definition this is a fixed pipeline. The coordinator's value comes from dynamically selecting the subagent subset that fits the query; without that selection, you have prompt chaining with extra steps."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "pre"
  },
  {
    "question": "To 'improve latency,' an engineer modifies the system so the search subagent passes its results directly to the synthesis subagent, bypassing the coordinator. Which best describes the consequence?",
    "options": [
      "Latency improves and there is no architectural downside, since the coordinator was just relaying messages.",
      "Throughput improves, but synthesis quality drops due to attention dilution from extra messages.",
      "The coordinator loses visibility into intermediate state — observability, consistent error handling, and gap detection are all degraded.",
      "It violates the Anthropic API spec and the request will fail at runtime."
    ],
    "explanations": [
      "Wrong — the coordinator is not 'just relaying.' It enforces partitioning, observes intermediate state, and decides whether refinement is needed. Bypassing it removes those.",
      "Wrong — attention dilution is a different concept (about prompt-internal token allocation). Bypassing the coordinator is an architectural break, not an attention issue.",
      "Correct — hub-and-spoke exists precisely so the coordinator can observe inter-subagent traffic and intervene. Peer-to-peer routing kills observability, fragments error handling, and prevents coverage-gap detection.",
      "Wrong — there is no API-level prohibition; the system runs. The break is architectural, not protocol-level, which is exactly what makes it dangerous: it works, just badly."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "pre"
  },
  {
    "question": "A coordinator delegates a multi-source synthesis task and returns the first draft directly to the user. Reports occasionally have entire sub-domains missing. What pattern is the coordinator missing?",
    "options": [
      "An iterative refinement loop where the coordinator evaluates the synthesis output for gaps and re-delegates targeted queries to search/analysis subagents until coverage is sufficient.",
      "A second synthesis subagent that runs in parallel and votes on the final output.",
      "Few-shot examples in the synthesis subagent's prompt.",
      "A more capable model for the synthesis step."
    ],
    "explanations": [
      "Correct — iterative refinement is exactly the missing pattern. The coordinator must compare the draft against the original query, identify gaps, re-delegate targeted searches, and re-invoke synthesis until coverage holds.",
      "Wrong — a second synthesis subagent doesn't fix decomposition gaps. If neither synthesis ever sees evidence on the missing sub-domain, both will omit it.",
      "Wrong — few-shot examples shape format and style, not coverage. The missing sub-domain isn't a style issue; the synthesis subagent never received evidence on it.",
      "Wrong — a stronger model can't synthesise evidence it doesn't have. The fix is upstream of synthesis, in the coordinator's gap detection and refinement step."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "pre"
  },
  {
    "question": "A coordinator in a multi-agent research system needs to invoke a web search subagent on a sub-task derived from 'report on EV adoption in Norway in 2025.' Which invocation brief best aligns with hub-and-spoke principles?",
    "options": [
      "'Search the web' — keep the brief minimal so the subagent isn't biased.",
      "A structured brief with the subdomain (EV adoption), country (Norway), year scope (2025), preferred source types, and expected output shape.",
      "'You are an expert. Use your judgement to research EV adoption thoroughly.'",
      "Pass the entire prior conversation history (user turn + coordinator's reasoning) and let the subagent extract what it needs."
    ],
    "explanations": [
      "Wrong — minimal briefs strand the subagent. Subagent context is isolated, so without explicit scope and constraints, it will return generic search results.",
      "Correct — explicit scope, constraints, and output shape are exactly what the coordinator must pass because subagent context is isolated. This brief is focused and actionable.",
      "Wrong — flattery and 'use your judgement' don't substitute for structured context. The subagent will still lack scope, country, year, and output shape.",
      "Wrong — dumping the full coordinator history bloats the subagent's context and forces it to do the coordinator's job (extraction). Pass only what the subagent needs, in a focused form."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "post"
  },
  {
    "question": "Your synthesis subagent returns a draft missing the music sub-domain in a 'creative industries adoption of gen-AI' report. The coordinator's options are below. Which is the correct refinement step?",
    "options": [
      "Re-run the entire pipeline from scratch with a stronger system prompt.",
      "Ask the user to clarify whether music is in scope for their definition of 'creative industries.'",
      "Re-delegate a targeted search query for music, then re-invoke synthesis with the augmented evidence appended.",
      "Edit the draft directly to add a placeholder music section, marked TODO for the user to fill in."
    ],
    "explanations": [
      "Wrong — re-running from scratch discards the work already done on visual arts and film. Targeted refinement preserves valid output and only fills the gap.",
      "Wrong — punting to the user for a definition that's already reasonable hides the architectural fix. Iterative refinement is meant to detect and close gaps without user intervention.",
      "Correct — this is the textbook iterative refinement loop. Detect gap → targeted re-delegation to the appropriate subagent → re-invoke synthesis with the new evidence merged in.",
      "Wrong — placeholder TODOs are an admission of failure that ships unfinished work. Refinement must close the gap, not paper over it."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "post"
  },
  {
    "question": "You are researching 'the global semiconductor supply chain in 2025' with three search subagents in parallel. The user cares about both regional differences AND source-type diversity. Which partitioning is most defensible?",
    "options": [
      "All three subagents run the same broad query; rely on synthesis to dedup overlapping results.",
      "Partition by region only — North America, EU, Asia — accepting that source-type diversity will not be ensured.",
      "Partition by source type only — news, industry reports, academic — accepting that regional differences may be uneven.",
      "Combine: partition by region across the three subagents, and within each subagent's brief require diverse source types so each region's evidence is balanced across news/reports/academic."
    ],
    "explanations": [
      "Wrong — same broad query produces overlap; synthesis dedup is a fallback, not a substitute for partitioning. Both axes the user cares about are still under-covered.",
      "Wrong — a region-only partition ignores half of what the user asked for (source diversity). The output will have regional breadth but uneven evidence quality.",
      "Wrong — a source-type-only partition ignores the user's stated regional interest. Evidence will be diverse in source but uneven in regional coverage.",
      "Correct — when the user cares about two orthogonal axes, the coordinator can partition on the primary axis (region) and require diversity on the secondary axis inside each subagent's brief. Both axes are covered without duplication."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "post"
  },
  {
    "question": "A coordinator wants to detect coverage gaps in synthesis output before returning the report to the user. Which signal is the most architecturally sound for this gap-detection step?",
    "options": [
      "Compare the synthesis draft's section headings against the subtopic set the coordinator derived from the original user request, and flag any subtopic without a corresponding section.",
      "Check that the draft's word count exceeds a threshold proportional to the user's expected report length.",
      "Verify the synthesis subagent's response had stop_reason of end_turn rather than tool_use.",
      "Run the synthesis subagent a second time and only return the report if both drafts are textually identical."
    ],
    "explanations": [
      "Correct — comparing the realised sections against the planned subtopic set is exactly what gap detection looks like. It catches sub-domains the coordinator decomposed for but the synthesis dropped.",
      "Wrong — word count is a weak proxy. A draft can hit the word target while completely omitting a sub-domain (it just goes deeper on the others).",
      "Wrong — stop_reason indicates the subagent finished its turn cleanly. It says nothing about whether the content covers all planned sub-domains.",
      "Wrong — re-running for textual identity is fragile (LLMs vary) and doesn't actually check coverage. Two drafts can match each other while both miss the same sub-domain."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "post"
  },
  {
    "question": "A coordinator handles two queries in one session: (a) 'what's the current S&P 500?' and (b) 'summarise the past five quarterly earnings reports for AAPL and synthesise the trend.' How should dynamic subagent selection differ between them?",
    "options": [
      "For (a), invoke only a finance/lookup subagent; for (b), invoke search + docs + synthesis + report subagents.",
      "Always invoke the full pipeline; (a) just produces a shorter report than (b).",
      "Reject (a) entirely — the multi-agent system is over-engineered for a single-step factual lookup.",
      "Run both queries through the same fixed pipeline but cache (a)'s answer for future requests."
    ],
    "explanations": [
      "Correct — dynamic subagent selection picks the smallest subset that satisfies the query. (a) is a single-step lookup; (b) genuinely needs decomposition, evidence, and synthesis.",
      "Wrong — invoking the full pipeline on (a) wastes tokens, introduces irrelevant noise, and produces a worse answer than a direct lookup.",
      "Wrong — refusing trivial queries punishes users for asking simple questions. The right move is to invoke a smaller subagent set, not to reject.",
      "Wrong — caching is orthogonal. The architectural failure is using a fixed pipeline; caching just hides the cost of doing the wrong thing twice."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "post"
  },
  {
    "question": "An engineer reports the multi-agent research system's outputs are 'directionally right but always miss something important.' Logs show the coordinator delegates to search, docs, and synthesis once, then returns the first synthesis draft. What's the most likely fix?",
    "options": [
      "Switch to a more capable model for the synthesis subagent.",
      "Add an iterative refinement loop where the coordinator evaluates each draft for gaps and re-delegates targeted queries to search/analysis subagents until coverage is sufficient.",
      "Increase the number of parallel search subagents from one to three with the same broad query.",
      "Move synthesis behind a separate coordinator so its context is more isolated."
    ],
    "explanations": [
      "Wrong — a stronger model cannot synthesise evidence the search step never gathered. The bug is upstream, in the lack of gap detection and refinement.",
      "Correct — 'always miss something important' is the textbook symptom of no refinement loop. Adding gap detection plus targeted re-delegation closes the gap class without changing the model.",
      "Wrong — more parallel subagents on the same broad query duplicate evidence without expanding coverage. Without partitioning, this is wasted spend.",
      "Wrong — adding another coordinator layer doesn't add gap detection; it adds latency and complexity. The missing pattern is the refinement loop on the existing coordinator."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "post"
  },
  {
    "question": "A team optimised their research system so the search subagent posts results directly to a shared queue that synthesis reads from, bypassing the coordinator. After deployment, intermittent search-subagent errors (rate limits, partial results) cause silent quality drops in the report. What's the architectural diagnosis?",
    "options": [
      "The search subagent needs better retry logic and exponential backoff.",
      "The synthesis subagent should validate evidence quality before incorporating it.",
      "Bypassing the coordinator broke hub-and-spoke; the coordinator can no longer enforce consistent error handling, propagate failures, or detect coverage gaps caused by partial search results.",
      "The shared queue should add a signature field so synthesis can verify source authenticity."
    ],
    "explanations": [
      "Wrong — retry logic on the search subagent treats one symptom but doesn't fix the missing cross-subagent error path. The coordinator should be deciding whether to escalate, retry, or accept partial results.",
      "Wrong — pushing validation down to synthesis ad-hocs the policy. With hub-and-spoke restored, error handling is centralised and consistent across subagents.",
      "Correct — the issue is exactly that the coordinator is no longer in the loop. Hub-and-spoke routes all traffic through the coordinator so it can see partial results, enforce policy, and decide on refinement. Bypass = silent failures.",
      "Wrong — authenticity is unrelated; the bug is in error visibility and propagation, not source verification."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "post"
  },
  {
    "question": "A coordinator invokes a doc-analysis subagent with 'extract key findings from these documents.' The user's actual question — 'what licensing changes did OEMs announce in Q3 2025?' — is in the coordinator's history but never repeated in the subagent's brief. The subagent returns a generic summary. What broke?",
    "options": [
      "The doc-analysis subagent needed a longer context window for the documents provided.",
      "The coordinator assumed the subagent would inherit context implicitly, but subagent context is isolated — the user's question must be passed explicitly in the brief.",
      "'Extract key findings' is the wrong verb; the prompt should have said 'summarise.'",
      "The system prompt is missing few-shot examples of correct extraction format."
    ],
    "explanations": [
      "Wrong — context window size is irrelevant when the subagent doesn't know what to look for. A larger window would still produce a generic summary.",
      "Correct — implicit context inheritance is the canonical anti-pattern. The subagent runs in an isolated context; the user's question is invisible unless the coordinator includes it in the invocation.",
      "Wrong — the verb choice doesn't matter when the subagent has no question to answer. 'Summarise' would produce the same generic output.",
      "Wrong — few-shot examples shape format, not focus. Without the user's question, the subagent will format a generic summary correctly."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "post"
  },
  {
    "question": "Which scenario is the strongest candidate for a coordinator-subagent system rather than a single-agent loop?",
    "options": [
      "A single-step factual lookup such as 'what's TSLA's current price?'",
      "A linear text transformation such as translating one paragraph from English to French.",
      "An open-ended research task spanning multiple source types and sub-domains, where the coordinator must dynamically dispatch subagents and synthesise their findings.",
      "A latency-critical chat exchange where every interaction must respond in under 100 ms."
    ],
    "explanations": [
      "Wrong — a one-shot factual lookup needs neither decomposition nor multi-source synthesis. A single agent (or a single tool call) is right-sized.",
      "Wrong — linear text transformations have no decomposition surface; multi-agent overhead adds latency without improving quality.",
      "Correct — multi-source, multi-sub-domain research is exactly what the coordinator-subagent pattern was designed for: decomposition, scoped delegation, aggregation, and refinement.",
      "Wrong — multi-agent systems add round-trips that fight a 100 ms latency budget. Latency-critical chat is the wrong fit; a single agent with focused tools is more appropriate."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "post"
  },
  {
    "question": "A coordinator has three parallel search subagents available. The user asks for 'a one-pager on solid-state battery adoption in 2025' and emphasises that they want diverse, high-quality evidence (industry, news, academic). Which partitioning best matches that goal?",
    "options": [
      "Partition by subtopic — one subagent per battery chemistry. Source diversity is treated as a secondary concern that synthesis can sort out.",
      "Partition by source type — one subagent on industry reports, one on news, one on academic papers. Each covers the full subject space within its source pool.",
      "Run the same broad query across all three subagents and rely on synthesis to dedup and label sources.",
      "Use only one subagent — partitioning across three over-engineers a one-pager and risks under-using the available budget."
    ],
    "explanations": [
      "Wrong — partitioning by subtopic optimises for sub-domain coverage. The user explicitly cares about source diversity, so source-type partitioning matches their priority better.",
      "Correct — when the user's priority is source diversity, partitioning by source type ensures industry/news/academic each get focused coverage and the synthesis stage receives a balanced evidence pool.",
      "Wrong — same query across all subagents produces duplicates regardless of source pool. The partition has to be in the coordinator's brief, not left to synthesis to recover.",
      "Wrong — dropping to one subagent abandons the parallelism budget. With three available subagents and an explicit diversity goal, partitioning beats serialisation."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.2"
    ],
    "phase": "post"
  },
  {
    "question": "You're configuring a research coordinator agent in the Claude Agent SDK and want it to delegate to specialised subagents. Which entry MUST appear in the coordinator's allowedTools list for delegation to work at all?",
    "options": [
      "\"Task\" — the built-in tool used to spawn subagents.",
      "\"Subagent\" — the dedicated subagent invocation primitive.",
      "\"Delegate\" — the Agent SDK alias for spawning peer agents.",
      "\"web_search\" — the tool that triggers a remote subagent under the hood."
    ],
    "explanations": [
      "Correct — subagent spawning in the Claude Agent SDK is performed via the built-in Task tool. If \"Task\" is missing from allowedTools, the coordinator cannot delegate and will silently try to do everything itself.",
      "Wrong — there is no built-in tool named \"Subagent\" in the Agent SDK. Spawning is done through the Task tool.",
      "Wrong — \"Delegate\" is not an SDK primitive. The mechanism is the Task tool.",
      "Wrong — web_search is a content tool, not a spawning mechanism. Allowing it doesn't enable delegation."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "pre"
  },
  {
    "question": "A coordinator agent emits Task({subagent_type: 'synthesis', prompt: 'synthesise the results'}). The synthesis subagent returns a vague paragraph with no citations. What's the most likely cause?",
    "options": [
      "The Task tool was misconfigured — it should attach the coordinator's recent messages automatically.",
      "The synthesis subagent received only the literal prompt 'synthesise the results' and had no access to prior findings or attribution metadata.",
      "Synthesis subagents in the SDK don't support citations by default.",
      "The coordinator should have used fork_session instead of Task for synthesis."
    ],
    "explanations": [
      "Wrong — the Task tool does not auto-attach prior messages. Subagent context is isolated by design.",
      "Correct — subagents see only what's in the prompt. With no findings, sources, or quality criteria included, the subagent has nothing to synthesise from and no provenance to cite.",
      "Wrong — citations come from passing structured findings with attribution into the prompt; nothing in the SDK forbids them.",
      "Wrong — fork_session is for divergent exploration from a shared baseline; it doesn't fix the fact that no findings were passed."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "pre"
  },
  {
    "question": "Which statement best describes how a subagent obtains the context it needs to do its job?",
    "options": [
      "It inherits the coordinator's full conversation history at spawn time.",
      "It reads from a shared scratchpad that all agents in the system can access.",
      "It receives only what the coordinator explicitly writes into the Task invocation prompt; nothing else is shared.",
      "It picks up context from the most recent prior subagent's output by default."
    ],
    "explanations": [
      "Wrong — subagent context is isolated. Coordinator history is not auto-inherited.",
      "Wrong — there is no implicit shared scratchpad in the SDK's subagent model. Cross-agent state must be passed via the prompt.",
      "Correct — subagents are isolated. The coordinator must hand-pass goal, scope, constraints, and prior findings in the Task prompt.",
      "Wrong — there's no automatic chaining between subagents. Anything one needs from another must be copied explicitly by the coordinator."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "pre"
  },
  {
    "question": "A coordinator wants to run three web search subagents in parallel — one per subdomain. Which approach actually achieves parallel execution in the Agent SDK?",
    "options": [
      "Emit one Task call, wait for the result, emit the next Task call, wait, then the third — relying on async scheduling under the hood.",
      "Emit a single Task call with all three subdomains in the prompt and let the subagent fan out internally.",
      "Set parallel: true on each AgentDefinition and emit calls one at a time.",
      "Emit three Task tool-use blocks in the same coordinator response, so the runtime executes them concurrently."
    ],
    "explanations": [
      "Wrong — that's serial. Each turn waits for the prior result; total latency is 3x a single search.",
      "Wrong — a single subagent runs one prompt; it doesn't fan out into parallel sibling subagents on its own.",
      "Wrong — there is no parallel: true flag on AgentDefinition; parallelism comes from emitting multiple Task calls in one turn.",
      "Correct — parallel subagents in the SDK are achieved by emitting multiple Task tool_use blocks in a single coordinator turn. The runtime runs them concurrently and returns all tool_results together."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "pre"
  },
  {
    "question": "Which is a LEGITIMATE use case for fork_session as opposed to plain Task delegation?",
    "options": [
      "Spawning a fresh, isolated web search subagent that has no need of prior session history.",
      "Comparing three alternative synthesis strategies against the same accumulated analysis baseline without restarting it for each.",
      "Allowing two subagents to send messages to each other directly without going through the coordinator.",
      "Deleting the coordinator's history to free up tokens before a long subtask."
    ],
    "explanations": [
      "Wrong — that's the canonical use case for plain Task. fork_session is overkill and would carry context the search agent doesn't need.",
      "Correct — fork_session branches the current session with all its built-up context, so each fork explores a divergent approach from the same baseline. That's exactly what comparing alternative strategies needs.",
      "Wrong — fork_session does not enable peer-to-peer subagent comms. All comms still flow through the coordinator (hub-and-spoke).",
      "Wrong — fork_session is not a context-clearing tool. It branches, it doesn't truncate."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "pre"
  },
  {
    "question": "An AgentDefinition for a synthesis subagent has allowedTools = []. Is this a configuration bug?",
    "options": [
      "Yes — every subagent must have at least one tool to be invokable.",
      "Yes — without tools the synthesis subagent cannot return a tool_result back to the coordinator.",
      "No — synthesis often reasons over its prompt and needs no tools; an empty allowedTools is a deliberate scoping choice.",
      "No, but only if fork_session is used instead of Task to invoke it."
    ],
    "explanations": [
      "Wrong — there is no minimum-tool requirement. Tool counts are scoped to the agent's role.",
      "Wrong — the subagent's final assistant message is what the coordinator receives as the tool_result, not a tool call from the subagent itself.",
      "Correct — synthesis typically reasons over the prompt's structured findings and emits a final response. No tools are needed, and scoping allowedTools narrowly is the recommended pattern (Task 2.3 — fewer tools per role improves selection reliability).",
      "Wrong — the choice between Task and fork_session is independent of the allowedTools=[] decision."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "pre"
  },
  {
    "question": "A coordinator passes search-subagent findings to a synthesis subagent as a single free-text paragraph: 'Universal banned vocal cloning, studios use GenAI more, 60% of releases ...'. The synthesis report ends up with no citations. What's the structural fix?",
    "options": [
      "Pass each finding in a structured format (e.g., JSON {id, claim, source: {type, name, url, date}}) so synthesis can reference and cite each piece of evidence.",
      "Wrap the paragraph in <evidence>...</evidence> XML tags and the model will infer citations.",
      "Tell the synthesis agent in its system prompt that 'citations are mandatory' — the format of the input doesn't matter.",
      "Switch the synthesis agent to use fork_session so it inherits the search subagent's session and its source list."
    ],
    "explanations": [
      "Correct — structured outputs that separate content from attribution metadata preserve provenance through the synthesis hop. Without IDs and source records, the synthesis agent has no way to cite reliably (Task 5.6).",
      "Wrong — XML tags don't reconstitute lost source records. If URLs and document names aren't in the input, no wrapping recovers them.",
      "Wrong — system-prompt instructions can't manufacture sources that aren't in the input. The structural problem is upstream.",
      "Wrong — fork_session does not give a subagent another subagent's session. Each subagent is its own isolated process."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "pre"
  },
  {
    "question": "The coordinator's system prompt for a research coordinator says: 'First call web_search_agent for music. Then call web_search_agent for film. Then call synthesis_agent. Then call report_agent.' What's the most accurate critique?",
    "options": [
      "The order is wrong — synthesis must come before search.",
      "It uses the wrong tool — Task should be replaced with fork_session in coordinator prompts.",
      "The prompt should specify allowedTools instead of subagent names.",
      "It's procedural rather than goal-oriented; it locks the coordinator into a fixed pipeline and removes dynamic subagent selection and parallel spawning."
    ],
    "explanations": [
      "Wrong — the order is conventional. The deeper issue is that the prompt is procedural at all.",
      "Wrong — fork_session is a divergent-branching tool, not a replacement for Task in routine delegation.",
      "Wrong — allowedTools is set on the AgentDefinition, not steered from the coordinator's runtime prompt; that's not the critique.",
      "Correct — goal-oriented prompts (state goal, scope, quality criteria) let the coordinator dynamically pick subagents and emit parallel Task calls when appropriate. Procedural prompts hard-code a serial pipeline and lose the orchestration benefit."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "pre"
  },
  {
    "question": "Which AgentDefinition field is what the coordinator's model uses to ROUTE a delegation to the right subagent type?",
    "options": [
      "system_prompt — the coordinator reads it to understand subagent capabilities.",
      "allowedTools — the longest tool list wins.",
      "description — a short, role-focused string the coordinator uses to pick subagent_type.",
      "name — the coordinator alphabetises and picks deterministically."
    ],
    "explanations": [
      "Wrong — system_prompt is the subagent's own instructions, not the coordinator's routing signal.",
      "Wrong — allowedTools constrains the subagent's actions, it doesn't drive routing.",
      "Correct — descriptions are the coordinator-visible signal. Vague or overlapping descriptions cause routing errors; specific disjoint descriptions enable reliable subagent selection.",
      "Wrong — names label the AgentDefinition but don't carry the role semantics the coordinator needs to choose between siblings."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "pre"
  },
  {
    "question": "Which of these is an ANTI-PATTERN when invoking subagents from a coordinator?",
    "options": [
      "Including the user's original goal verbatim in every Task prompt to preserve the why.",
      "Scoping each subagent's allowedTools to a small, role-specific list.",
      "Spawning multiple Task calls in a single coordinator turn for parallel work.",
      "Sending each subagent a one-line prompt and assuming they'll infer the goal, scope, and constraints from prior context they don't actually see."
    ],
    "explanations": [
      "Wrong — restating the goal is a recommended practice; subagent context is isolated, so the goal must be in the prompt or it's lost.",
      "Wrong — narrow allowedTools is a best practice (Task 2.3). It prevents cross-specialisation misuse and improves selection reliability.",
      "Wrong — that's how parallelism is achieved in the SDK. It's the correct pattern, not an anti-pattern.",
      "Correct — this is the canonical anti-pattern. Subagents see only what's in the Task prompt; assuming inheritance from prior context is the single biggest cause of off-target subagent output."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "pre"
  },
  {
    "question": "In Scenario 3 (multi-agent research), the coordinator must spawn one search subagent per subdomain (music, film, visual arts) so they execute concurrently. The total wall-clock time should match a single search, not 3x. Which coordinator response shape achieves that?",
    "options": [
      "Three sequential turns, each emitting one Task call and awaiting the tool_result before the next.",
      "A single assistant turn containing three Task tool_use blocks; the runtime executes them concurrently and returns three tool_results together.",
      "One Task call whose prompt asks the search subagent to run all three subdomains internally.",
      "Three fork_session calls in one turn, each branching the coordinator's session for a different subdomain."
    ],
    "explanations": [
      "Wrong — that's serial. Each turn waits for the prior result; total latency is roughly 3x a single search.",
      "Correct — parallel subagent execution in the Agent SDK is achieved by emitting multiple Task tool_use blocks in a single coordinator turn. The runtime runs them concurrently and surfaces all tool_results back together.",
      "Wrong — a single subagent runs one prompt; it can't transparently fan out into three sibling subagents executing concurrently. You'd lose per-subdomain isolation and parallelism.",
      "Wrong — fork_session is for divergent exploration that needs the shared baseline. For routine parallel search per subdomain, plain Task is the right primitive — no need to carry the coordinator's full session into each fork."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "A Scenario 3 synthesis subagent keeps producing reports that conflate findings — two different studios' policies are merged into one bullet, and citations are missing. The coordinator currently passes findings as a paragraph: 'Universal banned vocal cloning. Disney expanded GenAI in pre-vis. Studios increasingly...' Which fix most directly addresses both symptoms?",
    "options": [
      "Add 'do not conflate findings; cite every claim' to the synthesis subagent's system prompt and re-run.",
      "Switch synthesis from Task to fork_session so it inherits the search subagent's source list.",
      "Have the coordinator emit findings to synthesis as structured items: [{id, claim, source: {type, name, url, date}}, ...] so synthesis can reference each by id and preserve attribution.",
      "Increase the coordinator's allowedTools to include web_search so it can re-verify the synthesis output."
    ],
    "explanations": [
      "Wrong — system-prompt instructions can't reconstitute attribution that wasn't passed. The structural problem is upstream of the synthesis prompt.",
      "Wrong — fork_session does not give the synthesis subagent another subagent's session. Subagent isolation isn't bypassable that way.",
      "Correct — structured findings with attribution metadata let synthesis cite each claim by id and prevent merging because each finding is a distinct record. This is the canonical fix from Tasks 1.3 and 5.6.",
      "Wrong — adding tools to the coordinator doesn't fix the synthesis subagent's input format. The problem is the shape of the data it receives, not access to extra tools."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "An engineer ships a research coordinator with allowedTools=['web_search', 'read_document']. The coordinator's traces show no Task tool calls — instead it calls web_search and read_document directly in its own context, which fills up after a few hundred sources. What change is required?",
    "options": [
      "Add 'Task' to the coordinator's allowedTools and move web_search/read_document to the search and document subagents' AgentDefinitions, so the coordinator delegates instead of doing the work itself.",
      "Increase the coordinator's max_tokens; the symptom is just context overflow.",
      "Switch to fork_session for every web_search call; forking will keep each search in its own branch.",
      "Combine web_search and read_document into a single 'research' tool to reduce per-tool context."
    ],
    "explanations": [
      "Correct — without 'Task' in allowedTools the coordinator literally cannot spawn subagents and falls back to in-context work. The fix is structural: enable delegation and scope content tools to the specialist subagents.",
      "Wrong — increasing max_tokens doesn't fix the architectural problem of the coordinator doing work that should be delegated. The coordinator's context still bloats.",
      "Wrong — fork_session forks a session, it doesn't redirect tool calls into specialist subagents. The coordinator still runs the calls in-context.",
      "Wrong — merging tools doesn't restore delegation. The coordinator still does the work in its own context, just with a chunkier tool."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "A team building a research coordinator wrote subagent descriptions: 'researcher_a: helps with research', 'researcher_b: does analysis'. Routing accuracy is poor — the coordinator picks researcher_a for analysis tasks and vice versa. Which fix targets the root cause?",
    "options": [
      "Force tool_choice to a specific subagent_type on every coordinator turn.",
      "Move both subagents under fork_session so the coordinator can compare their outputs.",
      "Make their AgentDefinitions identical so the coordinator can pick either freely.",
      "Rewrite the descriptions to be specific and disjoint — e.g., 'web_search_agent: searches public web for news/industry sources, returns {id, claim, source}' vs 'doc_analysis_agent: extracts findings from supplied document URLs, returns per-document summaries'."
    ],
    "explanations": [
      "Wrong — forcing tool_choice every turn defeats dynamic subagent selection. The coordinator should be able to choose based on the task; the underlying problem is that descriptions don't differentiate.",
      "Wrong — fork_session is for exploration from a shared baseline, not for fixing routing ambiguity between subagent types.",
      "Wrong — making them identical leaves routing arbitrary. The coordinator routes off descriptions; identical descriptions are why the bug exists.",
      "Correct — descriptions are the coordinator's primary routing signal. Specific, disjoint role descriptions let the coordinator pick reliably based on task semantics."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "After a multi-turn analysis, a coordinator wants to compare three alternative report styles (chronological, per-subdomain comparison, per-source-type breakdown) against the SAME accumulated analysis without re-running search and document agents. Which primitive matches?",
    "options": [
      "Three Task calls in one turn, each prompt re-pastes the full accumulated analysis.",
      "Three sequential Task calls, each running its own end-to-end analysis from scratch.",
      "fork_session into three branches; each branch invokes the report subagent with the same baseline context but a different style brief.",
      "A single Task call that asks one report subagent to choose the best of the three styles internally."
    ],
    "explanations": [
      "Wrong — workable but lossy and expensive: re-pasting the full analysis into every Task prompt burns tokens and risks dropping nuance. fork_session is the precise primitive for this case.",
      "Wrong — running the analysis from scratch three times throws away the shared baseline, defeating the purpose of comparing alternatives against the same evidence.",
      "Correct — fork_session branches the current session with all its built-up context, so each fork explores a divergent approach (one report style each) from the same baseline. That's the textbook fork_session use case.",
      "Wrong — a single subagent picking 'the best' internally hides the comparison. The coordinator wanted three concrete outputs to evaluate, not a self-judgment."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "A coordinator tries to push reliability by writing an extremely detailed step-by-step procedure into its subagent prompts: 'open the search tool, type the exact query foo, click first three results, summarise each in 50 words, return as bullet list.' Reliability metrics WORSEN. Why?",
    "options": [
      "The step-by-step prompt exceeds context limits and the subagent can't read it all.",
      "Procedural prompts strip the subagent of adaptability — when the search tool surfaces unexpected shapes (no results, paywalls, redirects) the subagent has no fallback because the procedure didn't anticipate it. Goal-oriented prompts (goal + scope + constraints + quality criteria) let the subagent recover.",
      "The Task tool can't transmit procedures longer than 200 words.",
      "Procedural prompts force tool_choice='any', which disables dynamic tool selection."
    ],
    "explanations": [
      "Wrong — context limits aren't the issue at this scale. The semantic problem is the prompt being procedural at all.",
      "Correct — goal-oriented prompts specify what success looks like and let the subagent pick a path. Procedural prompts lock it into one path that may not match the runtime; edge cases the procedure didn't list become silent failures. Specify goals + quality criteria.",
      "Wrong — there's no 200-word cap on Task prompts. The problem is semantic, not bandwidth.",
      "Wrong — there's no automatic linkage between procedural prompt content and tool_choice. tool_choice is set explicitly."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "Which statement most accurately captures the difference between Task and fork_session?",
    "options": [
      "Task is synchronous; fork_session is asynchronous.",
      "Task always uses the coordinator's allowedTools; fork_session uses a separate allow-list.",
      "Task is for short prompts; fork_session is for prompts longer than the context window.",
      "Task spawns a fresh, isolated subagent that sees only its prompt; fork_session branches the CURRENT session (with all its accumulated context) into independent forks. Task is for routine delegation; fork_session is for divergent exploration from a shared baseline."
    ],
    "explanations": [
      "Wrong — both are coordinator-driven primitives that run subagent work; the sync/async axis isn't the distinction. Parallelism comes from emitting multiple Task calls in one turn.",
      "Wrong — Task uses the spawned subagent's AgentDefinition.allowedTools, not the coordinator's. fork_session inherits the parent session's tool config. The allow-list framing isn't the core difference.",
      "Wrong — there's no length-driven choice between the two. The choice is semantic: fresh isolated context vs branched shared context.",
      "Correct — that's the textbook distinction. Task = isolated subagent, prompt-only context, routine delegation. fork_session = branch current session with its built-up context, for divergent exploration that needs the shared baseline."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "Two senior engineers debate how to invoke the synthesis subagent. Engineer A: 'just include the search results in the synthesis Task prompt verbatim.' Engineer B: 'summarise the search results into a paragraph and pass that — saves tokens.' From a Task 1.3 / 5.6 perspective, who's right and why?",
    "options": [
      "Engineer A — passing structured findings (with attribution) verbatim preserves provenance through the synthesis hop. Summarising drops source IDs, URLs, and dates that synthesis needs to cite correctly; once lost, attribution can't be reconstructed downstream.",
      "Engineer B — the synthesis subagent works better with prose. Summarisation acts as preprocessing.",
      "Neither — the synthesis subagent should be invoked via fork_session so it inherits the search session and bypasses the question entirely.",
      "Both are right; the tradeoff is purely a token-budget decision and either choice is fine."
    ],
    "explanations": [
      "Correct — provenance is the central concern. Pass findings as structured items {id, claim, source: {...}}; synthesis cites by id. Summarising paragraphs drop the metadata, breaking citations and inviting hallucinated sources.",
      "Wrong — synthesis works fine on structured input and emits structured + cited output. Summarisation upstream destroys the metadata.",
      "Wrong — fork_session doesn't share another subagent's session. Subagents are isolated processes.",
      "Wrong — it's not just a token-budget question. Summarising drops attribution, which is a correctness issue, not a cost trade."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "A research coordinator emits Task calls for three subdomains in parallel. They return findings, but the synthesis report still has clear coverage gaps — film coverage is half the depth of music. The three search prompts were nearly identical: 'find recent generative AI adoption news for {subdomain}'. What's the most likely root cause and fix?",
    "options": [
      "fork_session was needed instead of Task; switch all three to fork_session and rerun.",
      "The Task tool was misconfigured; remove and re-add 'Task' to allowedTools to force a refresh.",
      "Per-subdomain prompts didn't carry distinct scope/source-type partitioning, so subdomains with thinner public coverage came back light. Fix: add per-subdomain source-type guidance ('industry trade press for film, GVA reports for visual arts') and quality criteria ('flag findings older than 2024'), and let the coordinator iteratively refine if film comes back thin.",
      "Synthesis must do its own web_search; add web_search to the synthesis subagent's allowedTools."
    ],
    "explanations": [
      "Wrong — fork_session is for divergent exploration of the same baseline; it doesn't fix uneven per-subdomain coverage caused by under-specified search prompts.",
      "Wrong — there's no refresh problem. The Task tool worked; the prompts and refinement loop are at fault.",
      "Correct — uneven coverage is a coordinator-side decomposition issue: the per-subagent prompts didn't carry enough scope/source-type/quality guidance. Goal-oriented per-subdomain prompts plus an iterative refinement loop (Task 1.2) close the gap.",
      "Wrong — pushing search into the synthesis subagent violates role separation (Task 2.3) and fragments evidence collection. The fix lives in the coordinator's prompt design and refinement loop."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "Which of the following best describes the role of an AgentDefinition's `system_prompt` (versus its `description`)?",
    "options": [
      "system_prompt is what the coordinator reads to choose between subagent types; description is the subagent's running instruction set.",
      "system_prompt is the persistent specialisation instruction the SUBAGENT reads (its role, output format, mandatory behaviours); description is the short, role-focused string the COORDINATOR uses to pick which subagent_type to invoke.",
      "system_prompt and description are interchangeable; both are sent to the subagent at spawn time.",
      "system_prompt is only used in fork_session; description is only used by Task."
    ],
    "explanations": [
      "Wrong — that's reversed. The coordinator routes off description; the subagent reads system_prompt.",
      "Correct — clean separation: description routes (coordinator-side signal), system_prompt specialises the subagent (its persistent role and output rules). Vague descriptions cause routing errors; weak system_prompts cause off-target output.",
      "Wrong — they have distinct roles. Description is for routing; system_prompt is for the subagent's runtime behaviour.",
      "Wrong — both fields apply regardless of whether Task or fork_session is used."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "A customer support agent's system prompt says 'always call get_customer before process_refund.' Production traces show that 2 out of 100 sessions still issue refunds without a prior get_customer call. The team has tightened the prompt three times. Which change actually drives the failure rate to zero?",
    "options": [
      "Add a programmatic prerequisite (e.g., a PreToolUse hook on process_refund) that denies the tool call unless session.verified_customer_id is set by a prior get_customer success.",
      "Add a few-shot example of correct ordering to the system prompt and clarify the wording further.",
      "Lower the model temperature so the agent follows instructions more deterministically.",
      "Move the instruction from the system prompt to the user prompt for greater salience."
    ],
    "explanations": [
      "Correct — prompts give probabilistic compliance; gates/hooks give deterministic compliance. For sensitive operations like refunds, only programmatic enforcement eliminates the failure rate.",
      "Wrong — few-shot helps reduce error rate but cannot eliminate it. The question explicitly says three prompt iterations have not closed the gap.",
      "Wrong — temperature affects sampling but does not turn probabilistic compliance into deterministic compliance. The 1-in-50 failure path remains.",
      "Wrong — instruction salience is still prompt-based ordering. It changes the failure rate marginally; it does not make the rate zero."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "pre"
  },
  {
    "question": "Which best describes the difference between programmatic enforcement and prompt-based ordering?",
    "options": [
      "Programmatic enforcement is faster; prompt-based ordering is slower.",
      "Programmatic enforcement uses runtime gates or hooks that prevent disallowed tool calls before they execute, regardless of model behaviour; prompt-based ordering instructs the model and relies on it choosing to comply.",
      "Programmatic enforcement is only for read-only tools; prompt-based ordering is for write tools.",
      "Programmatic enforcement is for free-text outputs; prompt-based ordering is for tool calls."
    ],
    "explanations": [
      "Wrong — neither is materially faster than the other. The distinction is determinism vs probabilism, not latency.",
      "Correct — gates/hooks deny disallowed tool calls deterministically; prompt instructions ask the model to comply and have a non-zero failure rate. That's the central exam intuition.",
      "Wrong — enforcement is most often applied precisely to write tools (refunds, transfers). The read/write split isn't the distinction.",
      "Wrong — both apply to tool calls. Free-text outputs aren't gated by either mechanism."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "pre"
  },
  {
    "question": "Which of the following operations most clearly REQUIRES deterministic (programmatic) enforcement rather than prompt-based ordering?",
    "options": [
      "Selecting which built-in tool (Read vs Grep) to use when exploring a codebase.",
      "Choosing whether to summarise or list a multi-source synthesis.",
      "Calling process_refund without first verifying the customer's identity.",
      "Picking the order in which to phrase a user-facing summary."
    ],
    "explanations": [
      "Wrong — tool selection on read-only built-ins has no compliance impact. Prompt guidance is sufficient.",
      "Wrong — summarisation style is a stylistic prompt concern; failures are cosmetic, not compliance-relevant.",
      "Correct — refunds without identity verification are a financial/compliance failure. The acceptable failure rate is zero, so a hook or prerequisite gate is required.",
      "Wrong — phrasing order is stylistic, not a compliance-grade requirement."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "pre"
  },
  {
    "question": "An agent escalates to a human queue with the payload: { message: 'Customer is frustrated and wants a refund. Please help.' }. The human queue regularly responds with 'who is this customer?' What's the most accurate diagnosis?",
    "options": [
      "The escalation tool is misconfigured; the human queue should accept any payload shape.",
      "The agent should attach the entire conversation transcript instead of a short message.",
      "The model temperature is too low; raise it for more expressive escalations.",
      "The handoff is unstructured — it lacks customer_id, root cause, refund amount, and recommended action. The fix is a structured payload with those fields."
    ],
    "explanations": [
      "Wrong — the receiving system needs structure to act on; accepting any shape would push the burden onto humans to interpret prose every time.",
      "Wrong — humans don't read 40-turn transcripts reliably and the transcript may not even be accessible. Structured fields are the right artifact for handoff.",
      "Wrong — temperature has no bearing on the structural problem. The payload schema is the issue.",
      "Correct — structured handoffs (customer_id verified, root_cause, refund_amount, recommended_action) let the human queue act immediately without re-investigation. Free-text escalations force them to re-investigate."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "pre"
  },
  {
    "question": "A customer messages: 'I was double-charged on INV-7732, the headphones I ordered (O-991) arrived damaged, and my login isn't working.' Which approach best matches Task 1.4's pattern?",
    "options": [
      "Resolve the billing issue first, then ask the customer to re-mention the other concerns later.",
      "Verify the customer once, decompose the message into three concerns, and investigate them in parallel via multiple Task calls in a single coordinator turn — each subagent receives the verified customer_id as shared context.",
      "Ignore the secondary concerns and respond only to the most severe one (the duplicate charge).",
      "Escalate the entire message to a human immediately because multi-concern requests are too complex for an agent."
    ],
    "explanations": [
      "Wrong — that's serial firefighting. The customer has to repeat themselves; trust drops; subsequent concerns get lost.",
      "Correct — decompose up front, parallel Task calls in one coordinator turn (Task 1.3 mechanics), shared context (verified customer_id) so each sub-investigation operates on the same verified account without re-verifying.",
      "Wrong — silently dropping concerns is the worst failure mode. The agent must address all three.",
      "Wrong — multi-concern requests are exactly what coordinator-driven decomposition is for. Escalation is reserved for cases the agent genuinely cannot resolve."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "pre"
  },
  {
    "question": "Why pass `customer_id` (the verified ID) rather than the customer's name in an escalation handoff?",
    "options": [
      "Customer names are too long for the handoff schema.",
      "The model can't read names reliably.",
      "Names are ambiguous (multiple John Smiths, typos, nicknames); a verified ID uniquely identifies the account so the receiving human/system operates on the correct one.",
      "It's a localisation requirement — names don't translate across regions."
    ],
    "explanations": [
      "Wrong — payload size is not a meaningful constraint here.",
      "Wrong — the model reads names just fine. The issue is unambiguous receiver-side identification.",
      "Correct — names introduce ambiguity at the human-handoff hop; verified IDs eliminate that. This is exactly the kind of ambiguity Task 1.4 and Task 5.2 (escalation/ambiguity) flag as a structural issue.",
      "Wrong — there's no localisation rule at play; the pattern applies in any region."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "pre"
  },
  {
    "question": "Which of these is correctly described as PROGRAMMATIC enforcement (rather than prompt-based ordering)?",
    "options": [
      "A PreToolUse hook on process_refund that denies the call unless session.verified_customer_id is set by a prior get_customer success.",
      "A system-prompt sentence saying 'always verify identity before processing refunds.'",
      "A few-shot example showing the correct ordering of get_customer before process_refund.",
      "Lowering the model temperature to encourage stricter instruction-following."
    ],
    "explanations": [
      "Correct — runtime hooks that intercept and deny tool calls are the canonical example of programmatic enforcement. The model can't route around them.",
      "Wrong — that's prompt-based ordering. Probabilistic compliance only.",
      "Wrong — few-shot helps but it's still prompt content. Probabilistic compliance.",
      "Wrong — temperature is a sampling parameter, not an enforcement mechanism."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "pre"
  },
  {
    "question": "Which of the following is most LIKELY to be a wrong-answer distractor on the exam (i.e., an anti-pattern)?",
    "options": [
      "Verifying the customer once with get_customer and storing the verified ID in session state, then enforcing it via a PreToolUse gate on process_refund.",
      "Decomposing a multi-concern message into items and emitting parallel Task calls with shared context.",
      "Tightening the system prompt yet again to fix an agent that occasionally skips identity verification.",
      "Building structured escalation payloads with customer_id, root cause, refund amount, and recommended action."
    ],
    "explanations": [
      "Wrong (i.e., this is a correct pattern, not the distractor).",
      "Wrong (this is a correct pattern, not the distractor).",
      "Correct — that's the canonical anti-pattern. Repeated prompt iteration on a determinism failure is a sign you're using the wrong tool. The fix is a hook/gate, not yet another prompt edit.",
      "Wrong (this is a correct pattern, not the distractor)."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "pre"
  },
  {
    "question": "An agent decomposes a multi-concern customer message into three concerns and emits three parallel Task calls. Each Task prompt independently asks the subagent to call get_customer to verify the customer. What's the structural problem?",
    "options": [
      "The Task tool doesn't support parallel calls; this design is fundamentally broken.",
      "The subagents will each call process_refund.",
      "Re-verifying identity in each subagent wastes tokens and risks divergent interpretations of the customer; the verified customer_id should be passed as shared context once and never re-verified per subagent.",
      "Subagents must receive the entire conversation transcript to function correctly."
    ],
    "explanations": [
      "Wrong — the SDK supports multiple Task calls in one coordinator turn (Task 1.3). That's not the issue.",
      "Wrong — they would only call process_refund if their concern requires one and the gate allows it. Refund itself is not the problem here.",
      "Correct — verify once at the coordinator level, store verified_customer_id, pass it as shared context to each parallel Task. Re-verifying invites divergence and wastes tokens.",
      "Wrong — subagents need only the verified ID, the specific concern, and quality criteria — not the entire transcript. Task 1.3's isolated-context principle still applies."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "pre"
  },
  {
    "question": "A `process_refund` PreToolUse gate currently checks only 'is session.verified_customer_id non-null?' During a load test, a refund slipped through where the verified customer was C-48201 but the order belonged to C-49000. What's the gate-composition fix?",
    "options": [
      "Remove the gate — it's clearly broken and the model can self-correct.",
      "Tighten the gate to also verify that the order's customer matches the session's verified customer (i.e., compose preconditions: verified_customer_id non-null AND order.customer_id == verified_customer_id AND amount within policy).",
      "Replace the gate with a stronger system-prompt instruction.",
      "Switch from PreToolUse to PostToolUse — denying after the refund issues is safer."
    ],
    "explanations": [
      "Wrong — removing the gate makes the failure mode more frequent, not less. The agent without enforcement is exactly the original problem this task is about.",
      "Correct — gates should compose all preconditions, not a partial subset. Verified customer + matching order ownership + amount within policy. Each precondition is a small deterministic check.",
      "Wrong — prompt instructions don't give deterministic compliance. Gate composition does.",
      "Wrong — PostToolUse fires after the side effect; for financial operations the refund has already issued by then. PreToolUse is the right hook for blocking."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "pre"
  },
  {
    "question": "Scenario 1: a customer support agent regularly issues small refunds (under $50) without first calling get_customer. The product team has tightened the system prompt twice and added few-shot examples; the failure rate dropped from 4% to 0.5% but never reaches zero. Compliance requires zero. What change to the architecture closes the gap?",
    "options": [
      "Add an additional few-shot example to the system prompt; the rate is approaching zero asymptotically and one more example will get it there.",
      "Add a PreToolUse hook on process_refund that denies the call when session.verified_customer_id is null. Pair with a PostToolUse hook on get_customer that sets the verified_customer_id on success.",
      "Lower the model's temperature from 0.7 to 0.0 to make instruction-following deterministic.",
      "Move identity verification responsibilities into a separate subagent that the main agent invokes via Task before any refund."
    ],
    "explanations": [
      "Wrong — few-shot reduces probabilistic failure; it cannot eliminate it. Prompt-based ordering has a non-zero floor.",
      "Correct — programmatic enforcement (the PreToolUse gate driven by session state set by a PostToolUse hook on get_customer) is the only way to drive the failure rate to zero. This is the canonical Task 1.4 / 1.5 pattern.",
      "Wrong — temperature 0.0 reduces variance but does not turn probabilistic compliance into deterministic compliance. The model can still emit a refund call without prior get_customer.",
      "Wrong — pushing the verification into a subagent doesn't enforce ordering. The main agent can still call process_refund without having invoked the verification subagent first."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "post"
  },
  {
    "question": "Which of the following is most clearly an example of a STRUCTURED handoff payload appropriate for a Scenario 1 escalation?",
    "options": [
      "{ message: 'The customer Jane Roberts is upset about a billing issue and wants someone to call her back. She gave her phone number in the chat.' }",
      "{ customer: 'Jane Roberts', issue: 'billing', urgency: 'high' }",
      "{ customer_id: 'C-48201', root_cause: 'MFA lockout — agent has no permission to clear', refund_amount: null, recommended_action: 'Clear MFA lockout via admin console; verify with customer by call-back', transcript_summary: '...', investigation_state: { verified_customer_id: 'C-48201', concern_id: 'login_issue' } }",
      "{ description: 'see chat log', priority: 1 }"
    ],
    "explanations": [
      "Wrong — free-text prose with a name (not an ID), no structured fields. Forces the receiver to re-investigate.",
      "Wrong — uses customer name (ambiguous) instead of verified ID, no recommended action, no root cause, no refund amount. Halfway structured but missing the load-bearing fields.",
      "Correct — verified customer_id, explicit root cause, refund amount (or null), recommended action, transcript summary, and investigation state. The receiver can act immediately without reading the transcript.",
      "Wrong — 'see chat log' is the canonical anti-pattern. Receiver has no actionable structure."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "post"
  },
  {
    "question": "An agent is asked to handle a customer message that contains three independent concerns: a billing dispute, a damaged-goods return, and a login issue. Which orchestration pattern matches Task 1.4's recommendation?",
    "options": [
      "Call get_customer once to verify identity, decompose the message into three concerns, emit three Task calls in one coordinator turn (each carrying the verified customer_id as shared context), then synthesise a single response that addresses all three concerns.",
      "Resolve the concerns one at a time in three separate coordinator turns; the customer can wait while each concern is handled fully before moving to the next.",
      "Spawn three Task calls but skip the get_customer step in the coordinator; let each subagent verify identity independently to keep responsibilities scoped.",
      "Pass the entire conversation history to a single mega-subagent that produces a comprehensive response."
    ],
    "explanations": [
      "Correct — verify once, decompose, parallel Task calls with shared context, single synthesised response. This is the textbook Task 1.4 pattern (with Task 1.3 mechanics for the parallelism).",
      "Wrong — serial firefighting drops concerns 2 and 3, forces the customer to repeat themselves, and degrades trust. Not the recommended pattern.",
      "Wrong — re-verifying per subagent wastes tokens and risks divergent identity interpretations. The whole point of shared context is to verify once.",
      "Wrong — single mega-subagent with full history defeats decomposition; you lose parallelism and per-concern scope. It's also inefficient on tokens."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "post"
  },
  {
    "question": "Identify the BEST description of why prompt instructions alone are insufficient for compliance-grade workflow ordering.",
    "options": [
      "Prompts are processed only at the start of the conversation; mid-conversation instructions don't apply.",
      "Prompts are stored on the server and may be lost during failover.",
      "Prompts can't include conditional logic.",
      "Prompts give probabilistic compliance — even a well-written instruction has a non-zero failure rate. For operations like identity verification before financial actions, the acceptable failure rate is zero, which only programmatic enforcement (gates/hooks) can guarantee."
    ],
    "explanations": [
      "Wrong — system prompts apply throughout the conversation. Persistence isn't the issue.",
      "Wrong — prompt persistence isn't the issue. Determinism is.",
      "Wrong — prompts can include conditionals expressed in natural language. The issue isn't expressiveness; it's that compliance is probabilistic.",
      "Correct — the central distinction. Probabilistic compliance is fine for stylistic preferences; deterministic compliance via gates/hooks is required when the failure rate must be zero."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "post"
  },
  {
    "question": "A senior engineer proposes: 'Let's simplify the refund flow — the gate currently checks verified_customer_id, order.customer_id matches verified_customer_id, AND amount within policy. Let's drop the order-customer match check; the agent's prompt already says it should only refund the customer's own orders.' What's the right pushback?",
    "options": [
      "Agree — composing many small checks creates latency and the prompt covers it well enough.",
      "Disagree — but for unrelated reasons; the real issue is that PostToolUse should be used instead of PreToolUse.",
      "Disagree — gates must compose ALL preconditions, not a subset. Dropping the order-customer match check reintroduces the failure mode where a verified customer can trigger refunds against orders they don't own. Prompt instructions cannot deterministically prevent this.",
      "Agree — prompts have improved enough in modern models that the order-customer check is redundant."
    ],
    "explanations": [
      "Wrong — gate latency is negligible; the real reason to keep the check is correctness, not performance.",
      "Wrong — PostToolUse fires after side effects; for refunds the money has already moved. PreToolUse with composed checks is correct.",
      "Correct — gate composition is the whole point: each gate must cover all preconditions, otherwise an attacker or a confused agent can route around the missing check. Prompts can't substitute for the missing precondition.",
      "Wrong — model improvements reduce probabilistic error rates but don't make probabilistic compliance deterministic. Gates are the boundary."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "post"
  },
  {
    "question": "Which of the following is the strongest indicator that a workflow needs PROGRAMMATIC enforcement rather than prompt-based ordering?",
    "options": [
      "The workflow has more than five steps.",
      "A single failure has financial, compliance, or safety consequences (e.g., refund issued to the wrong account, identity-verification skipped before a transfer).",
      "The workflow involves more than one agent.",
      "The workflow uses fork_session for divergent exploration."
    ],
    "explanations": [
      "Wrong — number of steps isn't the criterion. A 3-step workflow with a financial action still needs enforcement; a 30-step exploratory workflow may not.",
      "Correct — when a single failure is unacceptable (financial, compliance, safety), only programmatic enforcement gives a zero failure rate. This is the canonical decision criterion.",
      "Wrong — multi-agent workflows benefit from coordination patterns but enforcement is decided by failure-cost, not agent count.",
      "Wrong — fork_session is a session-management primitive, unrelated to whether enforcement is required."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "post"
  },
  {
    "question": "An agent's escalate_to_human payload includes customer_name = 'John Smith' instead of the verified customer_id. The human queue keeps operating on the wrong account. Which fix most directly addresses the root cause?",
    "options": [
      "Train the human queue to verify the name against the chat transcript before acting.",
      "Add a 'last 4 digits of card' field alongside the name to disambiguate.",
      "Add the customer's email address to the payload as a secondary identifier.",
      "Replace customer_name with customer_id (the verified ID returned by get_customer). Names are ambiguous; verified IDs aren't."
    ],
    "explanations": [
      "Wrong — pushes the burden onto humans and assumes transcript access. The structural problem is that the payload uses an ambiguous identifier in the first place.",
      "Wrong — partial fix that still uses name as primary key. Adding more identifiers is fragile compared to using the verified ID directly.",
      "Wrong — email is also potentially ambiguous (shared accounts, role addresses, typos) and isn't the verified identifier returned by get_customer.",
      "Correct — the verified customer_id is the canonical, unambiguous identifier. Use it as the primary handoff key. Names go in human-readable summaries, not as routing keys."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "post"
  },
  {
    "question": "Compare PROMPT-BASED ordering and PROGRAMMATIC enforcement on the dimension that matters most for compliance: failure rate. Which statement is correct?",
    "options": [
      "Prompt-based ordering has a non-zero probabilistic failure rate even with strong instructions, few-shot examples, and low temperature; programmatic enforcement (PreToolUse hooks, prerequisite gates) drives the failure rate to zero for the gated tool calls.",
      "Both have identical failure rates in production; the choice is purely a stylistic preference.",
      "Programmatic enforcement has a higher failure rate because gates can themselves fail.",
      "Prompt-based ordering is strictly better because it preserves model agency."
    ],
    "explanations": [
      "Correct — that's the central trade. Prompts give probabilistic compliance; gates give deterministic. For compliance-grade operations, only deterministic compliance is acceptable.",
      "Wrong — failure rates differ materially. Prompts fail ~1–5% on tightly-instructed sensitive ops; well-written gates fail ~0%.",
      "Wrong — gate failures are rare and observable (the deny shows up in traces). Prompt failures are silent and recurring.",
      "Wrong — model agency is preserved on the open parts of the workflow; gates only constrain a small set of sensitive operations. The trade is correctness, not agency."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "post"
  },
  {
    "question": "An engineer suggests: 'When the customer raises three concerns, just let the agent handle them in order — that way the conversation feels natural.' Which Task 1.4 / Scenario 1 critique is most accurate?",
    "options": [
      "Disagree — but only because parallelism is faster. Latency is the only relevant axis.",
      "Agree — natural conversation flow is paramount; serial handling is fine.",
      "Disagree — serial firefighting consistently drops later concerns: by the time the agent finishes the first investigation, scope has shifted and concerns 2 and 3 are forgotten or partially addressed. The 1.4 pattern is decompose-up-front, parallel investigations with shared context, single synthesised response.",
      "Disagree — the agent should escalate any multi-concern message to a human queue."
    ],
    "explanations": [
      "Wrong — latency is a secondary benefit. The primary issue with serial handling is that concerns get dropped, not that it's slow.",
      "Wrong — a coherent unified response that addresses all three concerns IS natural conversation; what's unnatural is forgetting two of them mid-thread.",
      "Correct — parallel decomposition with shared context is the pattern precisely because it prevents the drop-of-concerns failure mode that serial firefighting reliably produces.",
      "Wrong — the agent should attempt to resolve what it can; escalation is reserved for concerns the agent genuinely cannot resolve, not for the multi-concern shape itself."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "post"
  },
  {
    "question": "Which combination of SDK primitives most directly implements 'no refund without prior verified identity, and refunds must be on the verified customer's own orders'?",
    "options": [
      "A system prompt that names the rules clearly; the agent follows them.",
      "A PostToolUse hook on get_customer that records session.verified_customer_id, plus a PreToolUse hook on process_refund that denies the call unless verified_customer_id is set AND the order's customer_id matches AND the amount is within policy.",
      "A few-shot prompt with three examples of correct ordering.",
      "A PreToolUse hook on get_customer to reject malformed inputs."
    ],
    "explanations": [
      "Wrong — system-prompt rules are probabilistic. The two conditions ('verified identity' AND 'order belongs to that identity') are exactly the kind of compliance-grade preconditions that need enforcement.",
      "Correct — PostToolUse on get_customer sets the session state; PreToolUse on process_refund composes all preconditions (verified, ownership, policy amount). This is the canonical Task 1.4 + 1.5 pattern.",
      "Wrong — few-shot reduces error rate but doesn't eliminate it. Doesn't satisfy 'no refund without' as a guarantee.",
      "Wrong — guarding get_customer's input format doesn't enforce ordering or ownership for process_refund. Wrong tool, wrong scope."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.4"
    ],
    "phase": "post"
  },
  {
    "question": "Three MCP tools used by a research agent return `published_at` in three different formats: ISO 8601 string, Unix epoch seconds, and a human-readable date. The synthesis subagent occasionally sorts findings incorrectly. Which Agent SDK pattern most directly fixes this?",
    "options": [
      "A `PostToolUse` hook (one per tool, or a shared hook that branches by tool name) that normalizes `published_at` to a canonical format (ISO 8601 UTC) before the result is appended to the conversation.",
      "A `PreToolUse` hook on each tool that rewrites the tool input to request ISO 8601 explicitly.",
      "A stronger system prompt that tells the synthesis subagent to interpret each format correctly.",
      "Adding a few-shot example showing how to compare the three formats."
    ],
    "explanations": [
      "Correct — `PostToolUse` runs after the tool executes and can rewrite the response before it enters context. Normalizing at the boundary means the model only ever sees the canonical format. Synthesis-stage interpretation is too late.",
      "Wrong — `PreToolUse` modifies the call, not the response. The MCP tools return the formats they return; you can't generally rewrite their inputs to change the output schema.",
      "Wrong — prompt-based interpretation is probabilistic. The model usually gets it right; the times it doesn't are silent bugs. A normalization hook removes the heterogeneity from context entirely.",
      "Wrong — few-shot reduces but doesn't eliminate the failure rate. Heterogeneity at the boundary is a structural problem; structural fixes use hooks."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "pre"
  },
  {
    "question": "A `PreToolUse` hook on `process_refund` returns `{ permissionDecision: 'deny', reason: 'policy' }`. The model keeps re-emitting the same `process_refund` call on subsequent turns. What's the most accurate diagnosis?",
    "options": [
      "The model is malfunctioning; switch to a different model.",
      "The deny reason is too terse to redirect the model. Replace it with a corrective instruction that names the alternative workflow (e.g., 'Refunds over $500 require manager approval; use escalate_to_human with a structured payload').",
      "`PreToolUse` denies are not actually surfaced to the model; use a `PostToolUse` hook instead.",
      "The hook should silently allow the call and log the violation for later review."
    ],
    "explanations": [
      "Wrong — the model is behaving as expected: it tried, got an opaque deny, and tried again. Model swap doesn't fix the missing redirect.",
      "Correct — the deny reason is the model's next-turn input. A useful reason names what the model should do instead (alternative tool, redirect path). Terse reasons leave the model stuck retrying.",
      "Wrong — `PreToolUse` deny reasons ARE surfaced to the model. The redirect signal flows through that reason field.",
      "Wrong — silent allow defeats the safety boundary. The whole point of the hook is to block the policy-violating call."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "pre"
  },
  {
    "question": "Which best describes the difference between `PreToolUse` and `PostToolUse` hooks?",
    "options": [
      "`PreToolUse` is for read tools; `PostToolUse` is for write tools.",
      "`PreToolUse` is synchronous; `PostToolUse` is asynchronous.",
      "`PreToolUse` fires before a tool call executes (and can deny or modify the call); `PostToolUse` fires after the tool has executed (and can transform the result before the model sees it).",
      "`PreToolUse` runs once per session; `PostToolUse` runs once per turn."
    ],
    "explanations": [
      "Wrong — both hooks apply across read and write tools; the read/write split isn't the distinction.",
      "Wrong — sync/async isn't the structural distinction. Both can be implemented synchronously.",
      "Correct — that's the lifecycle difference. `PreToolUse` is the enforcement point on the call; `PostToolUse` is the transform point on the response.",
      "Wrong — both hooks fire per tool call, not per session or turn."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "pre"
  },
  {
    "question": "An exam scenario describes: 'The agent must NOT issue refunds over $500 under any circumstance, even if the customer is persistent.' The available options include three prompt-engineering tweaks and one Agent SDK approach. Which is the right answer?",
    "options": [
      "Add a few-shot example of refusing a $700 refund.",
      "Tighten the system prompt to forbid refunds over $500.",
      "Lower the model's temperature to make it more compliant.",
      "Add a `PreToolUse` hook on `process_refund` that denies the call when amount > 500 and redirects via the deny reason to `escalate_to_human`."
    ],
    "explanations": [
      "Wrong — few-shot reduces the failure rate but doesn't make it zero. 'Under any circumstance' rules out probabilistic compliance.",
      "Wrong — system-prompt prohibitions are probabilistic. The phrase 'under any circumstance' signals a guarantee requirement.",
      "Wrong — temperature affects sampling; it doesn't turn probabilistic compliance into deterministic compliance. A non-zero floor remains.",
      "Correct — `PreToolUse` interception with a redirect is the deterministic answer. The question stem ('must not... under any circumstance') is the canonical 'use a hook' signal."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "pre"
  },
  {
    "question": "A `PostToolUse` hook on a search tool drops 35 of 40 returned fields, keeping only `id`, `title`, `url`, `snippet`, `published_at`. What's the primary architectural benefit?",
    "options": [
      "It speeds up the network call to the search tool.",
      "It reduces context bloat and improves the model's attention to the relevant fields, preventing lost-in-the-middle effects (Task 5.1).",
      "It changes the search tool's behaviour to return fewer fields in subsequent calls.",
      "It signals to the model that the search tool is unreliable."
    ],
    "explanations": [
      "Wrong — the hook fires after the tool returns; the network call is already done. No speedup.",
      "Correct — verbose tool output dilutes attention and can push key fields into the lost-in-the-middle zone. Trimming at the `PostToolUse` boundary keeps context lean and relevant.",
      "Wrong — the hook only affects what the model sees, not the tool's external behaviour. The tool still returns 40 fields; the model just sees 5.",
      "Wrong — there's no implicit unreliability signal from trimming. Trimming is a context-management discipline, not a quality signal."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "pre"
  },
  {
    "question": "Which scenario is BEST handled by a system-prompt instruction rather than a hook?",
    "options": [
      "Refunds over $500 must always escalate.",
      "Identity verification must precede any financial operation.",
      "Response tone should be friendly and concise.",
      "Tool outputs with Unix timestamps must always be normalized to ISO 8601."
    ],
    "explanations": [
      "Wrong — 'must always' signals a guarantee. Use a `PreToolUse` hook on `process_refund`.",
      "Wrong — financial-operation precondition is a guarantee. Use a prerequisite gate (Task 1.4) implemented via `PreToolUse`.",
      "Correct — tone is a stylistic preference where probabilistic compliance is acceptable. System-prompt instruction is the right level. A hook is overkill.",
      "Wrong — timestamp normalization is a structural data-shape problem. Use a `PostToolUse` hook so the model never sees the heterogeneous formats."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "pre"
  },
  {
    "question": "The agent's research synthesis is producing inconsistent date sorting because three search-side MCP tools return dates differently. A team lead suggests 'rewrite the synthesis prompt to handle each format.' Which critique most accurately captures why this is the wrong layer?",
    "options": [
      "Synthesis-stage normalization is too late: by the time data reaches the synthesis prompt, the heterogeneous formats have already entered context across multiple turns. Normalize at the `PostToolUse` boundary so the model never sees raw heterogeneity.",
      "Synthesis subagents in the SDK don't support date arithmetic at all; the team should pre-compute dates before invocation.",
      "The team should remove dates from the synthesis altogether — they aren't required for the report.",
      "Date heterogeneity is a non-issue; it only feels broken because the synthesis prompt is too short."
    ],
    "explanations": [
      "Correct — normalize at the source (the `PostToolUse` boundary), not at the consumer. Synthesis can't reliably reconstruct what the boundary should have homogenised in the first place.",
      "Wrong — synthesis subagents reason over text; they can do date arithmetic when the format is consistent. The issue is the inconsistency, not the capability.",
      "Wrong — dates are part of the user's request (chronological reporting). Removing them ducks the structural issue.",
      "Wrong — heterogeneity is a real, repeatable bug. Prompt length doesn't fix it."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "pre"
  },
  {
    "question": "A team writes ten near-identical `PostToolUse` hooks, one per MCP tool, each normalizing the same `published_at` field to ISO 8601. What's the recommended refactor?",
    "options": [
      "Disable normalization in nine of the ten and let the model handle the others probabilistically.",
      "Move normalization into a `PreToolUse` hook on each tool.",
      "Move normalization into the system prompt and remove the hooks.",
      "Replace the ten hooks with a single shared `PostToolUse` hook that branches by tool name (or by field heuristics) and applies the canonical format in one place."
    ],
    "explanations": [
      "Wrong — that reintroduces heterogeneity for nine of ten tools. The whole point is consistency.",
      "Wrong — `PreToolUse` operates on the input, not the response. It cannot retroactively rewrite output formats.",
      "Wrong — prompt instructions give probabilistic compliance, not deterministic. Hooks are the right layer.",
      "Correct — a shared hook with one canonical-format definition is easier to evolve and less prone to copy-paste drift between tools. The branching keeps it generic without losing per-tool specificity."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "pre"
  },
  {
    "question": "Which hook lifecycle position is correct for BLOCKING a tool call before any side effect occurs (e.g., before a refund actually issues)?",
    "options": [
      "`Notification` — fires when the user is notified of an action.",
      "`Stop` — fires when the agent stops running.",
      "`PreToolUse` — fires before the tool executes; can deny the call and prevent the side effect.",
      "`PostToolUse` — fires after the tool executes; can transform the response."
    ],
    "explanations": [
      "Wrong — `Notification` is unrelated to tool execution.",
      "Wrong — `Stop` fires at session termination; far too late to block a tool call.",
      "Correct — `PreToolUse` is the enforcement point. Deny here and the side effect never happens.",
      "Wrong — `PostToolUse` fires after the side effect. For a refund, the money has already moved by then. Use `PreToolUse` for blocking."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "pre"
  },
  {
    "question": "Which is an ANTI-PATTERN for `PreToolUse` hook design?",
    "options": [
      "Composing multiple preconditions in the gate (verified customer + ownership match + amount within policy).",
      "Returning a deny reason that names the alternative workflow the model should pursue.",
      "Using `PreToolUse` for prerequisite gates (e.g., requiring `verified_customer_id` before `process_refund`).",
      "Returning a deny reason like `'reason': 'policy'` with no redirect — leaving the model to retry the same call repeatedly."
    ],
    "explanations": [
      "Wrong (this is a correct pattern, not an anti-pattern).",
      "Wrong (this is a correct pattern, not an anti-pattern).",
      "Wrong (this is a correct pattern, not an anti-pattern).",
      "Correct — terse reasons without redirects leave the model stuck. The deny reason is the model's next-turn input; use it to redirect to an allowed path."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "pre"
  },
  {
    "question": "Scenario 1: a customer requests a $750 refund. Policy: refunds over $500 require manager approval. The agent currently has no `PreToolUse` hook on `process_refund`. Which is the production-ready fix?",
    "options": [
      "Add a few-shot example of refusing a $700 refund and lower the model temperature to 0.0.",
      "Add a `PreToolUse` hook on `process_refund` that denies when amount > $500, with a deny reason like: 'Refunds over $500 require manager approval. Use escalate_to_human with a structured handoff (customer_id, root_cause, refund_amount=750.00, recommended_action).'",
      "Remove `process_refund` from the agent's `allowedTools` entirely so it never has access; force every refund through the human queue.",
      "Add a `PostToolUse` hook on `process_refund` that detects amounts > $500 and emits a warning."
    ],
    "explanations": [
      "Wrong — both are probabilistic. Few-shot reduces failure rate but doesn't eliminate it; temperature 0.0 doesn't change the failure floor for compliance-grade rules.",
      "Correct — `PreToolUse` interception with a redirect to `escalate_to_human`. The deny reason is the model's next-turn input; naming the alternative workflow makes the redirect graceful for the customer.",
      "Wrong — over-correction that loses self-service for the 80% of legitimate refunds under $500. The correct boundary is amount-based, not tool-removal.",
      "Wrong — `PostToolUse` fires AFTER the refund has executed. The money has already moved; a warning is too late."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "A research coordinator's three search MCP tools return `published_at` in ISO 8601, Unix epoch seconds, and Unix epoch milliseconds respectively. Synthesis sometimes orders findings incorrectly. Which is the BEST architectural fix?",
    "options": [
      "Add a per-tool `PreToolUse` hook that rewrites the call to request ISO 8601 explicitly.",
      "Tell the synthesis subagent in its system prompt to interpret each format and convert internally before sorting.",
      "Add a `PostToolUse` hook (one shared, branching by tool name or by numeric heuristics) that normalizes `published_at` to ISO 8601 UTC before the response enters context.",
      "Remove `published_at` from synthesis input — the model can infer recency from the surrounding text."
    ],
    "explanations": [
      "Wrong — `PreToolUse` modifies the input, but most MCP tools don't accept format-selection parameters. You can't generally force the response shape via the call.",
      "Wrong — synthesis-stage normalization is too late. The heterogeneous formats have already entered context across multiple turns; bugs are subtle and intermittent.",
      "Correct — normalize at the boundary so the model only ever sees the canonical format. Single shared hook with per-tool branching is easier to evolve than N parallel hooks.",
      "Wrong — recency inference from prose is unreliable; published_at is structured data and should be respected, not discarded."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "Which combination of hook events MOST DIRECTLY implements 'no refund without prior verified identity'?",
    "options": [
      "A `PostToolUse` hook on `get_customer` that records `session.verified_customer_id` on success, paired with a `PreToolUse` hook on `process_refund` that denies the call when `verified_customer_id` is null.",
      "A `PreToolUse` hook on both `get_customer` and `process_refund` that logs every call.",
      "A `PostToolUse` hook on `process_refund` that retroactively reverses refunds without verified identity.",
      "A system-prompt instruction telling the agent to always verify before refunding."
    ],
    "explanations": [
      "Correct — the `PostToolUse` on `get_customer` lifts the verification result into session state; the `PreToolUse` on `process_refund` reads that state to enforce the precondition. Deterministic guarantee.",
      "Wrong — logging doesn't enforce. Compliance requires a deny path, not just observability.",
      "Wrong — retroactive reversal is far worse than blocking. Side effects (refunds, customer notifications, accounting entries) have already happened.",
      "Wrong — prompt instructions are probabilistic. The exam stem implies a guarantee requirement; only hooks meet it."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "Which best explains why hook deny `reason` text should be a corrective instruction (e.g., 'Use escalate_to_human with the following structured payload...') rather than terse error code text ('policy' / 'denied')?",
    "options": [
      "Reasons are logged for compliance audits and need to be human-readable.",
      "The Agent SDK refuses denies with reasons under five words.",
      "Customers see the reason text directly in the chat.",
      "The deny reason is appended as the tool result the model reads on its next turn; a corrective instruction lets the model redirect to an allowed path. Terse reasons leave the model stuck retrying or failing visibly."
    ],
    "explanations": [
      "Wrong — audit logging is a side benefit but not the primary design driver. The model-facing function dominates.",
      "Wrong — there's no minimum-length restriction in the SDK.",
      "Wrong — the deny reason is internal (model-facing). Customer-facing messages are the agent's subsequent assistant output.",
      "Correct — the reason is the model's input. Naming the alternative workflow turns a block into a productive redirect."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "Which is the strongest signal in a question stem that the right answer involves a HOOK rather than a system-prompt instruction?",
    "options": [
      "The agent should usually verify identity before processing refunds.",
      "Prefer a friendly tone in customer-facing replies.",
      "Refunds must not exceed $500 under any circumstance; a compliance audit will check this.",
      "The agent should generally call get_customer first."
    ],
    "explanations": [
      "Wrong — 'usually' implies probabilistic acceptable. Prompt territory.",
      "Wrong — tone is stylistic; probabilistic compliance is fine. Prompt territory.",
      "Correct — 'must not... under any circumstance' + 'compliance audit' = guarantee requirement. Only `PreToolUse` interception delivers a zero failure rate.",
      "Wrong — 'generally' implies probabilistic acceptable. Prompt territory."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "An agent's MCP tool `get_invoice` returns `issued_at` as Unix epoch seconds and `due_at` as Unix epoch milliseconds (mixed). What's the BEST `PostToolUse` hook design?",
    "options": [
      "Drop `due_at` to avoid confusion; the model only really needs `issued_at` for most workflows.",
      "Detect each field's format and convert both to ISO 8601 UTC strings (numeric < 10^10 → epoch seconds; numeric < 10^13 → epoch ms; pre-existing strings pass through). The model now sees consistent ISO 8601 across all date fields.",
      "Leave the formats as-is; instruct the model in the system prompt to recognise epoch seconds vs milliseconds by magnitude.",
      "Convert both fields to Unix epoch milliseconds (a single canonical numeric format) and let the model do epoch-to-date conversion when needed."
    ],
    "explanations": [
      "Wrong — dropping data the user might need is over-correction. Normalize, don't discard.",
      "Correct — handle each field individually with format detection, normalize to a single human-readable canonical (ISO 8601). The model reasons over consistent shapes.",
      "Wrong — prompt-based magnitude detection is probabilistic and error-prone, especially at boundary timestamps. Normalize at the boundary.",
      "Wrong — numeric epochs are harder for models to reason about than ISO 8601 strings; the canonical should optimise for the consumer (the model)."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "An engineer says: 'Hooks are too complicated; we should just write tighter system prompts.' Which response is most aligned with Task 1.5's framing?",
    "options": [
      "Agree — prompts are easier to iterate on and modern models follow them well.",
      "Disagree — but only because hooks are faster at runtime.",
      "Disagree — and propose moving every behaviour (tone, citation style, response length) into hooks for consistency.",
      "Disagree — for compliance-grade requirements, prompts give probabilistic compliance with a non-zero failure floor; hooks are the right tool because they give deterministic guarantees. Hooks aren't 'more complicated' — they're a small focused unit per concern, often simpler than the gnarly prompt language people write to compensate for their absence. Use prompts for style; use hooks for guarantees."
    ],
    "explanations": [
      "Wrong — prompts have a non-zero failure floor regardless of model strength. For 'must not' rules, that floor matters.",
      "Wrong — runtime speed isn't the central trade. Determinism vs probabilism is.",
      "Wrong — over-correction. Hooks for stylistic concerns is over-engineering.",
      "Correct — the canonical 1.5 framing. Right tool for right job: prompts for style, hooks for guarantees."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "Three MCP tools return success/failure differently: `news_search` returns `status_code: 200/404/500`, `industry_db` returns `status: 'OK'/'NOT_FOUND'/'ERROR'`, `archive.fetch` returns `success: true/false`. Which `PostToolUse` design is best?",
    "options": [
      "A shared `PostToolUse` hook (branching by tool name) that maps all variants to a single canonical enum (e.g., `status: 'success' | 'not_found' | 'error' | 'permission_denied'`) plus an optional `error_message` field. The model now reasons over one vocabulary.",
      "Leave them as-is; the model can interpret each format from context.",
      "Three independent `PreToolUse` hooks that rewrite the call to request a uniform format.",
      "Convert all of them to numeric HTTP-style codes (200/404/500) since HTTP status is a recognized standard."
    ],
    "explanations": [
      "Correct — single canonical enum, single shared hook, branching by tool name. The model reasons over one status vocabulary across tools, which makes synthesis and gap-detection (Task 1.2) straightforward.",
      "Wrong — multi-format status is the source of subtle bugs (e.g., confusing 'no results' with 'access denied' — Task 5.3). Normalize at the boundary.",
      "Wrong — `PreToolUse` modifies the call, not the response. Most MCP tools don't accept response-format parameters anyway.",
      "Wrong — numeric codes are less readable for downstream reasoning than enums. Optimise the canonical for the consumer (the model's reasoning), not for legacy familiarity."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "Which behaviour is correctly described as a `PostToolUse` hook responsibility (NOT a `PreToolUse` responsibility)?",
    "options": [
      "Denying a `process_refund` call when the amount exceeds $500.",
      "Blocking a `lookup_order` call when the customer has not yet been verified.",
      "Trimming a 40-field search result down to 5 relevant fields before the response is appended to the conversation.",
      "Validating that an outgoing `escalate_to_human` payload contains all required structured fields."
    ],
    "explanations": [
      "Wrong — denying a call before it executes is `PreToolUse`. The whole point is to prevent the side effect.",
      "Wrong — blocking a call before execution is `PreToolUse` territory.",
      "Correct — trimming the response after the tool executed and before it enters context is exactly `PostToolUse`. It's a transformation on the response, not the call.",
      "Wrong — validating an outgoing call is `PreToolUse` (the call hasn't executed yet). `PostToolUse` runs on the response, not the call."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "An exam scenario: 'The agent occasionally tries to call a `read_pii` tool when servicing customers in the support tier role. Policy: support tier must not access raw PII. Which architectural fix is correct?",
    "options": [
      "Add a few-shot example showing the agent declining `read_pii` and offering the sanitised alternative.",
      "Add a `PreToolUse` hook on `read_pii` that denies the call when `session.role == 'support_tier'`, with a redirect reason: 'Support tier cannot access raw PII; use `read_anonymized_summary` instead.' The model redirects to the sanitised alternative on the next turn.",
      "Reword the system prompt to forbid `read_pii` more strongly.",
      "Add a `PostToolUse` hook on `read_pii` that strips PII fields from the response."
    ],
    "explanations": [
      "Wrong — few-shot reduces probability of misuse, doesn't eliminate it. Compliance-grade requirement needs a hook.",
      "Correct — `PreToolUse` blocks the call deterministically for the support tier; the redirect reason names the sanitised alternative tool. Policy guarantee + graceful redirect.",
      "Wrong — system-prompt prohibition is probabilistic. The role-based access policy demands a guarantee.",
      "Wrong — `PostToolUse` runs after the read has already happened; the PII has already been retrieved and may have been logged. `PreToolUse` is the correct safety boundary."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "In the context of agentic task decomposition, what is 'prompt chaining' (a fixed sequential pipeline)?",
    "options": [
      "A loop in which the agent revises subtasks each round based on intermediate findings.",
      "A sequence of prompts whose order and structure are fixed in advance, where each step's output flows into the next.",
      "Running multiple unrelated prompts in parallel with no coordination.",
      "A single mega-prompt that asks the model to perform every step at once."
    ],
    "explanations": [
      "Wrong — that describes dynamic adaptive decomposition, the opposite shape.",
      "Correct — prompt chaining means a known recipe executed identically each time; the steps are decided by the architect, not by intermediate findings.",
      "Wrong — that's parallel fan-out without sequencing, not chaining.",
      "Wrong — a mega-prompt is the anti-pattern that decomposition is designed to replace."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "pre"
  },
  {
    "question": "An agent must investigate the root cause of a production incident with no predefined runbook. Which decomposition shape fits best?",
    "options": [
      "A fixed sequential pipeline with all steps locked at design time.",
      "A single mega-prompt that contains every log, metric, and deploy.",
      "A dynamic adaptive plan that revises the open subtasks based on what each round of findings reveals.",
      "Parallel one-shot fan-out with no coordinator and no integration step."
    ],
    "explanations": [
      "Wrong — a fixed pipeline cannot follow leads it produces; it dutifully runs predetermined steps even when the data points elsewhere.",
      "Wrong — one giant prompt suffers attention dilution and still doesn't adapt as evidence comes in.",
      "Correct — incident root-cause is open-ended; the next probe depends on what the prior probe surfaced. The plan must update each round.",
      "Wrong — uncoordinated parallel probes can't revise the plan based on findings; you'll execute irrelevant probes and miss the lead."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "pre"
  },
  {
    "question": "A reviewer agent must inspect a 40-file PR. Why decompose into a per-file local pass plus a separate cross-file integration pass instead of using one giant prompt?",
    "options": [
      "A single mega-prompt suffers attention dilution: when 40 files compete for the model's attention, per-file detail attention degrades and subtle local issues (typos, off-by-ones, missing null checks) drop off.",
      "Single prompts cost more tokens than two passes; cost is the primary driver.",
      "The model has a hard 10-file limit per prompt regardless of context size.",
      "Decomposition reduces the model's reasoning to constant time complexity."
    ],
    "explanations": [
      "Correct — the bottleneck is attention, not capacity. Two passes give the local pass full per-file attention and the integration pass focused cross-file scope.",
      "Wrong — token cost is not the architectural reason; the issue is attention quality, and decomposition often costs more total tokens.",
      "Wrong — there is no fixed 10-file ceiling. Models can hold many files; they just can't attend to each uniformly.",
      "Wrong — decomposition doesn't change algorithmic complexity; it changes attention distribution."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "pre"
  },
  {
    "question": "After per-file local-review subagents complete, what should the cross-file integration pass receive as its input?",
    "options": [
      "The full raw contents of every file re-concatenated into one prompt.",
      "Only the diff hunks of each changed file.",
      "A randomized 20% sample of the raw file contents.",
      "Structured summaries from the local passes — file paths, exported symbols, local issues, API changes — without the raw file contents."
    ],
    "explanations": [
      "Wrong — re-feeding raw files re-creates the attention-dilution problem the decomposition was designed to avoid.",
      "Wrong — diff-only loses surrounding context (exports, public surfaces) needed to detect cross-file contract drift.",
      "Wrong — random sampling produces inconsistent integration coverage and may miss the very files where cross-file issues live.",
      "Correct — the integration pass should reason over structured summaries so its attention focuses on cross-file structure (contracts, naming, integration coverage), not on local detail."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "pre"
  },
  {
    "question": "A team's PR review agent uses one prompt containing every changed file. They report that on larger PRs, local issues like missing null checks and small typos are being missed. What is the most likely cause and the right fix?",
    "options": [
      "The model is selecting wrong tools — switch to a different model.",
      "Attention dilution from a single mega-prompt — decompose into per-file local-analysis passes plus a separate cross-file integration pass.",
      "The system prompt needs more polite phrasing — add reassuring language.",
      "Tool calls are missing — add more MCP tools to the agent."
    ],
    "explanations": [
      "Wrong — model choice doesn't fix attention dilution; the problem is how the work is structured, not which model runs it.",
      "Correct — packing many files into one prompt dilutes per-file attention. Per-file local pass + cross-file integration pass restores focused attention at each layer.",
      "Wrong — phrasing has no bearing on attention distribution across many files.",
      "Wrong — adding tools doesn't address the structural issue of asking one prompt to attend to many files at once."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "pre"
  },
  {
    "question": "An incident-response agent executes a fixed 5-step playbook (logs → metrics → deploys → DB → vendor) on every incident. The team complains it often misses obvious leads — e.g., logs reveal one failing service but the agent still runs the DB and vendor checks. What is the architectural fix?",
    "options": [
      "Lengthen the playbook to 10 steps so more is covered.",
      "Shorten the playbook to 3 steps and accept lower coverage.",
      "Replace the fixed playbook with a dynamic adaptive plan: after each step, revise the open subtasks based on what was just found, dropping irrelevant probes and adding lead-driven ones.",
      "Run all 5 steps in parallel instead of serially."
    ],
    "explanations": [
      "Wrong — making the script longer doesn't make it adaptive; it just executes more irrelevant steps.",
      "Wrong — fewer steps reduce coverage but still cannot follow leads.",
      "Correct — open-ended investigation requires the plan to revise based on findings. The fixed playbook cannot follow leads its own steps produce; an adaptive plan can.",
      "Wrong — parallelism without adaptation still runs irrelevant probes; concurrency doesn't make a fixed plan responsive to evidence."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "pre"
  },
  {
    "question": "A coordinator is asked to 'add comprehensive tests to a large legacy codebase with no test plan.' Which approach matches the canonical decomposition for open-ended tasks?",
    "options": [
      "Map the codebase structure and existing coverage; prioritize modules by impact (criticality × coverage gap × downstream blast radius); then write tests adaptively, revising the plan as dependencies surface during execution.",
      "Iterate alphabetically through every file and write whatever tests come to mind for each.",
      "Use one mega-prompt that contains the entire repository and ask 'write tests.'",
      "Use a fixed sequential pipeline with the file list and per-file procedure both known up front."
    ],
    "explanations": [
      "Correct — this is the 'map → prioritize → adapt' shape: structural mapping produces an artifact, prioritization grounds selection in impact, and adaptation lets the plan revise as real dependencies surface.",
      "Wrong — alphabetical order ignores impact and dependency, producing tests that don't track risk or readiness.",
      "Wrong — a giant prompt suffers attention dilution and produces shallow output for any large repo.",
      "Wrong — the file list isn't actually known up front in a meaningful sense (which to test, in what order); the work is inherently discovery-driven."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "pre"
  },
  {
    "question": "An architect proposes 'we'll just use a 200K-token context window so the agent sees all 40 PR files at once and won't need to decompose into local + integration passes.' What is the strongest critique?",
    "options": [
      "200K is overkill for 40 files; use a smaller window.",
      "The cost of large contexts is prohibitive.",
      "Latency at 200K is unacceptable for a code review.",
      "Capacity isn't the bottleneck — attention is. A 200K context can hold the files but does not make per-file detail attention uniform; the only way to restore focused attention is to decompose."
    ],
    "explanations": [
      "Wrong — capacity sizing is a side concern; the architectural critique is about attention.",
      "Wrong — cost may be a factor, but it's not the principled critique. Even free, the attention problem remains.",
      "Wrong — latency is a side effect; the architectural critique is that capacity does not equal attention.",
      "Correct — attention degrades when many files compete in one prompt; widening capacity does not fix that. Decomposition is the architectural fix."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "pre"
  },
  {
    "question": "A team decomposes a code review into 22 per-file local-review subagents, each producing local findings, and skips the cross-file integration pass to save time. What is the most likely consequence?",
    "options": [
      "Cross-file issues — API contract drift across modules, naming inconsistencies, missing integration coverage, duplicated logic — go undetected because no pass is looking at them.",
      "Local issues such as null-check omissions will be missed.",
      "Subagent context isolation will fail and contaminate findings.",
      "The coordinator's context will overflow during result collection."
    ],
    "explanations": [
      "Correct — the per-file passes catch local issues; cross-file issues live in the gaps between files, and only the integration pass is shaped to see them. Skipping it means those issues escape review.",
      "Wrong — local issues are exactly what per-file passes catch; that part of review is intact.",
      "Wrong — subagent isolation isn't broken by skipping integration; that's a Task 1.3 mechanism.",
      "Wrong — collecting 22 structured summaries doesn't overflow context; in fact it's much smaller than re-feeding 22 raw files."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "pre"
  },
  {
    "question": "The workflow is 'generate API reference documentation for every endpoint in /api; the procedure for each endpoint is identical.' An architect proposes building this as a dynamic adaptive plan that re-decides each step. What is wrong with that choice?",
    "options": [
      "Nothing is wrong — adaptive plans always outperform chains.",
      "Adaptive plans are too slow for any production workflow.",
      "The work is predictable and enumerable: every endpoint goes through the same procedure. Adaptation introduces nondeterminism with no information gain. A fixed prompt chain is the correct shape.",
      "Adaptive plans cannot produce JSON output."
    ],
    "explanations": [
      "Wrong — adaptation is only a fit when intermediate findings shape the next subtask. For predictable enumerable work, it is the wrong tool.",
      "Wrong — speed isn't the principled critique; the principled critique is shape mismatch.",
      "Correct — a fixed chain is right when you can pre-enumerate every subtask. Adaptive plans add cost and nondeterminism for no benefit when the procedure is uniform.",
      "Wrong — both shapes can produce JSON; output format is not what distinguishes them."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "pre"
  },
  {
    "question": "Which statement most precisely captures when a fixed sequential pipeline (prompt chaining) fits versus a dynamic adaptive decomposition?",
    "options": [
      "Fixed pipelines fit open-ended investigation; adaptive plans fit predictable enumerable workflows.",
      "Both shapes fit any workflow equally; the choice is essentially stylistic.",
      "Fixed pipelines fit predictable, multi-aspect workflows where every input goes through the same known steps; adaptive plans fit open-ended investigation where the next subtask depends on what prior findings revealed.",
      "Adaptive plans should always be preferred because they are more flexible and self-correct."
    ],
    "explanations": [
      "Wrong — this inverts the rule. Fixed pipelines belong on predictable workflows, adaptive plans belong on open-ended ones.",
      "Wrong — the choice is principled, not stylistic. Picking the wrong shape produces brittle agents (scripted playbook misses leads, adaptive plan adds noise to enumerable work).",
      "Correct — the matching rule. The cheap mental test is: can you pre-enumerate every subtask before running the agent? Yes → chain. No → adapt.",
      "Wrong — adaptive plans burn LLM turns on plan-regeneration for no information gain on uniform workflows, and introduce nondeterminism into processes that should be deterministic."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "post"
  },
  {
    "question": "A coordinator runs 18 per-file local-review subagents in parallel. To best preserve the decomposition's attention benefit, what should the cross-file integration pass receive as input?",
    "options": [
      "All 18 raw files plus a summary of each file's findings, so the integration agent has full context.",
      "All 18 raw files only, with no summaries, to avoid biasing the integration agent.",
      "Only the file paths, and let the integration agent re-read each file as needed.",
      "Only the structured summaries (paths, exported symbols, issues, API changes) produced by the local passes — no raw file contents."
    ],
    "explanations": [
      "Wrong — re-attaching all 18 raw files re-creates the attention-dilution problem that decomposition exists to avoid; adding summaries on top makes the prompt even larger without restoring focus.",
      "Wrong — same problem as A without the summaries. The integration pass loses the local-analysis insights and inherits the dilution.",
      "Wrong — re-reading files defeats the parallel decomposition; the integration pass would re-do local analysis instead of focusing on cross-file structure.",
      "Correct — structured summaries let the integration pass attend exclusively to cross-file concerns (contract drift, naming, integration coverage) without the per-file noise."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "post"
  },
  {
    "question": "During an adaptive investigation into a deployment latency regression, the first probe finds latency spiked on service A specifically. What should the coordinator do next?",
    "options": [
      "Revise the plan: drop now-irrelevant broad probes (generic database health, vendor status), and add new subtasks targeted at service A — recent deploys, upstream caller patterns, dependency timeline.",
      "Continue executing the originally planned probes regardless of the finding, then synthesize at the end.",
      "Run all remaining initial probes in parallel without revision, to maximize coverage.",
      "Terminate the investigation and report 'latency spiked on service A' as the final answer."
    ],
    "explanations": [
      "Correct — that's the adaptive loop: ingest findings, revise the open-subtask list (drop dead-ends, add lead-driven probes, reprioritize), then dispatch the next round.",
      "Wrong — that turns the agent into a fixed chain again. The whole point of an adaptive plan is to follow leads as they appear.",
      "Wrong — running irrelevant probes in parallel wastes turns and doesn't move the investigation toward root cause.",
      "Wrong — 'service A spiked' is a symptom locator, not a root cause. The investigation should now drill into service A."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "post"
  },
  {
    "question": "A reviewer suggests 'for the integration pass, just re-feed all 40 files plus the local findings — that way the model has full context.' Why is this counter to the decomposition's design?",
    "options": [
      "The cost is too high relative to the value.",
      "Re-feeding raw files re-creates the attention-dilution problem the per-file decomposition was designed to avoid; the integration pass is meant to focus exclusively on cross-file structure by reasoning over structured summaries, not raw content.",
      "Models cannot accept more than 5 files at once regardless of context size.",
      "Local findings are redundant with raw file contents and only one is needed."
    ],
    "explanations": [
      "Wrong — cost is a side concern. The architectural problem is that re-feeding raw files defeats the very attention-restoration that decomposition exists to provide.",
      "Correct — the integration pass's value comes from a focused attention profile on cross-file concerns. Re-attaching all 40 files re-introduces the dilution that motivated the split in the first place.",
      "Wrong — there is no fixed 5-file ceiling; the issue is attention quality, not raw acceptance.",
      "Wrong — local findings and raw files carry different information; the findings are the structured artifact the integration pass is supposed to reason over."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "post"
  },
  {
    "question": "Why does parallel per-file local review pair naturally with coordinator-subagent orchestration (Task 1.3) rather than the coordinator handling each file in sequence within its own context?",
    "options": [
      "Subagents are intrinsically faster than the coordinator on any task.",
      "Subagents reduce token costs by a fixed 50% in this pattern.",
      "Each subagent runs in isolated context (Task 1.2), so per-file attention is uncontaminated by prior files; this amplifies the local-pass attention benefit. Parallel execution also reduces wall-clock time.",
      "Subagents share the coordinator's context, which improves cross-file reasoning during the local pass."
    ],
    "explanations": [
      "Wrong — there's no inherent speed advantage; the win is attention isolation and parallelism.",
      "Wrong — there is no fixed cost reduction. Subagents may even increase total tokens; the win is attention quality.",
      "Correct — context isolation per subagent means each file gets the model's full focused attention, with no leakage from other files. Combined with parallel dispatch, this is why the pattern dominates large-PR review.",
      "Wrong — subagents do *not* inherit the coordinator's context (Task 1.2 hub-and-spoke). That's the whole point — isolation is the feature."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "post"
  },
  {
    "question": "A security audit agent is deployed against a sprawling internal service with no pre-defined attack surface. The team is debating (a) a scripted 7-step playbook vs (b) an adaptive plan that revises subtasks each round. Which is correct, and why?",
    "options": [
      "The adaptive plan: the scope is open-ended, and findings (a discovered admin endpoint, an unexpected service dependency) generate new subtasks the playbook could not have pre-enumerated. The plan must revise as evidence accumulates.",
      "The scripted playbook: it is more controllable and produces predictable results.",
      "Neither — use a single mega-prompt with the entire codebase attached.",
      "The scripted playbook, but with the steps run in parallel for speed."
    ],
    "explanations": [
      "Correct — open-ended security audits are exactly the workflow shape that demands an adaptive plan: each finding may reveal a new surface to investigate, and a scripted playbook cannot follow those leads.",
      "Wrong — controllability comes at the cost of relevance. A scripted playbook will execute predetermined steps even when findings point elsewhere, and will miss surfaces that weren't pre-enumerated.",
      "Wrong — a mega-prompt suffers attention dilution and still cannot adapt to evidence as it accumulates.",
      "Wrong — parallelism doesn't make a fixed playbook adaptive; it just runs irrelevant steps faster."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "post"
  },
  {
    "question": "A coordinator is told 'add comprehensive tests to a 600-file legacy backend.' It immediately starts writing tests for the first file it encounters. What is the architectural error, and what is the correct fix?",
    "options": [
      "The model is too small for the task; switch to a larger model.",
      "The coordinator skipped the mapping pass. Without first producing a structural map (modules, exports, dependency graph, current coverage) and a prioritization grounded in impact (criticality × coverage gap × downstream blast radius), file selection degenerates to whatever it sees first. The correct shape is map → prioritize → adapt.",
      "Used too few subagents; spawn 600 parallel test-writing subagents at once instead.",
      "Should have used a single mega-prompt with all 600 files attached and asked 'write tests.'"
    ],
    "explanations": [
      "Wrong — model size doesn't fix the missing-decomposition error. A larger model will still pick a file at random if it isn't told to map and prioritize first.",
      "Correct — open-ended legacy-codebase work has three phases: map structure and coverage, prioritize by impact, then execute adaptively. Skipping mapping turns prioritization into guessing.",
      "Wrong — 600 parallel subagents without prioritization just amplifies the unguided selection problem.",
      "Wrong — 600 files in one prompt produce extreme attention dilution and shallow output; the issue is shape, not capacity."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "post"
  },
  {
    "question": "After a successful mapping pass on a legacy codebase, an architect proposes 'lock the prioritized list now and walk through it strictly in order — no revision during execution.' What goes wrong?",
    "options": [
      "Locking simplifies execution and is the correct call for any prioritized plan.",
      "Execution will be measurably slower without parallelism.",
      "Total token cost will exceed budget without explicit caps.",
      "Real codebases reveal dependencies only when modules are actually touched. A locked plan can't insert prerequisite subtasks (e.g., 'clarify module B's contract before testing module A') or deprioritize subtrees whose dependencies turn out to be already-tested. The plan must remain editable through execution."
    ],
    "explanations": [
      "Wrong — locking removes the adaptation that makes the shape work for open-ended tasks. The plan must respond to discoveries during execution.",
      "Wrong — speed isn't the architectural critique. The critique is that a locked plan cannot reflect what execution reveals.",
      "Wrong — token cost is a side concern; the principled issue is plan staleness.",
      "Correct — the third phase is 'dependency-aware adaptation' precisely because real dependencies surface during execution. The plan must absorb those findings (insert prerequisites, deprioritize, add new high-impact items)."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "post"
  },
  {
    "question": "Task 1.6 (decomposition) and Task 4.6 (multi-instance / multi-pass review architectures) describe overlapping ideas — per-file local + cross-file integration. How are they best understood as different lenses on the same principle?",
    "options": [
      "1.6 is the agent-workflow shape (chain vs adapt; per-file + integration as a coordination pattern across subagents). 4.6 is the prompt-engineering view of multi-pass review (independent reviewer instances catching what self-review misses, calibrated confidence per finding). Same underlying principle, different layer.",
      "They contradict each other; only one applies in any given exam question.",
      "1.6 applies to Claude Code only; 4.6 applies to the API only.",
      "4.6 deprecates 1.6 in newer SDK versions."
    ],
    "explanations": [
      "Correct — the principle (decompose into focused passes; pair local detail with integration breadth; use independent reviewer context to catch what self-review misses) shows up at both layers. 1.6 names the orchestration; 4.6 names the prompt-level review architecture.",
      "Wrong — they reinforce each other; the exam may probe either lens.",
      "Wrong — both apply across SDK and Claude Code surfaces; the distinction is the layer of abstraction.",
      "Wrong — neither task deprecates the other; they are complementary."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "post"
  },
  {
    "question": "A team is choosing decomposition shapes for two workflows: (W1) 'generate API reference docs for every endpoint in openapi.yaml' and (W2) 'investigate why our weekend batch job is producing duplicate rows.' Which assignment is correct?",
    "options": [
      "W1 → adaptive plan; W2 → fixed chain.",
      "W1 → fixed sequential pipeline (work is enumerable, the per-endpoint procedure is uniform, no findings change the plan). W2 → dynamic adaptive plan (open-ended; the next probe depends on what the prior probe revealed about the duplicate-row source).",
      "Both → fixed chain (predictability is always preferable).",
      "Both → adaptive plan (flexibility is always preferable)."
    ],
    "explanations": [
      "Wrong — this inverts the rule. W1 is enumerable and uniform (chain); W2 is discovery-driven (adaptive).",
      "Correct — applying the cheap mental test: W1's subtasks are pre-enumerable (the endpoint list); W2's next subtask depends on intermediate findings (which job step inserts duplicates? which input causes them?). Different shapes for different workflow types.",
      "Wrong — fixed chains on open-ended investigation produce scripted playbooks that can't follow leads, the canonical anti-pattern.",
      "Wrong — adaptive plans on enumerable uniform workflows burn turns on plan-regeneration for no information gain and introduce nondeterminism into a process that should be deterministic."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.6"
    ],
    "phase": "post"
  },
  {
    "question": "What does the `--resume <session-name>` flag do in Claude Code / Agent SDK?",
    "options": [
      "Re-attaches to a named prior session, restoring its conversation history and prior tool-result context so the agent continues where it left off.",
      "Spawns a parallel subagent that inherits the coordinator's tools.",
      "Creates an independent branch from a shared baseline for divergent exploration.",
      "Discards prior context and starts a brand-new session under the given name."
    ],
    "explanations": [
      "Correct — resumption restores the named session's accumulated state (conversation, tool results, decisions) so follow-up queries reason over the prior context.",
      "Wrong — that describes subagent spawning (Task 1.3), not session resumption.",
      "Wrong — that describes `fork_session`, the divergent-branch primitive.",
      "Wrong — discarding context and starting fresh is the *opposite* of resumption; that's `start fresh + summary`."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "pre"
  },
  {
    "question": "What is the canonical use case for `fork_session`?",
    "options": [
      "Continuing a paused investigation hours later from the same point.",
      "Persisting state across a Claude Code crash so it can recover.",
      "Creating independent branches from a shared analysis baseline to explore divergent approaches (e.g., comparing two refactoring strategies) without redoing the baseline per branch.",
      "Compressing the conversation history to free context tokens."
    ],
    "explanations": [
      "Wrong — that's `--resume`. Resumption continues a single session line; it does not create branches.",
      "Wrong — crash recovery is closer to structured manifests (Task 5.4); fork is about deliberate parallel exploration.",
      "Correct — fork copies the baseline up to the fork point and then runs each branch independently, so divergent approaches can be compared without contamination and without redoing the expensive baseline.",
      "Wrong — that describes context compaction, not forking."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "pre"
  },
  {
    "question": "An investigation session paused yesterday. Overnight, a teammate merged a PR that edited several files the prior session analyzed. What is the most reliable next move?",
    "options": [
      "Resume with `--resume <session-name>` exactly as before — saved tokens are worth the small risk.",
      "Resume but add a vague 'things may have changed, please re-check' to the next prompt.",
      "Wait until the prior session's cached tool results expire and resume then.",
      "Start fresh and inject a structured summary of the prior findings; re-run the relevant tools to obtain current data."
    ],
    "explanations": [
      "Wrong — resumption inherits cached tool results as ground truth. The model has no signal the data is stale and will confidently reason over outdated content.",
      "Wrong — vague re-check prompts force the model to either re-do too much (defeats resuming) or trust too much (still uses stale cache).",
      "Wrong — cached tool results don't auto-expire in a way that protects you; the staleness has already taken effect once external changes occurred.",
      "Correct — fresh + structured summary preserves the prior facts/decisions cheaply while ensuring tool re-queries return current data, which is the right answer when prior tool results are stale."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "pre"
  },
  {
    "question": "Why are named sessions (`--session-name auth-refactor-2026-04`) preferred over auto-generated session IDs for long-running investigations?",
    "options": [
      "Auto-generated IDs cannot be resumed at all.",
      "Named sessions are stable, meaningful handles that humans can reliably find days later; auto-generated IDs are forgettable for multi-day work.",
      "Named sessions consume fewer tokens.",
      "Auto-generated IDs are tied to a single machine and cannot be shared."
    ],
    "explanations": [
      "Wrong — auto-generated IDs can be resumed; the issue is human findability, not technical capability.",
      "Correct — names are durable handles. For investigations spanning multiple work blocks, future-you (or a teammate) needs to find the right session reliably.",
      "Wrong — naming has no impact on token consumption.",
      "Wrong — that's not a property of the IDs; the practical issue is human memory."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "pre"
  },
  {
    "question": "A team has spent significant tokens building a shared baseline analysis of a legacy module. They now want to evaluate two refactoring approaches (strangler-fig vs clean-room rewrite) without contaminating one analysis with the other. Which primitive fits best?",
    "options": [
      "Continue both analyses sequentially in the same session.",
      "Start two fresh sessions and redo the baseline analysis in each.",
      "Use `fork_session` twice to create independent branches from the shared baseline; each branch explores one approach.",
      "Use `--resume` twice on the same baseline session, alternating between the two approaches."
    ],
    "explanations": [
      "Wrong — sequential exploration in one session lets the first approach bias the analysis of the second; comparison is contaminated.",
      "Wrong — wastes the expensive baseline work and risks the two baselines drifting subtly across re-analyses.",
      "Correct — `fork_session` creates independent branches that share the baseline up to the fork point but evolve independently after. Each conclusion is uncontaminated by the other.",
      "Wrong — `--resume` continues a single session line; alternating prompts on the same line doesn't isolate the explorations from each other."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "pre"
  },
  {
    "question": "An architect resumes an investigation session. Since the pause, two specific files have been edited; everything else is unchanged. What's the right way to handle the resume?",
    "options": [
      "`--resume` and immediately ask the substantive follow-up question; trust the cached context.",
      "`--resume` and explicitly tell the session which two files changed and the nature of each change, instructing it to re-read those files; everything else stays cached.",
      "Discard the session entirely and redo the whole investigation from scratch.",
      "`--resume` with a generic 'something might have changed, please verify the codebase' prompt."
    ],
    "explanations": [
      "Wrong — silent resumption with stale cache for two files means the model reasons over outdated content for those files; conclusions can be confidently wrong.",
      "Correct — targeted re-analysis: explicit named files + nature of change lets the session re-read exactly what's stale and integrate updates without redoing valid work.",
      "Wrong — discards a lot of valid analysis. Targeted re-analysis is the precise and cheaper fix.",
      "Wrong — vague prompts produce either over- or under-correction; the model has no scope to act on."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "pre"
  },
  {
    "question": "What is the primary failure mode of resuming a session whose underlying systems have changed since it paused?",
    "options": [
      "The session prompt grows too long and exceeds the context window.",
      "The session crashes immediately with a stale-cache error.",
      "The model treats cached tool results (`Read`, `Grep`, `Bash` outputs) as ground truth and reasons confidently over outdated data, with no signal anything is wrong.",
      "Tool calls fail because the SDK detects the staleness and refuses to execute."
    ],
    "explanations": [
      "Wrong — context size isn't what the staleness pattern is about; the issue is data accuracy, not size.",
      "Wrong — there is no automatic staleness detection. Resumption silently inherits the cached results.",
      "Correct — this is the canonical failure mode. Stale tool results in resumed context are silent poison; the model has no signal to know they're outdated.",
      "Wrong — the SDK does not detect staleness. That's why the human architect has to make the resume-vs-fresh call deliberately."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "pre"
  },
  {
    "question": "Starting a fresh session instead of resuming, what should you do to avoid throwing away the prior investigation's value?",
    "options": [
      "Inject a structured summary (scope, key findings, decisions, open questions, provenance flags) so the new session inherits the *facts* without inheriting *stale tool results*.",
      "Paste the entire prior conversation history into the new session as raw text.",
      "Skip any handoff — the new session should rediscover everything from scratch.",
      "Re-run every tool call from the prior session before starting any new work."
    ],
    "explanations": [
      "Correct — the structured summary is the cheap way to transfer accumulated knowledge while leaving stale tool outputs behind. The new session re-queries tools as needed and gets current data.",
      "Wrong — pasting raw history reintroduces the stale tool results the fresh start was meant to escape.",
      "Wrong — without a summary the new session redoes discovery, wasting tokens and risking divergent conclusions.",
      "Wrong — re-running every tool call defeats the cost benefit of fresh-with-summary; only re-query tools when the new work needs them."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "pre"
  },
  {
    "question": "Which of the following best distinguishes `--resume` from `fork_session`?",
    "options": [
      "They are interchangeable; the choice is stylistic.",
      "`--resume` continues a single session line from where it paused; `fork_session` splits the session into independent branches that share history up to the fork point but then evolve separately.",
      "`--resume` is for CLI use; `fork_session` is API-only.",
      "`fork_session` is just a shortcut that calls `--resume` twice."
    ],
    "explanations": [
      "Wrong — they solve different problems: continuation vs branching. Picking the wrong one produces contamination or duplicated baseline work.",
      "Correct — that's the precise distinction. Resume = continue one line; fork = split into two independent lines from a shared point.",
      "Wrong — both primitives are exposed in both surfaces; the distinction is semantic, not interface.",
      "Wrong — fork creates independent branches; calling resume twice on the same name doesn't produce isolated branches."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "pre"
  },
  {
    "question": "An exam scenario: 'A weeks-long investigation session has been resumed many times. The underlying repository is stable; nothing material has changed since yesterday's pause. The team has a follow-up question to ask.' Which is the correct action?",
    "options": [
      "Resume with `--resume <session-name>` and ask the follow-up — prior context is mostly valid and resumption is the right call here.",
      "Start fresh with a structured summary; resumption is never reliable for multi-resume sessions.",
      "Use `fork_session` so the follow-up exploration is isolated from the rest of the investigation.",
      "Discard the session and redo the investigation to be safe."
    ],
    "explanations": [
      "Correct — when prior context is mostly valid (no material external changes), resumption is exactly the intended use. The risk pattern only fires when something has changed since the pause.",
      "Wrong — fresh-with-summary is for the staleness case. Applying it when nothing has changed wastes the prior context's accumulated state.",
      "Wrong — fork is for divergent exploration from a baseline, not for follow-up questions on a single line of investigation.",
      "Wrong — discarding a valid session and redoing weeks of work is unjustified when nothing has changed."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "pre"
  },
  {
    "question": "An investigation session named `q2-billing-audit` paused 18 hours ago. Since then, two PRs were merged that touched 5 of the 22 files the session analyzed. What is the architecturally correct next move, and why?",
    "options": [
      "Resume with `--resume q2-billing-audit` and ask the next substantive question; the session's cached context will reason over the new state automatically.",
      "Start a fresh session, inject a structured summary of the prior session's findings (scope, key conclusions, decisions, open questions), and let the fresh session re-query tools for the changed files. The summary preserves prior facts cheaply; tool re-queries guarantee current data.",
      "Use `fork_session` to create one branch per merged PR.",
      "Resume the session with a generic 'something may have changed since yesterday, please re-check the relevant files' prompt."
    ],
    "explanations": [
      "Wrong — resumption silently inherits cached `Read`/`Grep` results from before the PRs landed. The model treats them as ground truth and reasons over outdated content, producing confidently-wrong answers.",
      "Correct — the staleness blast radius (5 of 22 files plus possible downstream effects) is wide enough that fresh-with-summary is the right call. The summary keeps prior decisions/findings without dragging the stale tool results into the new session.",
      "Wrong — `fork_session` is for divergent exploration from a shared baseline, not for handling external file changes. Wrong primitive.",
      "Wrong — vague re-check prompts produce either over- or under-correction. The model has no precise scope to act on, and the cache problem isn't fixed."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "post"
  },
  {
    "question": "A team has captured an expensive shared baseline analysis of a payments module (dependency graph, test coverage, business rule extraction). They want to compare three migration strategies — strangler-fig, clean-room rewrite, and incremental in-place refactor — without one analysis biasing the others. Which approach is correct?",
    "options": [
      "From the baseline session, call `fork_session` three times to create three independent branches; each branch explores one strategy. All three inherit the baseline; conclusions stay independent because forks don't share post-fork state.",
      "Run all three analyses sequentially in the baseline session, separated by clear delimiters in the prompts.",
      "Start three fresh sessions and redo the baseline in each.",
      "Use `--resume` three times on the baseline session, asking one strategy per resume."
    ],
    "explanations": [
      "Correct — `fork_session` is the precise primitive for divergent exploration from a shared baseline. Each fork inherits the baseline; their post-fork explorations are independent; the comparison is contamination-free.",
      "Wrong — sequential analyses in one session let earlier analyses bias later ones (the model 'remembers' what it just argued for). Comparison is contaminated.",
      "Wrong — wastes the expensive baseline three times and risks subtle drift in baseline understanding across the redoes.",
      "Wrong — `--resume` continues one line of conversation; alternating strategy questions on that line doesn't produce independent branches."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "post"
  },
  {
    "question": "Which statement most accurately describes the cost-benefit tradeoff between `--resume` (with stale cache) and 'start fresh + structured summary'?",
    "options": [
      "Resume is always cheaper because it reuses prior tokens, so it should be the default.",
      "Fresh is always cheaper because tools re-execute quickly, so it should be the default.",
      "They have identical cost profiles; pick by personal preference.",
      "Resume is cheaper at startup but has a high recovery cost if cached results are stale (silent bad answers). Fresh + summary has higher startup cost but bounded recovery cost (re-query the missing piece). The expected-cost winner depends on whether anything has changed since the pause."
    ],
    "explanations": [
      "Wrong — resume's startup token saving is small; its risk-adjusted cost spikes when stale data drives wrong conclusions, and that risk is invisible to the model.",
      "Wrong — fresh starts pay re-discovery costs that are real; they aren't always cheaper.",
      "Wrong — the cost profiles differ materially. The whole point of the trade-off is choosing based on the staleness condition.",
      "Correct — that's the precise framing. Resume is fine when nothing material has changed; fresh + summary wins when staleness risk is non-trivial because the silent-bad-answer cost dwarfs the token savings."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "post"
  },
  {
    "question": "An architect proposes always using `--resume` 'because it saves tokens.' What's the strongest critique of using resumption as the default?",
    "options": [
      "Resumption inherits cached tool results as ground truth. When the underlying systems have changed, the model reasons confidently over stale data with no signal — silently wrong answers cost far more than the small token savings.",
      "Resumption is slower than starting fresh because re-attaching to a session takes time.",
      "Resumption requires manual session naming, which is cumbersome.",
      "Resumption cannot be used in CI environments."
    ],
    "explanations": [
      "Correct — that's the architectural critique. The token savings are real but small; the silent-failure cost when prior tool results are stale is potentially very large and undetectable.",
      "Wrong — re-attach time is not the architectural issue; correctness under change is.",
      "Wrong — naming is a small UX concern, not the principled critique against default-resume.",
      "Wrong — resumption is not blocked from CI; that's not the issue."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "post"
  },
  {
    "question": "What distinguishes `fork_session` from spawning parallel subagents (Task 1.3) when both can run analyses in parallel?",
    "options": [
      "They are functionally identical; either works.",
      "Subagents start with no inherited context — each is isolated from the start (Task 1.2 hub-and-spoke). `fork_session` branches start with the *full session baseline* up to the fork point, then evolve independently. Forks are the right primitive when divergent explorations need to share an expensive baseline.",
      "Subagents can use tools; forks cannot.",
      "Forks share state with each other after the fork; subagents do not."
    ],
    "explanations": [
      "Wrong — they solve different problems. Subagents = clean-context delegation; forks = shared-baseline branching.",
      "Correct — the difference is what each branch *inherits at start*. When the baseline is expensive and the explorations need to start from the same ground truth, forks are right; when each parallel investigation needs clean context, subagents are right.",
      "Wrong — both can use tools; the distinction is initial context.",
      "Wrong — forks are *independent* after the fork point; that's the entire point of the primitive."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "post"
  },
  {
    "question": "When starting a fresh session after a long-running investigation, what makes a structured summary effective vs a poor handoff?",
    "options": [
      "A good summary is short and only mentions the final conclusion.",
      "A good summary pastes the entire conversation transcript so nothing is lost.",
      "A good summary lists scope (files/systems under investigation), key findings with summary evidence, decisions already made, open questions for the resumed work, and provenance flags on findings whose supporting tool results may now be stale. A poor summary omits the open questions or the provenance flags.",
      "Any summary is equally effective as long as it mentions the project name."
    ],
    "explanations": [
      "Wrong — final conclusion alone strips away the supporting facts/decisions the new session needs to continue from.",
      "Wrong — pasting the full transcript reintroduces the stale tool results the fresh start was meant to escape.",
      "Correct — those five elements are exactly the structured-summary contract. Open questions tell the new session where to pick up; provenance flags tell it which findings to re-verify before relying on them.",
      "Wrong — summary quality varies dramatically by what it preserves vs strips. Project name alone is not actionable handoff."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "post"
  },
  {
    "question": "An investigation session is paused. Two named files have changed since the pause; everything else is unchanged. The team needs to ask a follow-up question. Which technique uses resources most efficiently while staying correct?",
    "options": [
      "Discard the session and start fresh from scratch.",
      "`--resume` and immediately ask the follow-up; trust the cached context for everything.",
      "`--resume` plus a precise re-analysis instruction: name the two changed files, describe the nature of each change, instruct the session to re-read those files only and update conclusions accordingly. Other files keep their valid cached state.",
      "`fork_session` so the follow-up runs in an independent branch."
    ],
    "explanations": [
      "Wrong — discards much valid prior analysis. Targeted re-analysis is cheaper and equally correct.",
      "Wrong — silent stale cache for the two changed files leads to confidently-wrong conclusions on those files.",
      "Correct — targeted re-analysis: precise scoping (named files + nature of change) lets the session re-read exactly what's stale and integrate updates without redoing valid work. Most efficient correct option.",
      "Wrong — fork is for divergent exploration from a shared baseline, not for handling targeted file changes on a continuation."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "post"
  },
  {
    "question": "How does Task 1.7 (resume / fork) relate to Task 5.4 (scratchpads, structured manifests, /compact)?",
    "options": [
      "5.4 covers durability *within* a long-running session (scratchpad files, structured manifests for crash recovery, /compact to free context). 1.7 covers durability *across* sessions and parallel branches (--resume, fork_session, structured-summary handoffs). The manifests from 5.4 are exactly the artifacts that feed 1.7's structured-summary pattern; together they are the durability story for long agentic work.",
      "They contradict each other; pick one or the other.",
      "1.7 deprecates 5.4 in newer SDK versions.",
      "5.4 is for Claude Code only; 1.7 is for the Agent SDK only."
    ],
    "explanations": [
      "Correct — they are complementary durability layers. 5.4 keeps long sessions surviving context turnover; 1.7 keeps work surviving session boundaries and enables divergent exploration. Manifests written by 5.4 become summaries injected via 1.7.",
      "Wrong — they are complementary, not in tension. The exam may probe either lens.",
      "Wrong — neither task deprecates the other; they cover different layers.",
      "Wrong — both apply across Claude Code and SDK surfaces."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "post"
  },
  {
    "question": "Which scenario most clearly demands `fork_session` (rather than `--resume` or fresh+summary)?",
    "options": [
      "A weeks-long audit pauses overnight and resumes the next morning with no external changes.",
      "A teammate merged a PR last night that touched the modules under investigation.",
      "After expensive shared baseline analysis, the team needs to compare three migration strategies side-by-side without one analysis biasing the next.",
      "The session is approaching its context-window limit and needs compaction."
    ],
    "explanations": [
      "Wrong — that's a textbook `--resume` case. Nothing has changed; resumption is the right primitive.",
      "Wrong — that's a textbook fresh-with-summary case. Stale tool results are the trigger; fork doesn't help here.",
      "Correct — divergent exploration from a shared baseline is exactly what `fork_session` was designed for. Independent branches share the baseline up to the fork point and then evolve separately, so each strategy's analysis is uncontaminated.",
      "Wrong — context-window pressure is a Task 5.4 problem (/compact, manifests); fork doesn't address it."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "post"
  },
  {
    "question": "Why is 'just paste the entire prior conversation transcript into a new session' an inadequate substitute for a structured summary when starting fresh?",
    "options": [
      "Pasted transcripts exceed token limits in all cases.",
      "Pasting the raw transcript reintroduces the stale tool results (cached `Read`/`Grep`/`Bash` outputs) that the fresh start was meant to escape; the model treats them as ground truth and reasons confidently over outdated data — same failure mode as a naive resume. A structured summary preserves the prior *facts and decisions* without the stale tool outputs.",
      "Transcripts cannot be parsed by the model.",
      "Transcripts are private and cannot be shared with the model."
    ],
    "explanations": [
      "Wrong — large transcripts may pressure context, but that's not the architectural critique. The principled issue is correctness, not size.",
      "Correct — the whole point of fresh-with-summary is to *leave behind* the stale tool results. Pasting the raw transcript drags them right back in. The summary's value is its curated structure: facts and decisions only, no cached tool outputs presented as ground truth.",
      "Wrong — models can read transcripts; that's not the issue.",
      "Wrong — there's no privacy block; the issue is what the transcript contains, not whether it can be shared."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.7"
    ],
    "phase": "post"
  },
  {
    "question": "A team's coordinator runs web_search in one turn, gets results, then in the next turn emits Task({subagent_type: 'synthesis_agent', prompt: 'Produce a 2-page report on AI adoption in music in 2025.'}). The synthesis output is generic and cites no sources. What's the most direct fix?",
    "options": [
      "Add more search subagents to gather more evidence before synthesising.",
      "Add web_search to the synthesis subagent's allowedTools so it can search if it needs evidence.",
      "Include the search subagent's complete findings — with attribution metadata — verbatim in the synthesis Task prompt, since subagent context is isolated.",
      "Increase the synthesis subagent's max_tokens budget so it has more room to elaborate."
    ],
    "explanations": [
      "Wrong — more search subagents produce more findings, but if those findings are still not passed into the synthesis Task prompt, synthesis sees nothing. The bug is at the coordinator's invocation, not the search step.",
      "Wrong — adding web_search to synthesis is cross-specialisation misuse (Task 2.3). Synthesis should reason over the prompt; tool sprawl degrades selection and dilutes the role.",
      "Correct — subagent context is isolated. The Task prompt is the only input synthesis sees, so the coordinator must copy the prior findings (preserving attribution) into that prompt. Without it, synthesis improvises.",
      "Wrong — there's nothing for the model to elaborate on. Bigger budgets don't manufacture evidence; missing inputs require the coordinator to supply them."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "A coordinator wants to spawn three web_search subagents in parallel, each scoped to a different subdomain (music, film, visual arts). Which assistant-turn shape achieves true parallelism in the Agent SDK?",
    "options": [
      "Three sequential turns, each containing one Task tool_use block, waiting for the prior result before emitting the next.",
      "A single tool_use block whose input includes a list of three subdomains; the runtime fans them out internally.",
      "A single assistant turn containing three Task tool_use blocks, each with subagent_type='web_search_agent' and a different subdomain-scoped prompt.",
      "A single Task tool_use block invoking a 'parallel_search_agent' that internally loops over the subdomains."
    ],
    "explanations": [
      "Wrong — that's serial, not parallel. Each turn waits for the prior tool_result, so wall-clock cost is the sum (3×) rather than the max (1×).",
      "Wrong — there's no list-based fan-out in the Task input schema. The coordinator must emit one Task tool_use block per subagent invocation.",
      "Correct — true parallelism in the SDK is defined by multiple Task tool_use blocks in the same coordinator turn. The runtime executes them concurrently and returns all tool_results together.",
      "Wrong — a single 'parallel_search_agent' that loops internally is sequential inside one subagent. The coordinator-level parallelism comes from emitting multiple Task calls per turn, not from a wrapper subagent."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "A coordinator needs to invoke a doc_analysis subagent on a freshly fetched PDF. The subagent doesn't need any prior session context — just the PDF and a goal. Which primitive is appropriate, and why?",
    "options": [
      "Task — spawn a fresh isolated subagent and pass the PDF reference plus the goal in the prompt. Routine delegation should not inherit session history.",
      "fork_session — clone the coordinator's session so the subagent inherits any context that might turn out to be relevant.",
      "A direct read_document call from the coordinator itself — spawning a subagent for one document is overhead.",
      "Both Task and fork_session in parallel, then merge the outputs to be safe."
    ],
    "explanations": [
      "Correct — Task is the right primitive for routine isolated delegation. The subagent gets only what's in the prompt, which keeps its context focused.",
      "Wrong — fork_session is for divergent exploration from a shared baseline. Using it for routine delegation contaminates the subagent with irrelevant history and inflates tokens.",
      "Wrong — pulling the document analysis into the coordinator's own context bloats it (lost-in-the-middle risk per Domain 5) and forfeits the per-role specialisation a doc_analysis subagent provides.",
      "Wrong — running the same delegation through two primitives doesn't add safety; it doubles cost and forces a merge step."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "A coordinator uses fork_session for every subagent invocation because the team thought 'it's faster than re-pasting context.' After deployment, subagent prompts overshoot token budgets and outputs include irrelevant tangents from prior turns. What's the architectural diagnosis?",
    "options": [
      "fork_session inherits the full current session; using it for routine delegation forces every subagent to carry baseline context it doesn't need, contaminating focus and inflating tokens. Use Task for routine delegation; reserve fork_session for divergent exploration from a shared baseline.",
      "fork_session has a memory leak the team should report to Anthropic.",
      "The forks are running on stale context — the coordinator should checkpoint the session before each fork.",
      "fork_session is fine here; the team should reduce its system prompt size to compensate for the inherited tokens."
    ],
    "explanations": [
      "Correct — this is the canonical fork_session misuse. fork_session is the right primitive only when each branch genuinely needs the shared baseline (e.g., comparing alternative report styles against the same accumulated analysis). For everything else, Task keeps contexts isolated and tight.",
      "Wrong — there is no memory leak. The behaviour is by design: forks inherit the session. The misuse is the team applying it where Task is correct.",
      "Wrong — staleness isn't the symptom. The forks are seeing exactly what the session contained; the issue is that they shouldn't have inherited it at all.",
      "Wrong — shrinking the system prompt papers over the symptom. The architectural fix is to switch to Task for non-divergent invocations."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "You are defining the AgentDefinition for a synthesis_agent that combines structured findings into a cited report by reasoning over its prompt. Which allowedTools value is most appropriate?",
    "options": [
      "['web_search', 'read_document'] — so it can verify any claim that looks suspicious.",
      "['Task'] — so it can spawn sub-subagents if it needs to.",
      "[] — synthesis reasons over the prompt; giving it unrelated tools invites cross-specialisation misuse and degrades selection reliability when too many tools are exposed.",
      "['web_search', 'read_document', 'Task'] — maximum flexibility lets it adapt to any input."
    ],
    "explanations": [
      "Wrong — letting synthesis re-search introduces cross-specialisation: it produces inconsistent evidence relative to what the search subagent already returned, and breaks the coordinator's centralised observability.",
      "Wrong — giving synthesis the Task tool effectively turns it into a second coordinator, breaking the hub-and-spoke topology and creating two competing decomposition authorities.",
      "Correct — synthesis is a reasoning role over a prompt. An empty allowedTools is the principled choice; it prevents misuse, keeps the per-agent tool count low (Task 2.3 — 18 tools degrades selection vs 4–5), and matches the role's specialisation.",
      "Wrong — 'maximum flexibility' is the anti-pattern. It compounds cross-specialisation misuse with tool overload; both degrade subagent reliability."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "A coordinator routes 'analyse this 2024 SEC filing' to web_search_agent instead of doc_analysis_agent. The two AgentDefinition descriptions are 'web_search_agent: helps the team research things on the internet' and 'doc_analysis_agent: helps the team look at documents and find stuff.' What's the cleanest architectural fix?",
    "options": [
      "Add a routing layer that pre-classifies queries before the coordinator sees them.",
      "Use forced tool_choice to always select doc_analysis_agent for filing-related queries.",
      "Train a custom classifier on historical routing decisions and replace the coordinator's selection logic.",
      "Rewrite the descriptions to be specific and disjoint — e.g., 'web_search_agent: searches public web sources for current news, blog posts, and recent reports' vs 'doc_analysis_agent: extracts findings from PDF/HTML documents already retrieved into the session.' The model uses descriptions as the primary routing signal."
    ],
    "explanations": [
      "Wrong — bolting on an external classifier hides the underlying defect (overlapping descriptions) and adds a separate failure mode. Fix the routing signal at its source.",
      "Wrong — forced tool_choice for one phrase doesn't generalise; the next ambiguous filing-adjacent query routes wrong again. The structural fix is in the descriptions.",
      "Wrong — same as above, but with more engineering. Custom classifiers are warranted only when the description-based routing has been proven insufficient even after disjoint descriptions are written.",
      "Correct — AgentDefinition descriptions are the model's primary routing signal in a coordinator-subagent system. The original descriptions overlap (both 'help with research'). Specific, disjoint descriptions fix the routing at its source."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "For a search subagent invoked to find authoritative recent evidence on a topic, which prompt design best preserves the subagent's adaptability while still holding it to a measurable standard?",
    "options": [
      "A goal-oriented prompt that names the goal, scope, constraints (date range, source types), output shape, and quality criteria — letting the subagent pick the search strategy that fits the tool surface.",
      "A fully procedural prompt: 'Search exactly term X, take the first three results, summarise each in 50 words, return as a list.' Procedure prevents drift.",
      "A vague single-line prompt: 'do a thorough search of this topic.' Maximum freedom maximises creative recall.",
      "A goal-oriented prompt without quality criteria, on the theory that criteria limit the subagent's creativity."
    ],
    "explanations": [
      "Correct — goals + scope + constraints + criteria is the prescribed shape. The subagent adapts its execution to whatever the tool surface returns while still being held to the criteria the coordinator cares about.",
      "Wrong — procedural prompts lock the subagent into one execution path that may not match the tool surface (e.g., the first three results may be ads). They also remove recovery from edge cases.",
      "Wrong — a vague prompt produces vague output. Without measurable criteria the subagent has no definition of done; the result is 'whatever happened.'",
      "Wrong — quality criteria are what make goals measurable. Dropping them returns the prompt to ambiguity; the subagent can no longer self-evaluate."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "You want every synthesis output to flag thin evidence (claims supported by only one source) and every report to be Markdown with citations by source ID. Where should each constraint live?",
    "options": [
      "Both in the user's original message at runtime.",
      "Both in the coordinator's user-facing system prompt.",
      "Persistent role and format constraints belong in each subagent's AgentDefinition system_prompt; per-invocation goal, scope, and quality criteria belong in the Task prompt the coordinator emits.",
      "Both should be hard-coded into the runtime via PostToolUse hooks; prompts are unreliable."
    ],
    "explanations": [
      "Wrong — pushing constraints to the user message means every user request must repeat them, and the runtime can't enforce them across users. Persistent role constraints belong in the AgentDefinition.",
      "Wrong — the coordinator's system prompt isn't visible to subagents (subagent context is isolated). Constraints placed there have no effect on synthesis or report subagents.",
      "Correct — this is the proper division. Persistent specialisation (role, format) lives in the AgentDefinition's system_prompt; per-invocation goals/scope/criteria live in the Task prompt. The coordinator composes the latter; the AgentDefinition holds the former.",
      "Wrong — hooks (Task 1.5) are for deterministic enforcement of runtime invariants (e.g., normalising tool results), not for shaping subagent specialisation. Use AgentDefinition system_prompts for that."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "A coordinator emits three Task calls in one turn — all with prompt 'find recent gen-AI adoption news' — for music, film, and visual arts. Synthesis returns heavily duplicated bullets. What's the diagnosis specific to Task 1.3 mechanics?",
    "options": [
      "Parallel Task execution doesn't actually run concurrently in the SDK; results are processed sequentially.",
      "The synthesis subagent's system_prompt is missing instructions to deduplicate.",
      "The Task tool's input schema does not guarantee parallelism unless an explicit 'parallel: true' flag is set.",
      "The coordinator parallelised correctly (three Task calls in one turn) but failed to partition scope in the per-subagent prompts. Parallel subagents have isolated contexts and cannot deduplicate against each other; each subagent's prompt must give it a disjoint slice (e.g., 'music', 'film', 'visual arts')."
    ],
    "explanations": [
      "Wrong — multiple Task tool_use blocks in one turn run concurrently by design. The latency profile here would already confirm that; the symptom (duplication) doesn't trace to ordering.",
      "Wrong — synthesis can deduplicate textual overlap, but the deeper issue is that all three subagents searched for the same evidence. The fix is upstream of synthesis.",
      "Wrong — no such flag exists. Concurrency comes from emitting multiple Task tool_use blocks in one assistant turn, not a runtime flag.",
      "Correct — this is the textbook coupling of Task 1.2 (scope partitioning) and Task 1.3 (parallel Task calls). Parallel subagents see only their own prompt; without a disjoint slice in each, they converge on the same evidence."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "You emit Task({subagent_type: 'doc_analysis_agent', prompt: 'extract findings'}). Beyond the prompt field, what context does the subagent receive at boot from the coordinator's session?",
    "options": [
      "The full coordinator system prompt, conversation history, and prior subagent results — the runtime stitches them in transparently.",
      "Only the Task call's prompt field. The subagent's system prompt comes from its own AgentDefinition; nothing from the coordinator's runtime context is auto-inherited.",
      "The coordinator's allowedTools list, so the subagent knows what its peer agents can do.",
      "A read-only handle to the coordinator's working memory that the subagent can call into when it needs more context."
    ],
    "explanations": [
      "Wrong — this is the misconception Task 1.3 is built to dispel. There is no auto-stitching; the runtime does not inject coordinator context into Task subagents.",
      "Correct — subagent context is isolated. Boot-time inputs are the AgentDefinition's system prompt + the Task call's prompt field. Anything else the subagent needs must be copied into that prompt by the coordinator.",
      "Wrong — there's no shared visibility into peer allowedTools. Each subagent only knows what its own AgentDefinition exposes.",
      "Wrong — there is no shared-memory handle. All cross-agent state passes through the coordinator (hub-and-spoke); the subagent cannot reach back into coordinator state."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.3"
    ],
    "phase": "post"
  },
  {
    "question": "A web_search MCP tool returns 42 fields per result (raw HTML, redirect chain, headers, scoring metadata) but the agent only needs `title`, `url`, `snippet`, `published_at`, `id`. After six search calls in one session, the agent's attention to the relevant fields drops and it starts citing wrong URLs. Which fix is most aligned with Task 1.5?",
    "options": [
      "Add a `PostToolUse` hook on the search tool that trims responses to the five required fields before they enter the conversation; the model never reasons over the other 37.",
      "Increase the model's max_tokens budget so attention isn't constrained.",
      "Tell the agent in the system prompt to 'ignore irrelevant fields and focus on title/url/snippet.'",
      "Switch to a smaller model whose tighter context window forces it to be selective."
    ],
    "explanations": [
      "Correct — verbose tool output trimming at the `PostToolUse` boundary is a canonical 1.5 pattern (and a Task 5.1 callback). The model only sees the five fields it needs, so attention stays on what matters and provenance is preserved.",
      "Wrong — bigger token budgets don't fix attention dilution; they create more room for irrelevant content to drown out the relevant fields.",
      "Wrong — that's prompt-based filtering, which is probabilistic. The whole symptom (citing wrong URLs after six calls) shows the model is losing focus across long sessions; an instruction won't fix what a hook can.",
      "Wrong — model size doesn't address the structural issue. The fix is upstream of the model: trim the tool output before it enters context."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "A `process_payment` tool occasionally receives an `amount` field with a currency-symbol prefix (e.g., `'$120.50'`) due to upstream prompt formatting drift. The downstream MCP tool only accepts numeric amounts. Which `PreToolUse` hook design is appropriate?",
    "options": [
      "Deny the call with reason 'amount must be numeric' so the model retries on the next turn.",
      "Modify the tool input in the hook — strip non-numeric characters and cast to a number — and let the call proceed. `PreToolUse` hooks can transform inputs, not only allow or deny.",
      "Let the call go through unmodified; the MCP server should validate inputs.",
      "Move the parsing to a `PostToolUse` hook so the original input is preserved for audit purposes."
    ],
    "explanations": [
      "Wrong — denying for a fixable input wastes a turn and can loop if the model regenerates the same format. Deny is for policy violations, not formatting hygiene.",
      "Correct — `PreToolUse` has three operations: allow, deny, or transform. Sanitising the amount before dispatch is the third operation, used exactly when the input needs hygiene rather than policy review.",
      "Wrong — relying on the MCP server to validate trades a deterministic boundary for a remote one and surfaces ugly errors back to the model, which then has to interpret them.",
      "Wrong — `PostToolUse` fires after the tool ran. By that point the call has already failed with the malformed input."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "Which sequence correctly describes the tool-call lifecycle in the Agent SDK, including hook positions?",
    "options": [
      "PostToolUse hook → tool execution → PreToolUse hook → next model turn.",
      "PreToolUse hook → next model turn → tool execution → PostToolUse hook.",
      "Model emits tool_use → PreToolUse hook → tool execution → PostToolUse hook → tool_result appended to conversation → next model turn.",
      "PostToolUse → PreToolUse → tool execution → next model turn (the `Pre` and `Post` names refer to display order in dashboards, not lifecycle position)."
    ],
    "explanations": [
      "Wrong — the order is reversed. Pre fires before execution, Post after.",
      "Wrong — the next model turn fires after the result is appended, not between Pre and execution.",
      "Correct — `PreToolUse` fires before the dispatch (allow/deny/modify); the tool runs; `PostToolUse` fires before the result is appended (transform); the model sees the post-hook result on its next turn. This lifecycle is what determines which hook to use for which concern.",
      "Wrong — the names match the lifecycle exactly. `PreToolUse` is before execution; `PostToolUse` is after."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "A `PreToolUse` hook denies a `process_refund` call with a `reason` field. Where does the model see that reason on its next turn?",
    "options": [
      "The reason is silently discarded; the model sees only that the call returned no result.",
      "The reason is logged for ops dashboards but never reaches the model.",
      "The runtime injects the reason into the coordinator's system prompt for the next turn.",
      "The reason is delivered as a `tool_result` (marked as an error) tied to the original `tool_use_id`, so the model reads it on its next turn and can adjust by calling an alternative tool the reason names."
    ],
    "explanations": [
      "Wrong — silent denies would leave the model spinning. The runtime surfaces the reason precisely so the model can correct course.",
      "Wrong — logging only would defeat the redirect pattern. The model needs the text on its next turn for the deny to be productive.",
      "Wrong — the system prompt is static. Runtime hook outputs flow through `tool_result` blocks, not by mutating the system prompt.",
      "Correct — the deny reason is delivered as a `tool_result` tied to the original `tool_use_id` (Task 1.1 mechanics). The model reads it on the next turn and emits the alternative call the reason described."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "A `PreToolUse` hook on `process_refund` returns `{permissionDecision: 'deny', reason: 'policy'}` when amounts exceed $500. In production, the agent now responds 'I'm unable to help with that, please try again.' Customers are confused. What's the architectural fix?",
    "options": [
      "Rewrite the deny reason as a corrective instruction that names the redirect (e.g., 'Refunds over $500 require manager approval; call `escalate_to_human` with a structured handoff payload'). The reason becomes the model's next-turn instruction.",
      "Remove the $500 cap so the agent can always self-serve.",
      "Catch the deny in a `PostToolUse` hook and rewrite the user-facing message there.",
      "Add a few-shot example in the system prompt showing the agent how to respond gracefully after a deny."
    ],
    "explanations": [
      "Correct — block without redirect is the anti-pattern. The reason field is the model's next-turn input; making it a concrete corrective instruction (which alternative tool to call, with which payload shape) lets the agent recover the customer's intent through an allowed path.",
      "Wrong — removing the cap reverses a deliberate policy decision and creates a compliance incident.",
      "Wrong — `PostToolUse` does not fire on denied calls (no execution happened). Even if it did, rewriting user-facing text isn't where the fix belongs; the fix is to give the *model* a redirect.",
      "Wrong — few-shot examples are probabilistic. The deterministic fix is structuring the deny reason itself as a corrective instruction."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "A team debates where to enforce a 'no refunds over $500' rule: (a) inside the `process_refund` MCP tool's own logic, returning an error from the tool, vs (b) in a `PreToolUse` hook that denies the call. Which is better, and why?",
    "options": [
      "In-tool error is better — it keeps policy close to the domain and reduces the hook surface area.",
      "A `PreToolUse` hook is better — policy enforcement stays in the agent harness (one auditable place), the offending call is blocked before it dispatches (no network round-trip, no partial side effects), and the agent layer can evolve the policy without redeploying the MCP server.",
      "They are equivalent — both stop the action and both surface a message to the model.",
      "Implement both — defense in depth always wins."
    ],
    "explanations": [
      "Wrong — coupling policy to each tool spreads enforcement across N MCP servers, fragments audits, and ties policy to deployment cadence of every server.",
      "Correct — the harness-side `PreToolUse` hook is the proper enforcement boundary. It is centralised, auditable, evolvable without MCP server redeploys, and (crucially) prevents the call from dispatching at all — the alternative leaks the attempt downstream.",
      "Wrong — they have different blast radius. In-tool enforcement still triggers the call (network, possibly partial state changes); the hook prevents dispatch entirely.",
      "Wrong — defense in depth has its place, but the question asks which is the right architectural enforcement boundary. The hook is the answer; adding both adds complexity without changing the deterministic outcome."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "A team wants the agent's customer-facing messages to consistently use a friendly, concise tone. An engineer proposes a `PostToolUse` hook that detects unfriendly phrasing and rewrites it. What's the better approach and why?",
    "options": [
      "Implement the proposed hook — deterministic enforcement is always preferable.",
      "Implement two hooks — one for tone, one for redundancy in case the first misses.",
      "Use the system prompt to specify the tone. Style and preference are exactly the class of concern that probabilistic compliance handles well; reserve hooks for compliance-grade requirements (policy caps, prerequisite gates, data normalization, sensitive-field redaction).",
      "Train a small classifier model and route every agent message through it."
    ],
    "explanations": [
      "Wrong — hooks for stylistic concerns over-engineer the problem. Tone variance is acceptable; policy violations are not. Hooks shouldn't be the default for every concern.",
      "Wrong — two hooks for the same probabilistic concern is two points of failure for a problem that the system prompt would handle adequately.",
      "Correct — Task 1.5's framing is explicit: hooks for deterministic guarantees, prompts for probabilistic style/preference. Tone fits the latter; reaching for a hook here is over-engineering and unnecessary infrastructure.",
      "Wrong — adding a classifier is heavier engineering than the original problem warrants. The right answer is the system prompt."
    ],
    "answer": 2,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "An engineer plans to block `process_refund` calls in a `PostToolUse` hook by checking the response and 'rolling back' if `amount > 500`. Why is this the wrong choice?",
    "options": [
      "`PostToolUse` hooks cannot read tool inputs, so the cap can't be enforced there.",
      "`PostToolUse` fires only on successful calls, so it can't block.",
      "The Agent SDK doesn't allow any hook to roll back side effects.",
      "`PostToolUse` fires after the tool has executed — the side effect (refund issued, money moved) has already happened. Blocking belongs in `PreToolUse`, before dispatch."
    ],
    "explanations": [
      "Wrong — `PostToolUse` can read both the tool input and response. The issue is timing, not access.",
      "Wrong — `PostToolUse` fires for both success and failure responses. The deeper issue is lifecycle position, not result filtering.",
      "Wrong — over-general. The real problem is that the *external* side effect (the refund processed by the payment system) has already happened by the time `PostToolUse` runs.",
      "Correct — `PostToolUse` is too late for a block. By the time it fires, the refund has been processed. Compliance enforcement that prevents side effects must happen in `PreToolUse`, before dispatch."
    ],
    "answer": 3,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "You have five MCP tools that each return timestamps in their own format. You want all five normalised to ISO 8601 UTC. Two designs are on the table: (a) one shared `PostToolUse` hook that branches by tool name, vs (b) five separate `PostToolUse` hooks each pinned to one tool. Which is the better choice for maintainability?",
    "options": [
      "One shared hook — the canonical format is defined in one place, evolution is centralised, and audits trace through a single piece of code. Branching by tool name (or by field heuristics) is straightforward.",
      "Five separate hooks — each is simpler in isolation, and a bug in one doesn't affect the others.",
      "They are equivalent; pick whichever the team prefers stylistically.",
      "Neither — timestamp normalisation should always happen in the MCP server itself, not in agent hooks."
    ],
    "explanations": [
      "Correct — a single shared hook with a tool-name branch is the maintainable design. The canonical format is defined once; updating it (e.g., changing how UTC offsets are handled) doesn't require touching five files. Audits and reviews see one code path.",
      "Wrong — N parallel hooks invites code drift. The first time the canonical format changes, the team has to remember to update all five places consistently.",
      "Wrong — they aren't equivalent on maintainability. Duplicated logic is the worse path; centralising the format in one branch-by-tool hook is strictly better.",
      "Wrong — pushing normalisation to MCP servers fragments the canonical-format definition across server-owner teams. Keeping it in the agent harness is what gives you a single source of truth."
    ],
    "answer": 0,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  },
  {
    "question": "A `lookup_customer_full` MCP tool returns the customer's full SSN and home address even when the agent only needs name and email. Compliance requires those fields not enter the conversation log at all. Which design satisfies the requirement?",
    "options": [
      "A system-prompt instruction telling the agent to 'ignore the SSN and address fields and never reference them in responses.'",
      "A `PostToolUse` hook on `lookup_customer_full` that strips the SSN and address fields from the response before the `tool_result` enters the conversation. The model never sees them, and they never appear in logs.",
      "A `PreToolUse` hook that denies every call to `lookup_customer_full` and redirects to a different tool.",
      "Manual review of conversation logs after the fact, redacting SSN and address before logs are exported."
    ],
    "explanations": [
      "Wrong — prompt-based 'ignoring' is probabilistic. The fields still enter the context and the conversation log even if the model never quotes them. Compliance requires they not enter the log at all.",
      "Correct — `PostToolUse` runs before the `tool_result` is appended to the conversation. Stripping sensitive fields there means they never enter context, never appear in logs, and the model has no way to reference them. This is the deterministic, compliance-grade fix.",
      "Wrong — denying every call denies the agent's ability to do its job. The agent legitimately needs the name and email; the fix is field-level redaction inside the response, not call-level denial.",
      "Wrong — post-hoc log redaction can't undo that the data was sent to the model and could have been quoted in earlier responses. Pre-context filtering is the only deterministic fix."
    ],
    "answer": 1,
    "topics": [
      "Domain 1",
      "1.5"
    ],
    "phase": "post"
  }
]